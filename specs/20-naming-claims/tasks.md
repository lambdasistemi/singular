# Tasks

One slice: first-release naming claims.

- [ ] T-lean-layer: Naming Lean module with fixtures, well-formed Insert, `namingStep` / `namingResolve`, and `naming-no-delete`.
- [ ] T-lean-statements: Naming statements in a separate module and inventory; generic forty-one records unchanged.
- [ ] T-lean-proofs: Every naming statement `PROVED` from standard axioms; naming corpus exported without changing the generic corpus bytes.
- [ ] T-sim-engine: `simulator/naming.mjs` transcribes the frozen naming layer; identity lists both labelled profiles.
- [ ] T-sim-journey: Headless replay queues two `alice` claims, folds the first to Active, refuses the second with `occupied-key`.
- [ ] T-sim-delete: Crafted generic Delete against `namingStep` refuses with `naming-no-delete`; generic profile still Deletes.
- [ ] T-docs-page: `docs/naming-demo.md` is the playable entry, with diagrams, implemented-versus-pending labels, finite-model limits, and an unaccepted-candidate statement. Speech restamped. Reserved files untouched.
- [ ] T-preview: Candidate-bound preview identity via existing preview job; served bytes include the naming profile.

## Slice

`S-naming-claims` — OWNER. Gate: untracked `./gate.sh`. Final subject:
`feat: make first-release naming claims playable in the docs`.
