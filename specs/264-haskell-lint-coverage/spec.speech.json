{
  "requirements": [
    {
      "text": "The contributor's existing lint command now discovers active Haskell sources and rejects a malformed source in a newly covered directory. Duplicate Cabal dependencies were removed. Public exports and executable names remain stable. The classified carrier compiles only its supported set and names every retained command it does not verify.",
      "pause": 400
    }
  ],
  "source-inventory-at-intake": [
    {
      "text": "The intake inventory maps the library, fifteen executables, and three test suites to their Cabal source directories. The direct GHC naming test and drift sources join lint discovery separately. The final boundary names the two formatter exclusions and the HLint debt instead of hiding them.",
      "pause": 400
    }
  ],
  "evidence-limit": [
    {
      "text": "The root build gate never stood for all offchain Cabal components. The classified carrier inventories all nineteen declarations and compiles ten included members used by required workflows or currently supported commands, plus the library and tests. Nine retained commands remain unverified with issue owners. Its successful build does not execute a command or prove a package-wide build.",
      "pause": 400
    }
  ],
  "candidate-boundary-and-epic-completion": [
    {
      "text": "The reviewed source revision inventories seventy Haskell files in twenty-two directories. Fourmolu checks sixty-eight files and preserves two independent verifier files at intake bytes. HLint checks fifty-seven files in nine directories; thirteen files are outside that run under thirteen configured debt-directory names. Older measurements of two hundred fourteen and two hundred eighteen hints belong to earlier snapshots, not this head. Final epic issue 278 owns those lint and format gaps and all other repository code. The classified carrier built ten members and leaves nine retained commands unverified. Direct failures at connected verifier, recovery rows and li zero one remain historical revision-bound evidence; issues 172, 282 and 283 own the distinct repairs.",
      "pause": 400
    }
  ],
  "a-011-forward-correction-to-current-release-instructions": [
    {
      "text": "The operator ruled the old release README outdated and directed a forward correction. Current and future instructions distinguish verified commands from retained legacy commands; already published archives remain unchanged. The assembled archive checker and four checksum-valid negative controls enforce the corrected availability promise. The earlier documentation correction passed its own gate and exact-head CI on the old main base. Main now binds accepted Lean source 03fd9e0 and constitution version 1.10, whose retraction admission is outside this ticket's bounded fold journey. The rebased head needs fresh audit, gate and exact-head CI. Nothing here makes an unbuilt legacy command work or verifies retraction behavior.",
      "pause": 400
    }
  ],
  "_source": {
    "file": "spec.md",
    "sha256": "4f5bc3e43ddda0ac644f17a7ca1546041aa561145464b369a07123544facf364"
  }
}
