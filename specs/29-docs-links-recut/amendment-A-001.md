# Amendment A-001 — pending-publication class authorized, auto heuristic rejected (parent ruling, no original rewrite)

Parent answer `../answers/A-001-publication-state-and-budget.md` read FULL; Q-001 + BLOCKED-exec6.md acknowledged as owner evidence (not audit). No merge/tag/release.

## Budget (authoritative, replaces plan.md ceilings)

Incremental 20→21, lifetime 35→36, owner 7→8; initial-audit 7 + repair-audit 6 preserved. Owner spent 6/8; exec7 = corrected prefix/control rerun, exec8 = full changed-checker gate. Denominator is 8 prospectively (no retroactive `8/7`). Audit launches 0/2, submissions 0/2; historical 15/3/2 immutable. Any further need returns receipts first; no bundled invocations.

## Scope (amends spec.md/plan.md acceptance, originals preserved)

New explicit result `pending publication` (`absentUntilRelease` machine name OK): route proven candidate-introduced AND absent from a verified older production deployment. Meaning: NOT YET PUBLISHED — never PASS, never future-proof. Keep every route in denominator with source, requested/final URL, HTTP outcome, candidate identity, comparison deployment identity, classification reason.

REJECTED: `merge-base HEAD @{upstream}` + `ls-tree` heuristic (upstream is planning head not deployed evidence; URL routes ≠ repo paths; absence-alone misclassifies typos/base pages).

REQUIRED mechanism: deployment-bound evidence. Rendered route inventories tied to respective identities + explicit source→rendered-route mapping (index/trailing-slash/fragment). Deployed ref explicit from publisher/marker/artifact evidence (cf. preview `candidate.txt` marker pattern already proven); `auto` must obtain equivalent evidence or return UNKNOWN/BLOCKED. Production identity unestablishable → provenance UNKNOWN/BLOCKED, evidence deferred to required post-publication check; never fabricate pending-publication, never block preview/archive collection. Phase-split acceptance, not waiver.

Candidate-new validity: in candidate built inventory AND resolves on EXACT candidate preview AND archive server. Existing-production 404s fail; arbitrary missing fail; missing anchors fail; network/unknown blocked. Production-only pending never excuses preview/archive failures. Once production IS the candidate, pending disallowed. Discrimination tested in existing prefix/control recipe; served-404 + prefix-escape kills preserved.

## Build/preview sequencing (authorized)

Stage owned untracked specs/29 files in worktree before building (reversible, no push/acceptance); honest dirty/precommit source identity, final evidence bound to committed candidate. Push of UNACCEPTED draft preview authorized once coherent (existing brief authority); stale-preview absence = pending evidence only. Merge-ready requires: current preview + archive checks, all existing production routes assessed, explicit production-only pending/unknown rows. Post-merge: verify remaining production obligations before user-task completion.

Fences hold: full reader coverage (no ticket-page exclusion), no release-workflow/deployment-subsystem work, smallest evidence-bound comparison, gate wrapper/oracle hashes unchanged, no silent overwrites. Sol inspects fail-closed boundaries + exact candidate evidence.
