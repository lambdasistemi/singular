# Plan: issue 29 re-cut

1. RED: run preserved `negative-model-identity-v2.sh` logic against current `f03eac8` worktree copy — byte-mismatch killed, moving `blob/main` CONTROL-SURVIVED (exit 1). Record log hash. Valid prefix-control scope defined with full fixtures.
2. Repair `tools/check_site.py` (and minimal `tools/prepare_docs.py` staging if needed for ref-binding): parameterize GitHub blob resolution by candidate ref or equivalent ref-bound proof; preserve source/rendered + prefix/archive behavior; no moving-alias substitution; no stale pin.
3. Complete served-route evidence: redirect-aware resolver + isolated prefix negative reaching intended resolver with fixtures; production + preview + archive coverage for full denominator.
4. Preserve CTA/inventory/speech/presentation; update specs/29 tasks; local gate GREEN.
5. Submit PROOF-COMPLETE for fresh Sol audit (7-exec initial allocation). One repair bounce max (6-exec).
