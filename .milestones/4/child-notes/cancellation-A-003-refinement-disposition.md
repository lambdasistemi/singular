# Proceed with the model-preserving repair; remove the new signer condition

Read refund-binding-refinement.md in full; desk acknowledgement POINTER-1789400087-3398868. Bound source/model f558d0e8fc916eef494fffcef09cfe2ac5582b8e. Proposal SHA c2e7df2e9375359eefd4b7c847b25c7209ddc789336cc2f66d1c74cf2fd7676d matches; refinement SHA91f68e0348aa8c3d553b60e211a1a5f5640e4c5360d0657ee33c6c6e7dd5cc5e. Desk inspected preservation-probe-v2.log (SHA c53d25b01c9fbb06470e8a473b283df0166b08904032b964f5aef0c4f2cb3ff5), both named contexts passed. Earlier zero-test invocation remains excluded. These are component observations only.

## Authority and disposition

This supersedes A-002's blanket representation hold and the desk's earlier atomic-registration permission question. No operator answer is inferred. Fresh reading of constitution section II explicitly says: "No approval is needed to repair code that contradicts clear Lean. Technical representation choices may proceed when their behavioral correspondence is explicit and checked; a difference in representation alone is not a model error."

The refinement now makes that correspondence concrete after removing the unnecessary new request-owner creation signer. No new lifecycle/economic ruling is required for the bounded implementation below. Proceed in the existing #117/PR118 lane. Do not seek another generic approval for the same technical representation.

1. Preserve the existing controller authorization for Insert mint. DO NOT add a request-owner signature requirement to creation. A funding witness required by a transaction's chosen inputs is not a new protocol role. Retraction still requires its existing request-owner signer and phase-2 interval.
2. The new versioned Insert approval may use the proposed canonical Plutus Data preimage: domain; consumed seed outref; registry state-policy/token identity; co-created native request output index; exact request address and existing datum; immutable full refund address; existing four-field naming datum. Co-create request and claim in the registration transaction. This refines the abstract authenticated request/claim association and immutable proposal.refundAddress; no new modeled intermediate state or approval-conversion step is introduced.
3. Fix the expected applied native-request script hash as an application instantiation parameter. Derive it from the pinned native blueprint plus state-policy/cage-token parameters using the existing applyRequestParams/hashScript route. Check that credential at mint and both consuming paths, along with exact origin/index, datum, registry identity and required native redeemer. Bind construction/identity checks to actual applied artifacts. Do not accept a caller-redeemer/manifest assertion as validator authority. This implements Model.insertNative's request.destination == config.requestAddress, not an administrator or mutable setting. Necessary artifact-construction/manifest instantiation code belongs to the minimal repair; this is not the economic deposit parameter.
4. Make Maintain/Recover require actual representative custody under the expected registry policy, preserving their other accepted checks. NamingLifecycle.maintainDestination/recoverController both resolve namingRecord/recordApplication and validateLifecycleOutput; pending claims do not satisfy this. The currently accepted pending Maintain context is therefore evidence of a code/model mismatch, not authority to preserve claim relocation. Record and check the stationary pending-claim invariant before relying on current claim txid plus request index. Preserve Active maintenance/recovery positives and generic moveAction's existing abstract scope.
5. Compose exact request Retract and claim cancellation, consume/burn the pair with no representative mint/root mutation and refund to the full committed address. Do not introduce new deposit economics, weaken fees/value preservation, or conflate a generic retraction alone with completed naming cancellation.

Before dependent acceptance, add the smallest executable representation statement/example showing the validated pair maps to the same model requestId/refund; corresponding cancellation removes request and claim while preserving records/root; pending maintenance/recovery refuses and Active positives remain. Check mismatched request/refund/native credential using the already commissioned focused route. These checks establish the declared mapping, not a new assurance campaign. If implementation exposes an actual behavioral contradiction beyond this mapping, return the concrete case before changing the model or expected result. Ordinary technical details proceed without repeated proposal cycles.

## Compatibility and delivery limits

Old application addresses and old approval tokens remain governed by old scripts; no new builder can grant them this path retroactively. Preserve this fact in PR/release/interface documentation. #117 can deliver corrected artifacts, production builders and isolated-devnet acceptance. It does not authorize a migration, replacement registry, signing identity change or shared-preprod transaction. Do not claim #114's existing-deployment connected cancellation is complete after this repair lands. Desk keeps that dependent acceptance open and owns the compatible-deployment follow-up/decision; do not widen this ticket or create a migration yourself.

Keep WithdrawApproval as the existing compatibility component route, never proof for connected Insert cancellation. Integrate accepted #110 before final acceptance; report only concrete overlap requiring desk resolution. CLI #114 consumes the implemented builder/receipt after landing. Deposit model lock-point Q-001 remains pending and separate.

Acknowledge RESUMED A-003 with existing signer rules, explicit applied-request hash binding, Active-only custody and old-deployment limit. Proceed through baseline node reproduction, minimal code/model mapping, real devnet positives/refusals and current-head CI. One worker/one PR, no new audit or campaign. No merge until desk sequencing and owner merge-guard. Update your recovery/interface and PR body to reflect this exact disposition.
