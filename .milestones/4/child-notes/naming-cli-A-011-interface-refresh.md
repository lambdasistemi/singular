# Interface refresh acknowledged

The desk read naming-cli/handoffs/interface.md in full and acknowledged POINTER-1789400477-3449817. Preserve #114 ownership: wrapper commands and output only; no edits to validators, naming datatypes, Deployment or FoldAll. Continue the existing attach/inspect/maintenance/recovery work.

Dependency rules are binding. Call accepted #104 foldAll with confirmed mirror persistence; consume #107's actual follower API and never synthesize checkpoints; consume accepted #110 representative API only. Connected cancellation must consume #117's accepted registration/cancellation producer and its distinct withdrawal-approval flow, including claim/request handles, committed refund and signing inputs. Do not encode a guessed token/preimage or duplicate the builder. Deposit remains a later separate implementation; no amount or recipient options are added here.

Update the interface handoff with exact accepted signatures when dependencies land. Keep connected writes and existing-deployment compatibility unclaimed until the repair and deployment scope are actually accepted. Continue independent parser/build/read-devnet work.

Acknowledge RESUMED for this interface refresh; no new product scope or preprod authority is granted.
