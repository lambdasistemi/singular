# Answer: additive ownership now; accepted integration before landing

Your question, proposed issue and command map were read in full. The proposed coherent wrapper is authorized as a dedicated ticket. File it with the current corrections below, freeze that issue body, open the labeled/assigned draft PR, and proceed. No further issue-filing approval is required.

## Shared paths

You own the new CLI/library/test paths and additive declarations uniquely named for singular-naming and naming-cli-e2e in offchain/singular-registry.cabal, offchain/flake.nix, the actual current registry CI workflow, release packaging/checks, justfile and documentation navigation. Those additive edits may proceed NOW in your isolated branch; waiting for every prerequisite to land merely to add a cabal/app stanza is unnecessary. Preserve sibling declarations; no renaming, deleting or recutting their jobs/commands. You own the new user manual/command documentation, with narrow navigation additions.

Existing journey/builders/deployment or naming datatype behavior shared with M1/#104 remains with its current owner until that change lands. Develop your extracted user-parametrized routines in new production modules against accepted main; do not cherry-pick an unaccepted sibling or edit its worktree. Consume the actual accepted #106/#110 API before finalizing those adapters. If additive declarations conflict when main moves, integrate the landed main in your lane and resolve only your own additions while retaining the sibling entries; that is routine owner work, not another permission round. A required change to somebody else's behavior or interface goes back to the desk as a concrete question.

## Dependency and landing order

- M1 owns #106 then the remaining #110 integration/close; do not take it over. Use their actual merged manifest and representative-policy identities before final acceptance.
- #104 implementation is independent and continues now; its existing preprod requirement uses the later M1 handoff. It owns adaptive batching.
- #107 dispatches as soon as #106 merges, independently of #104. It owns chain reconstruction.
- Your existing-lifecycle wrapper can develop now. Its final acceptance and landing consume merged #106, #110, #104 and #107. Do not claim follow/fold-all integration with a speculative or unaccepted adapter.
- Pricing does NOT block this wrapper ticket's existing-lifecycle acceptance. The separate deposit ticket owns protocol support AND the subsequent real deployment-parameter/refund-address integration into singular-naming after your wrapper lands. That later diff completes the additional user pricing story. No inert flag and no hidden future-work completion claim.

## Settled pricing correction before filing

The operator has now specified the refund destination: an address committed at registration. The amount is a deployment parameter and the deposit becomes refundable only at Over. Read NOTE-003 and the updated ruling; replace the obsolete "recipient unresolved" text in your proposal/map before filing. Price code is not part of this wrapper's frozen acceptance. A separate protocol owner is commissioned at /tmp/projects/singular/milestone-4/naming-deposit and will provide its actual API contract through the desk.

## Pairing adapter

Explicit claim/request references are acceptable transaction inputs if source evidence confirms they are necessary; a register command must report the references it creates clearly. Do not add an indexer or pick ambiguous claims arbitrarily. The folder's normal user journey should consume emitted references or an explicit mapping file without forcing users to understand test-runner internals. Keep the distinction visible between the node-rebuilt registry mirror and any pending-request attachment inputs. If this makes the already frozen manifest-only fresh-checkout outcome impossible, raise that precise data-discovery gap before claiming the milestone outcome; do not silently weaken it.

Acknowledge RESUMED Q-001-shared-files-and-deposit-timing and continue. Preserve your own current resume and report actual issue/PR URLs. Shared preprod remains pending the verified M1 handoff and a desk-assigned writer slot; none is granted by this answer.
