# Q-002 — second behavioral failure; return for re-cutting

The ticket brief says: "A failed ticket twice means return for re-cutting, not a third campaign." I am treating the two observed behavioral verification failures as reaching that stop condition. No third repair campaign, new audit, or preprod submission is started.

Result: adaptive folder implementation and node acceptance are present in draft PR https://github.com/lambdasistemi/singular/pull/113, remote 80c7b69d95d062d6545fcd19945b56f7f7a0bbcc. Local follow-up 572f26d706827b0159042eb6edd17b4038e4e2de adds --fold-only for a supplied registration name, avoiding the full journey's already-occupied fixed fixtures on preprod. It is committed, clean, and passed two independent attached devnet runs; it has not been pushed after diagnosing the stop condition.

1. First failure: full normal E2E at 2b78dc5 encountered TimeTranslationPastHorizon at slot1500, the exclusive horizon1500. Repaired at252d17d using a floored validity bound and fatal ContextError handling. Full repaired E2E passed10/0, missing-loop control failed the intended chain-drain assertion, journey passed11/11, units106/0.
2. Second failure: PR113 CI at80c7b69, retirement job104035189176, N2 strict refusal attribution cannot extract a script-hash field from `show err`. This is attributable to the new derived Show instance for FoldBuildFailure: the reason was already rendered by the transaction builder, and deriving Show wraps/escapes it again. The runner's parser accepts the previous raw/once-escaped field shapes. Existing main f558d0e has a green Registry run34860361911; retirement source and Node are unchanged in this PR. The node's refusal itself occurred; its required attribution check failed and remains red.

Evidence: evidence/ci-retirement-80c7b69.log, line288; remote https://github.com/lambdasistemi/singular/actions/runs/34861417759/job/104035189176. Source: ConnectedFold.hs FoldBuildFailure deriving Show; retirement/Main.hs rowN2Mismatch uses pinEvalRefusal (show err), whose extractScriptHashes recognizes the existing field shapes. No validator, model or expected-result change is proposed.

Concrete suggested re-cut: retain the typed exception for loop control but give it an explicit Show instance preserving the original `connectedFold: build failed: ` message and rendered reason verbatim. A reviewable un-applied patch is handoffs/refusal-rendering-proposal.patch. Verify with the existing retirement runner (including the N2 wrong-reason control), the adaptive positive/control, and current-head CI; no extra assurance campaign. The patch has NOT been applied or executed.

Other evidence: adaptive CI job passed at80c7b69; full registration devnet passed after foldAll integration; attached-folder-smoke exit0 at572f26d, two processes loaded/advanced the same manifest/mirror and deployment verify passed8claims. All receipts are indexed in handoffs/verification.md.

Decision needed from desk: disposition/re-cut under the brief's two-failure limit, including whether to resume this preserved lane for the proposed bounded repair. I recommend preserving this lane and limiting the re-cut to error-rendering compatibility; do not weaken retirement attribution. Existing Q-001 preprod handoff remains pending independently. No further user/project grant is needed for preprod once the already-settled handoff condition and desk writer assignment are actually met.
