# Preserve the modeled outcome; compile the missing binding before a new design ruling

Q-001 read in full; desk acknowledgement POINTER-1789399542-3277012 is in milestone STATUS. The missing authenticated association is real. Neither an arbitrary reference input nor a controller-selected destination satisfies the existing committed-refund promise. The desk has NOT authorized a new hash preimage, fifth datum field, token constructor or extra approval transition.

Existing authority resolves part of the question. At f558d0e, NamingLifecycle.cancelNamingClaim checks the exact request's committed refund, calls Model.step .withdraw, removes that request via consume and removes the matching claim while preserving records. A successful connected cancellation must therefore dispose of that exact pending request and claim, with no remaining request capable of activating the name. A sequence that merely destroys a claim or retracts a request cannot be presented as this completed transition. Do not invent an intermediate economic refund rule.

The existing production retract boundary is also concrete: onchain/validators/request.ak validateRetract (lines84 onward) is Insert-only, requires the request owner's signature and the phase-2 interval; offchain/lib/Singular/Registry/TxBuilder/Retract.hs builds that interval from submittedAt + processTime through retractTime. This repair does not authorize bypassing/changing those rules or immediate all-times withdrawal. Prefer composition in one transaction using the existing Retract boundary if implementable. If an inseparable conflict with the frozen outcome appears, give the exact evidence and user-visible consequence before affected implementation. This is preservation of the existing boundary, not a new timing ruling.

Your three broad representation directions are not yet a concrete design to approve. Continue the already authorized source/probe/devnet work and prepare handoffs/refund-binding-proposal.md: recommend ONE minimal binding, with exact authenticated fields and establishment time; how it binds registry, exact request and immutable refund without a circular transaction-ID dependency; mint/fold/cancel validation and existing signers; whether claim and request are created together or an existing transition establishes the association; transaction disposition; overlap with #110; and the smallest model statement/example needed to express it. State user-visible changes separately from encoding details. Do not implement the new representation while preparing this.

Do not shift byte encoding choices to the operator as an open-ended technical questionnaire. The desk will examine your concrete recommendation against the settled semantics, then seek an actual operator/model ruling for any new representation/transition required by the mandate. No operator answer is inferred from elapsed time or the instruction to investigate. The separate deposit-model lock-point question remains pending and unchanged.

Issue/draft PR intake can freeze the existing bug outcome with design choice explicitly pending; reproduction, additive tests and interface handoff continue now. Acknowledge RESUMED Q-001 limited to proposal and unaffected work, while affected representation/authorization implementation stays held. Publish the issue/PR identities and proposal promptly; no extra audit owner or campaign.
