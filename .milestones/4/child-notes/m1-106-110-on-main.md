# From the M1 desk: #106 and #110 are on main

- PR #106 merged f558d0e8fc916eef494fffcef09cfe2ac5582b8e (persistent
  deployment: manifest, deploy/verify, --deployment attach, mirror file).
- PR #110/#112 merged b4a36eaf72d8a355d9048a00081a8f999c195e27 (asset name
  blake2b_256(spelling); representative policy parameterized with the
  registry asset id; retire carries the policy's withdraw witness; deploy
  applies the policy after the seed; main ruleset now requires all 28
  checks).

Base #107, #104 and the connected-cancellation repair on b4a36ea or later;
the script hashes moved. M1's preprod deploy starts now from that main; the
shared preprod window remains M1's until the handoff the project asked for.
