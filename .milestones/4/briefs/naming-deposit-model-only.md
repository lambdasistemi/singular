# Operator correction: FIRST ticket is the executable model only, in parallel

The operator has just explicitly instructed: "so first ticket is update the model to accomodate that, run it in parallel".

This immediately narrows your first ticket to the Lean model. Do NOT file the combined model/validator/builder/CLI ticket described in the initial brief. If no issue is filed yet, file a model-only issue and one draft PR. If one was just filed, stop before implementing beyond the model and report its identity to the desk so the ticket boundary is corrected explicitly rather than silently widened. No product implementation beyond the Lean model is authorized in this first ticket.

Your model must accommodate the settled operator rules: registration deposit amount is a deployment parameter; the deposit stays locked while the name is held; it becomes refundable when the naming lifecycle reaches Over, to an address committed at registration. Read /tmp/projects/singular/milestone-4/rulings/2026-09-14-naming-artifact-and-deposit.md. Preserve all existing naming semantics outside that change. Raise any actual ambiguity from the existing transition definitions as a concrete user story; do not invent an economic amount, alternative refund recipient, adjustable administrator, or early refund.

Owned files for this first diff: affected lean/Singular model definitions, relevant executable statements/examples and necessary model runner/corpus serialization or explanatory model documentation. New behavior must be executable through the existing Lean/model entry point; name that exact command in the issue body. Positive examples exercise configured amount, committed recipient and Over refund; relevant negative examples demonstrate insufficient deposit, early release and redirected refund are refused in the model. These are model observations, not on-chain evidence. Run the actual model command and applicable existing CI, preserve candidate/model revision and meaningful failing-control evidence. Avoid whole-model coverage or new assurance campaigns.

Forbidden in this first ticket: validators, production offchain builders, deployment manifests/script identities, user CLI pricing flags, preprod writes and new deployments. The implementation follow-up is a separate ticket after this model change is accepted and merged. Your current commission ends with the model ticket's delivered diff and executable evidence; do not automatically begin the implementation follow-up.

Run now IN PARALLEL with #104 and naming-cli; they retain their existing work. No dependency on #106/#110 is required merely to model the new deposit, though preserve the current Lean authority and identify actual model overlap before editing. One Codex gpt-6-astra/high worker/one issue/one PR, no children/auditors/reseats. Rename your window to singular-ms4-t<issue>-deposit-model when the issue exists. You may use your already assigned isolated worktree/branch or choose a model-specific branch at bootstrap; record actual identity in your resume.

Your initial broad implementation acceptance is superseded before ticket filing by this explicit operator correction. Draft the model-only acceptance, file it with feat/paolino/milestone 4/planner, open draft PR, update the model, verify, and report readiness. Normal guarded merge sequencing still belongs to the desk; owning lane executes merge-guard after green CI. Do not wait for another go. Journal NOTE NOTE-001 read immediately, with model-only scope and whether any issue or non-model edit had already occurred.
