# Epic 17 owner — resume record

Current as of 2026-09-12T19:05Z. Phase: `ddfc4e9` is ACCEPTED as a component and
frozen; the next combined revision is in flight at `%990`; the Fork slice is in
flight at `%995`. Earlier headers describing an in-progress `617e434` or `fe89e68`
acceptance are obsolete — those campaigns are superseded and their dispositions
are in `handoffs/acceptance-evidence-index-ddfc4e9.md`.

## Seat

```
/run/current-system/sw/bin/claude --dangerously-skip-permissions --model 'claude-opus-5' --effort high
```

| field | value |
|---|---|
| my pane | `%913` (`singular:1.1`) |
| my window | `singular:singular-e17-t77-representative-fold` |
| session | `singular` |
| parent desk | **Codex, pane `%988`, `singular:3.1 singular-ms1-onchain-keri`** |
| runtime root | `/tmp/projects/singular/milestone-1/epic-17` |
| epic checkout | `/code/singular-e17` |
| staffing | GLM/Muse mechanical only; **no auditors, no native subagents**; at most the four authorized implementation slots; counters carry across runs — a new run never resets spend |

Route all reporting and cross-epic requests through pane `%988`.

### Roster

| worker | pane | state |
|---|---|---|
| `ticket-62/commit-owner` | — | retired, merged |
| `ticket-66/commit-owner` | — | retired, merged |
| `ticket-74/commit-owner` | — | retired, held; worktree `/code/singular-e17-issue-74` RETAINED |
| `ticket-77/commit-owner` | %979 | retired (capacity handback at `46d2e25`); in `.retired-workers` |
| `ticket-79/commit-owner` | — | retired, merged |
| **`ticket-77/commit-owner-2`** | **`%990`** | **LIVE and PARKED at `PROOF-COMPLETE`** — muse, `harness=pi provider=opencode-go model=muse-spark-1.3-contributor effort=xhigh`, worktree `/code/singular-e17-issue-77`, START 13:09:51Z, PROOF-COMPLETE 14:26:44Z, idle at 44.5% of 1M context. **Do not relaunch, reset or duplicate it.** It is awaiting my verdict; no question is pending (Q-001 closed by A-001, `RESUMED` recorded) |
| **`ticket-81/commit-owner`** | **`%995`** | **LIVE, the fourth slot** — glm, `harness=pi provider=zai model=glm-5.3-flash effort=max`, worktree `/code/singular-e17-issue-81`, branch `fix/fork-absence-insert-81` on `5bd7c79`, START 17:48:26Z. The Fork absence-insert repair (#81) per NOTE-049. Launch: `glm --approve` |

Launch line to replay only if `%990` actually dies:

```
muse --approve      # wrapper pins provider=opencode-go model=muse-spark-1.3-contributor effort=xhigh
```

## Children

| child | state |
|---|---|
| [#62](https://github.com/lambdasistemi/singular/issues/62) recovery `LR01..LR11` | merged `051d65e` |
| [#66](https://github.com/lambdasistemi/singular/issues/66) retirement initiation | merged `ad071d5` |
| [#79](https://github.com/lambdasistemi/singular/issues/79) imported-validator repair | merged — PR [#85](https://github.com/lambdasistemi/singular/pull/85), head `bf9f42d`, merge `56e0fcd`, 40 checks green, issue closed. Consumer handoff: `handoffs/79-to-epic18.md` |
| [#77](https://github.com/lambdasistemi/singular/issues/77) real representative + fold into `Active` | **ACTIVE, candidate submitted** — see below |
| [#74](https://github.com/lambdasistemi/singular/issues/74) completion, `Over`, reuse | held; its dependency is the connected lifecycle, not an open ruling (`Q-002` answered and landed). Worktree `/code/singular-e17-issue-74` is retained provenance at `606a1ed` — **do not remove it** |

All carry milestone 1.

## #77 — exact current state

Branch `feat/real-representative-fold` in `/code/singular-e17-issue-77`, clean
tree, **local only** (never pushed, no PR):

```
617e434  fix: bind representative policy to registry state (E-001)   <- CANDIDATE
0891eb1  fix: rename tampered representative row to misnamed
46d2e25  feat: fold spelling-keyed names into Active with real representatives
d1fc24a / 55dea40 / f3a68b1   (ownerless repair)          base 56e0fcd
```

Full candidate SHA — S3 requires it in full:
`617e43440758d779a94a2aea35aab6540ec796ec`.

Worker handoff: `ticket-77/commit-owner-2/handoffs/issue-77-repair.md`.
E-001 defect witness preserved: `ticket-77/commit-owner-2/handoffs/e001-witness/`.

### What the repair did (worker-reported, acceptance pending my gates)

`State` gains a fifth field `representative_policy: PolicyId`; genesis/bootstrap
sets it, `validModify` preserves it with a refusal test, and naming `fold()` /
`retire()` read it from the spent state and require the moved representative's
policy to equal it. Both pins survive (the representative keeps its app-address
pin). No Lean change, no frozen-spec change, no new transition, no weakened
refusal. Reported identity moves: state `e51b…→2bf61b…`, request
`44420f…→c3eec4…`, app `a49c…→703c…→52dbf5…`, attacker fixture pinned.

### What I owe on it — the acceptance I own and never delegated

Instrument results are in the **Acceptance state** table below; this is the
standing obligation list, corrected in place. The earlier "S3 running" passage
and the A1-v1 reference that stood here are **void** — S3 came back RED on its
own defect, and A1 v1's header bound the wrong gate.

1. **`gates/supplement-77-S3.sh`** (sha256 `9e391fc3…`, frozen) — executed; RED on
   its own verifier-path defect after legs 1–6 passed. Its devnet run is real and
   bound, and is the evidence A2 adjudicates.
2. **`gates/amendment-77-A2-verifier-executable-path.sh`** (sha256 `cd569ffb…`) —
   the correction, plus S3 steps 4–5 carried out. GREEN, 17/17.
3. **`gates/fault-controls-77-S3-A2.sh`** — GREEN; v1 retained REJECTED.
4. **`gates/gate-77-v3.sh`** (sha256 `7fa81cb7…`, **frozen — never edit**) plus
   **amendment `gates/amendment-77-A1-v2-recovery-leg-env.sh`** (sha256
   `82dbc1e2…`), which binds `7fa81cb7…`. **A1 v1
   (`amendment-77-A1-recovery-leg-env.sh`, sha256 `5de8be9d…`) bound
   `b67ff15a…` = gate-77-v2 and is superseded for its header only**; it is
   retained unedited. A1 GREEN establishes the `MPFS_BLUEPRINT` one-liner as
   necessary and sufficient for `recovery-preserved`.
5. **Open limits the worker named** (carry them as debt, do not silently drop):
   `key3` naming-only negatives now refuse on missing state rather than isolating
   `identity`; the remaining devnet per-check splits (mint-alone, value-alone,
   retire-input-alone, retire-custody-alone, preservation-devnet) are open with
   hooks in `census-devnet-mutant.log`; retire runs `cfaSkipEval=True`; the
   retire direct mutant was inconclusive on an aiken silent crash.
6. **Serial integration** (`NOTE-038`): my final branch = finalized #17 commit +
   epic 18's consumer-only companion **`7816305`**, imported mechanically, sole
   integration ownership, no parallel writers.

## What I owe epic 18 — one final coherent handoff, through the desk

`handoffs/ownerless-schema-handoff-to-18.md` carries a **SUPERSEDED** section:
the four-field `State` I first sent is void, and the identity table in it is
stale. Epic 18 has been told not to pin it. After the gates above pass I send
**once**, superseding both versions: the `State` constructor with final field
order and types, the SDK codec, every script identity with its parameter count,
the compiler identity, blueprint paths, and the candidate SHA — read out of the
**built blueprints**, not out of prose. Consumer companion `7816305` then needs
reconciliation against that final shape; its final affected devnet checks are
still owed by epic 18.

## Settled — do not re-litigate or repeat unchanged

- root's clean-clone `f3a68b1` Aiken check (1411 checks, 0 errors);
- the S3 exit-17/stale-verdict adjudication fix, and S2 v1/v2 rejected;
- the combined-tree attribution question (pending #77 changes **were** present);
- **E-001's verdict**: incorrect concrete mapping, **no user decision owed**;
- **A-001 option (A), state-anchored `config.representativePolicy`**: genesis
  sets it, `Modify` preserves it, naming `fold`/`retire` bind the moved policy.
  Options (B) config singleton, (D) merged policy, (E) one-sided pin are refused
  with reasons in STATUS 13:22:18Z; (C) on-chain apply is declined on cost, not
  soundness, and remains the fallback if (A) hits a wall;
- amendment A1;
- the successor's corrections to me: `S3_EVIDENCE`/candidate derivation was
  already fail-closed in `46d2e25`, and recovery/retirement were already
  connected via `ConnectedFold` — the predecessor handoff's §5 was a fourth
  stale passage.

## M1 blockers this epic does not own but must not contradict

**[#87](https://github.com/lambdasistemi/singular/issues/87) — Blaster validation
of the compiled Aiken invariants.** M1 blocker, **REQUIRED / NOT YET
ESTABLISHED**. Epic 18 owns the inventory, artifact and #80 gate integration;
**this epic supplies** the repaired production blueprint and its
title/parameter/schema/compiler identities and fixes any validator defect it
exposes. Not established by Aiken test counts, S2/S3 metadata controls, or
model/simulator replay. `COULD-NOT-EVALUATE` blocks acceptance; a toolchain
obstacle is a blocker, never a waiver. Epic 18's current worker is
`t87b-blaster-refinement` (muse, `%993`); predecessor `%982` retired; its
refinement/multi-script/final-artifact evidence is open and **does not accept my
candidate**.

**A bounded epic-17 release is therefore not complete M1.**

## Binding rules

Constitution 1.0.0 and `code-the-design`: Lean is the behavioral authority
including for imported code; Lean owns the vocabulary; two distinct executable
implementation layers per invariant; a mutation census is a discrimination
control, not a layer; humans must match theorem and story clauses both ways.
Semantics, invariants, corpus and protocol requirements are fixed inputs —
ambiguity goes to the user as a concrete story before any semantic edit. The
parent tripwire `handoffs/check-repair-contract.sh` runs against every candidate.
Devnet: private shallow `TMPDIR`; **never touch `/tmp/cardano-e2e`**. Epic 18 owns
`conformance/`, the release-archive integration and the #80 coverage ratchet.
Release line: the complete epic-17 artifact first, then epic 18's integrated
conformance artifact; packaging never closes acceptance debt.

## Wait pattern — preflight before arming, always

Two-space tag column, and `COMPLETE` is always in the alternation:

```sh
R2=/tmp/projects/singular/milestone-1/epic-17/ticket-77/commit-owner-2/STATUS.md
grep -cE '  (NOTE|BLOCKED|RESUMED|GATE-PASS|GATE-FAIL|PROOF-COMPLETE|COMPLETE|CONTRACT-CHALLENGE)  ' "$R2"   # must be > 0
cursor=$(wc -l < "$R2")
/code/llm-settings/shared/skills/worker-protocol/scripts/wait-status "$R2" "$cursor" \
  '  (NOTE|BLOCKED|RESUMED|GATE-PASS|GATE-FAIL|PROOF-COMPLETE|COMPLETE|CONTRACT-CHALLENGE)  '
```

## Frozen gates (immutable, keep)

`gates/gate-62-v2.sh`, `gate-66-v2.sh`, `gate-74-v1.sh`, `gate-79-v2.sh`,
`gate-77-v3.sh`, `supplement-77-S3.sh`, `amendment-77-A1-recovery-leg-env.sh`.
Earlier versions and the rejected S2/S3 drafts are retained as the record of each
adjudicated repair.

## Acceptance state — COMPONENT ACCEPTED at ddfc4e9, 2026-09-12T18:58Z

Candidate `ddfc4e961d530ee4e62361aabbcc730a9b9809ed`, accepted against the
immutable detached root `/code/singular-e17-accept`. Full receipt index:
`handoffs/acceptance-evidence-index-ddfc4e9.md`. Every RED is an instrument
defect with a versioned amendment that is itself GREEN and shown able to fail;
no product leg failed; no frozen instrument was edited.

| instrument | result |
|---|---|
| `gate-77-v3` frozen | 17 legs exit 0, both must-fail controls exit 1 after executing rows, ONE leg RED (`recovery-preserved` = the A1 defect) |
| A2 | GREEN 17/17 |
| A3 `15bb4b50…` | GREEN — E-001 refusal CAUSE, receipt `/tmp/s77-accept3/A3-RETAINED.log` |
| fault controls | GREEN, four faults |
| S4 v4 `9e973282…` | GREEN, all legs incl. clean-PATH exit 0 |
| A1 v2 | GREEN, clean-bound |

Epic-18 handoff RELEASED as an accepted COMPONENT (not the final M1 tuple):
`handoffs/final-schema-handoff-to-18.md`.

## Superseded acceptance state — 2026-09-12T17:45Z

| instrument | result |
|---|---|
| `supplement-77-S3.sh` | **RED, instrument defect** — invokes the nix store path directly; a store output is a directory, bash exits 126. Legs 1–6 passed first, so the devnet run is real and bound. Evidence `/tmp/s77-accept/run-20260912T170015Z-$/` |
| `amendment-77-A2-verifier-executable-path.sh` (sha256 `cd569ffb…`) | **GREEN** — negative reproduces the 126/"Is a directory", positive `$store/bin/connected-verifier` exits 0; carries out S3 steps 4–5: **17/17 ESTABLISHED** for `617e434` |
| `fault-controls-77-S3-A2.sh` | **GREEN** — txid byte-flip → `connected.txid` REFUTED; attacker program substituted → `representative.applied` REFUTED; both sweep outcomes rewritten accepted → `refusal.sweep` COULD-NOT-EVALUATE; foreign candidate sha → nothing established. v1 retained REJECTED |
| `gate-77-v3.sh` (frozen, `7fa81cb7…`) | **run INTERRUPTED/SUPERSEDED, not a PASS and not a complete gate** (NOTE-050). Legs 1–14, 16, 17 GREEN incl. both must-fail controls; leg 15 `recovery-preserved` = the A1 defect; leg 18 `release-artifacts` terminated deliberately at 29:19 CPU after the packaging finding was reproduced at source. Evidence, archive, listing and process receipt all preserved under `/tmp/s77-accept/`. Reruns at the repaired candidate with fresh binding |
| `amendment-77-A1-v2-recovery-leg-env.sh` (sha256 `82dbc1e2…`) | **GREEN, but read it within its binding** (NOTE-052): corrected leg exits 0 with all three LR11 sub-cases; omitted leg exits 1 naming `MPFS_BLUEPRINT` at `journey/recovery/Main.hs:1900`. It takes a worktree PATH and has **no clean-candidate identity check**, and `%990` had already modified `journey/verifier/Main.hs` (18:53:16) before the positive leg closed (18:54:28). So this is **instrument-correction evidence against observed build inputs — NOT candidate-bound preservation at clean `617e434`**, and its blueprint identities were not retained. Evidence `/tmp/s77-accept/a1v2-run2` |

## Exact next action — 2026-09-12T18:58Z

1. **`%990` is working the combined revision** (`ticket-77/commit-owner-2/inbox/NOTE-003`,
   acknowledged 18:56:50Z): Lean-first empty-fold refusal, the retirement
   reference-input read, and operation-specific value routing — one candidate,
   three outcomes, separate controls each.
2. **`%995` owes its Fork site ruling** with exact commands and exit codes.
3. Then serial integration with epic 18 s companion `7816305`, then the naming
   retirement path.

## Superseded next action

1. **A1 v2** once the gate's last leg returns.
2. **The consolidated repair batch to `%990` is DELIVERED and acknowledged**
   (`ticket-77/commit-owner-2/inbox/NOTE-001-consolidated-repair-batch.md`,
   `NOTE NOTE-001 read` at 17:51:58Z). Three items, one adjudication, no later
   additions:
   - the **E-001 refusal cause** is unestablished. `tx-016-fold-foreign-policy-refused`
     is a ledger `refused` with **empty Plutus logs**, so the evidence says
     "refused by a script", not "refused because the moved representative's policy
     is not the state-anchored one". Needs a recomputed obligation;
   - the **producer identity** the final handoff owes: the production tool that
     applies the parameter, plus retained applied representative bytes and hash
     for the exact final application hash (owner 18's #87b Q-001 waits on it);
   - **package provenance** at the desk-granted temporary boundary
     `tools/assemble_onchain_release.py` + `tools/check_release.py`: the archive
     ships 3225 of 3378 entries from ignored `dist-newstyle`/`build` partitions,
     verified by my own `tar -tzf` and by `copytree` at `:43-45` with 0 tracked
     files under those paths. Contaminated-workspace control retained beside an
     honest clean assembly; completeness kept (corrupt an internal file AND a
     manifest entry, both rejected); no size threshold.
3. Re-verify at the NEW candidate with fresh binding — previous results are never
   relabelled onto it — then re-verify, then release `handoffs/final-schema-handoff-to-18.md` through `%988`
   (it stays DRAFT until the tuple is stable) and integrate `7816305` serially.
4. Then the naming retirement path.
5. **Value routing is QUEUED, not next** (NOTE-049 reassigned the fourth slot to
   the Fork repair). It takes the next free slot once source integration is safe.
   Contract and local derivation: `handoffs/refund-slice-source-request.md`
   revision 3 — the boundary is `state.ak validModify`, and the defect is a
   per-owner AMOUNT hole, not a crossed address.
6. **Fork absence-insert (#81) runs in parallel NOW** — child `ticket-81/commit-owner`,
   pane `%995`, worktree `/code/singular-e17-issue-81`, branch
   `fix/fork-absence-insert-81` on `5bd7c79`. It never writes
   `/code/singular-e17-issue-77`. Expected question: whether the repair site is the
   external Aiken `excluding()` lone-Fork arm, which needs an exact
   path/pin/provenance ruling from the desk before any vendoring. The in-repo
   off-chain path (`Cage/Trie.hs`, `Trie/Pure.hs`, `TxBuilder/Update.hs`) needs no
   such crossing.

**Do not implement the blanket "every processed request refunds its bond"** — that
is `.refundAll`, the mode the consumer does not use. The operation-specific table
is in the source request, revision 2.
