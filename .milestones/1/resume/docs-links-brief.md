# Standalone urgent fix: README and all documentation links

## Role, authority and outcome
You are the standalone TICKET-OWNER docs-links, immediate child of milestone owner%844. Runtime /tmp/projects/singular/milestone-1/docs-links, repo lambdasistemi/singular. Initial cwd is this runtime; bootstrap your own isolated repo/worktree. The operator requests: "the try simulator in the read me points to not what I expect, fix it now", "also verify all the links in the docs, Open the playable Singular simulator. is 404 i.e.", and "do this aside the epic". Fully fix the live README/docs navigation, verify ALL documentation links and report the coverage/remaining genuine external blockers. This is separate from epic15 and its naming work; do not attach your issue/PR as a child of15 or20, do not wait for the M1 simulator.

Read FULL brief, write START immediately using worker-protocol status-event with family/model/pane/PID/root. You are not alone in the codebase; do not revert edits made by others.

## Exact staffing
Use family=grok model=grok-4.6 as ticket owner, exact launch /run/current-system/sw/bin/grok --always-approve -m grok-4.6 interactive /dev/tty.
Implementation: one GLM commit owner, /home/paolino/.local/bin/glm --approve; family=glm harness=pi provider=zai model=glm-5.3-flash effort=max; no production secrets. Audit: one fresh Sol seat on submitted candidate, /run/current-system/sw/bin/codex --model gpt-5.6-sol --config model_reasoning_effort=high --dangerously-bypass-approvals-and-sandbox --cd <fresh-audit-worktree>. No other auditor family, no Opus, no native agents, no standing audit team, draft=NONE. One candidate plus at most one repair, at most two auditor launches (initial plus delta); do not silently expand campaign. Keep the task small: no speculative architecture/model/proof work for link repairs. Relevant existing tests and a concrete link inventory suffice; do not invent ritual tests for a URL string replacement.

## Current facts and priority
/code/singular is operator-owned and READ ONLY; do not edit/build/rebase/check out there. Epic owner%848 at /tmp/projects/singular/milestone-1/epic-15 and its Grok ticket20 continue the naming Lean/simulator work. They have been instructed through NOTE-003 that link repair belongs exclusively to this standalone lane; report overlapping documentation edits to parent, never touch/control their descendants.
Observed main at last check58eaabad9d15d4ac223aca912bc5966b16b534c7 (MUST refresh before branching). Release PR2 may be merging or held: refresh actual main; do not merge/edit/tag PR2 yourself. Parent asked epic owner to hold unexecuted release actions for this docs correction; preserve anything already done.
Current README line5: `<a href="simulator/"><strong>Try the simulation</strong></a>` points into the GitHub source tree instead of the playable page.
Current docs/simulation.md line3: `<a href="../../simulator/">Open the playable Singular simulator</a>` misresolves in deployed docs.
Correct existing live entry point https://lambdasistemi.github.io/singular/simulator/ returned HTTP200 to parent. That is the GENERIC registry simulator (Delete allowed), not a completed M1 naming profile. Fix navigation to what exists and make nearby wording accurate; do not fake or implement naming behavior. "admitted-model limits" in README is stale wording against proved-model docs; use accurate limits wording if changing it.

## Owned scope and non-goals
Own link destinations in README and documentation sources, required bound speech assets, and the smallest existing docs/link-check mechanism needed to verify link behavior. Inspect how README/docs are staged into MkDocs to choose portable correct URLs. Verify GitHub-rendered Markdown resolution and built/deployed documentation routes including project prefix /singular/, trailing-slash directory URLs and anchors. Own docs support only, not Lean models, simulator engine/formal/corpus/identity, protocol rules, versions/release workflow, indexer or source-copy work. Do not alter shared model code to make a docs test pass.

## Required skills and mechanics
Load orchestrator-contract, ticket-orchestrator, resolve-ticket, context-compiler, worker-protocol, tmux-orchestrator with recursive-dispatch, worktrees, documentation, verification, invariants and relevant gate rules. User scope and model allocation above override defaults. The docs task does not need a new Lean design loop or a sprawling planning package. Keep mandate proportional and readable. You own contract/spec/PR metadata and supervise GLM/Sol; do not write their behavior-changing code.
Discover/reuse any exact existing link-fix issue/PR before filing a small standalone docs bug issue assigned paolino, appropriate existing labels, Planning board Other/Personal (Status unset at filing, then update only as policy requires). Follow workflow/references/planner-board.md. No new epic and no unsolicited issue comments/reviews/pings. Create an issue-backed draft PR at first coherent candidate and use existing live preview path. Work in a clean isolated clone/worktree; record branch/base and preserve unrelated changes.

## Acceptance and verification contract
1. README "Try the simulation" opens the actual playable HTML on GitHub, not a repository directory. Docs "Open the playable Singular simulator" opens actual playable HTML on the deployed site, not a404. Use absolute canonical URLs where those sources are rendered in multiple contexts and verify responses/content identity, not only the literal source string.
2. Inventory EVERY link in README plus all reader-facing documentation under the repo's actual MkDocs/site coverage, including Markdown and raw HTML href/src, navigation, assets and anchors. Resolve each against the correct source/rendered base. Audit internal destinations and fragment IDs against built HTML, and request external URLs with bounded timeout/concurrency. Capture redirects and final status. Distinguish confirmed404/broken anchor from network denial/rate-limit403/429; do not claim all external links pass when inaccessible. Named external blockers must include the affected source and URL/status.
3. Correct broken in-scope links found by the inventory. Preserve intended destination/content; do not delete links to manufacture a pass. For third-party moved docs locate the authoritative replacement; no guesses. Absolute source-code links should stay source-code links when that is intended.
4. Run applicable repository docs/presentation/release/link checks. Update bound speech JSON freshness if page prose/link changes require it. Do not rerender unchanged diagrams or run unrelated Lean checks locally unless the repository's required gate demands them; CI must still pass on exact candidate.
5. Verify live PR preview against its candidate SHA and live main site after merge. Retain the complete link inventory with total/checked/pass/failure/blocked counts and per-link source/destination/result, final README/link target evidence, exact commands/exits and candidate identity. Existing static docs build succeeding alone is not full link verification.
6. Independent Sol review covers the actual fix, completeness of the inventory and whether GitHub/deployed route resolution is tested. One-line literal-string tests cannot establish browser navigation. Produce a small complete PR with no naming-engine or release metadata changes.

## Execution and merge authority
Operator requested FIX NOW, not merely report or file a ticket. You may create issue/branch/worktree/draft PR, commission the named pair, run necessary docs/link checks, push own branch and publish existing preview. Prepare the complete reviewed GREEN candidate. Send parent your exact PR/head/diff summary and checks/link evidence for immediate guarded merge authorization; do not ask the operator for another permission round. Parent will authorize the concrete result through a durable answer. Use merge-guard MCP only when authorized; no admin bypass, force pushing main, release tags or manual production upload. After merge, verify live docs/README link behavior and report completion, then clean up own workers.

## Progress, blockers and output
This work is urgent and independent of20. Persist autonomously through verified correction and reviewable PR; no repeated continue nudges. Parent sees only YOUR journal and artifacts, not GLM/Sol internals. Use send-pointer and post-cursor START/RESUMED for your children, maintain event-based foreground/harness supervision, no short polling loops. Concise user-facing STATUS lines should say which navigation now works and what is still blocked. Telegram belongs to parent; do not open a child topic.
Questions go questions/Q-NNN.md then BLOCKED; readiness/completion handoffs go handoffs/. On real blocker or merge-ready/terminal result send its durable pointer to%844 with parent STATUS /tmp/projects/singular/milestone-1/STATUS.md and NOTE|RESUMED acknowledgement. Check inbox at natural boundaries. Capacity is terminal COMPLETE with precise handoff, not silent disappearance.
Immediate output: START then issue/PR/worktree identities and bounded link inventory plan; final handoffs/docs-links.md includes merged SHA, actual live URLs, complete verification receipt, external unknowns, and cleanup evidence. Finish only when the requested fix is live/verified or a genuine explicit blocker prevents it. Do not claim milestone/epic delivery from this maintenance fix.
