# Epic 18 owner handoff → GPT Sol, 2026-09-12

- **Workers: two, both LIVE, both must stay running.** `%993` t87b-blaster-refinement (muse-spark-1.3-contributor, xhigh, `/code/singular-e18-blaster`, branch `feat/blaster-compiled-invariants`). `%995`/`%990` are epic 17's — not yours. `%994` t80e-execution-evidence (glm-5.3-flash, max, `/code/singular-e18-exec`, branch `feat/coverage-execution-evidence`).
- **t87b state:** HEAD `0c4acd5`, tree clean, `run.sh` green. Acked NOTE-011 at 20:31:23 and is **executing the Migrating FR3 leg now** (its own named `COULD-NOT-EVALUATE` debt in `EVIDENCE.md`; needs applied params `previousPolicies=[oldPolicy]` + predecessor setup, and a refusal that discriminates FR3 from the FR1 allowlist refusal already firing). Rebind the applied identity; do not carry `previousPolicies=[]` receipts onto it.
- **t80e state:** HEAD `0100012`, tree clean. Build path for the ledger rival witness **proven** in the isolated copy `/tmp/t80e-rival-witness` @ `ddfc4e9` (exe wired, compiles, skeleton runs green, outside git). Acked-and-working on NOTE-020. **Not landed: the three rival-case bodies and their runs.** No capacity or tool-limit claim attached.
- **Exact next action, t80e:** retire A using ONLY B's state, A's state absent. Three distinct cases (ordinary rival / forged-token control labelled separately / mandatory copied-policy B). Two bounds the CEK leg assumed and this one must not: B's bootstrap must actually execute through `validateMint` from B's own spent seed, and the successor must be a **real B-state transition**, not an A-fixture successor.
- **Open finding F-003 — routed to root, no verdict in lane.** t87b `7f746e9`: B's state (same ddfc MPFS script/address, B's seed-derived token, A's `DDFC_REP` copied into B's datum) authenticates A's retirement with A's state absent; honest RR0 also HALTs, RR1/RR2a/RR2b refuse nearby. I verified the mechanism at source: `sha256(B_SEED_TXID‖0x0000)` = committed `B_TOKEN`; `state.ak:228` binds B's own seed; `state.ak:239` leaves `representative_policy` unconstrained; `application.ak:18-26` never compares which token name. Root (NOTE-104) dispositioned it **bounded evidence for an already-commissioned repair** — not a waiver. Repair belongs to epic 17 under root-070/071.
- **The ledger leg is NOT discharged by `7f746e9`.** Root bound: the successor output still comes from the A span fixture, so it is expressly not a complete B-state ledger transition.
- **Missing executable consumer program — the live blocker on my own deliverable.** At `cardano-keri@14a64a46` the per-obligation authenticators exist as compiled registrable withdrawal validators (`observer_lifecycle.withdraw→validate_registration` for `inception`; `observer_advance.withdraw→validate_advance` for `rotationFrom`; plus migration/enforcement/entitlement), each with `publish` accepting its own `RegisterCredential`. **But no compiled validator is the consumer plugin** — nothing composes leaf transition + created checkpoint + allocation + go-request veto into one withdrawal-executable body; `processBody` is Lean only. **And `env.duplicity` has no compiled counterpart at all** (conviction only in `specs/151-convict-small` + simulator). Owner: cardano-keri consumer side. Not producer, not naming, not a checkpoint-machine rewrite.
- **Consequence:** exhibits E2/E3/E5 are producer-side once 17's bytes land; **E0/E1/E4 cannot execute on producer bytes alone.** `onchain/plutus.json` in `115-advance` is untracked at that commit with no toolchain → `COULD-NOT-EVALUATE`, not a blueprint.
- **Coverage: closed and verified, no repayment.** `0100012` — all 17 files under `conformance/coverage` parse, 71/71 Ran/OK with true exits, inventory 0 (192=109+83), ratchet 0 PASS, completion 1 INCOMPLETE at 188/188/11. Root's independent paired control passes there too. **P1/P6/P7 discrimination accepted; P2, P3, P4, P8, P9 still uncredited pending owner source review — that review is unfinished and is yours.** Two false-green causes now named in-journal: a second duplicated `def` at line 508, and pipe-masked exit codes.
- **Evidence pointers:** `handoffs/consumer-adapter-binding.md` (contract↦model binding; §6 is the artifact gap), `handoffs/authenticated-obligation-answer.md` (relabelled answered-by-contract; its posted-data and aggregate-only-routing findings stay live), `handoffs/rival-service-witnessed.md` (closure withdrawn in place), blaster `EVIDENCE.md` F-003, `t80e .../handoffs/0100012-coverage-tests-raw/`.
- **Unsent instruction: none.** NOTE-011 and NOTE-020 were both delivered and acknowledged. Nothing is parked on an unanswered question; no Q is open in either child.
- **Standing fences:** four implementation slots max (two are epic 17's), `/code/singular-e17-accept` read-only, epic 17 owns all producer shared paths and the release tooling, no new seat/budget/auditor, LI03 (never fake a ledger refusal or relabel a successful rival creation).
