# Current owner replacement

Authoritative current brief: owner-sol-20260912/brief.md. Exact launch: /tmp/projects/singular/milestone-1/epic-18/owner-sol-20260912/launch.sh. START verified in owner-sol-20260912/STATUS.md and epic STATUS. Root%988. A005: finish this epic with all existing acceptance, checkpoint and park; no successor epic. Existing implementation workers remain unchanged. Historical pre-replacement RESUME preserved at root handoffs/owner-replacement-20260912/epic18-old-RESUME.md. Consult current brief and outgoing handoff for debt; do not replay old commissioning work.

## Current owner checkpoint — 2026-09-12

- owner identity: pane `%914`, pane PID `568968`, family `codex`, model
  `gpt-5.6-sol`, effort `high`;
- inherited workers only: t87b `%993` / PID `4052478` in
  `/code/singular-e18-blaster`; t80e `%994` / PID `4182517` in
  `/code/singular-e18-exec`;
- wire review at `0100012b1afa318df3bae6cf40d6d9ee3507110e` accepts P4 at the
  bounded wire layer; P2/P3/P8 keep named clause/layer debt and P9 earns no
  obligation credit. Evidence: `handoffs/owner-wire-source-review-0100012.md`;
- NOTE-109 proposal and exact missing compiled boundary:
  `handoffs/bounded-consumer-adapter-proposal-note109.md`, corrected under
  NOTE-110: epic 18 owns the compiled compositor and registration E0/E1/E4 is an
  independent tranche using the existing lifecycle/checkpoint sources; only
  go/revive/convict retain the missing reap/dormant dependencies. Do not fabricate
  a receipt or count producer bytes alone as E0/E1/E4;
- t80e remains in the already-started isolated ledger rival witness. Owner
  rejected two earlier compile-green claims after live-source review. Under
  NOTE-025 it is repairing the now-correct single activation (after honest A
  setup and before LT01) with an LT01-terminal rival path and a post-submit
  proof that the copied-policy result is a real B successor. No devnet run is
  authorized until the next owner source review; preserve every invalid attempt;
- t87b historical verification through clean `bbaa6e6`: owner first
  invocation failed pre-execution because the default MkDocs shell lacked Aiken;
  the documented `AIKEN_BIN` invocation then passed the full gate (Aiken
  `v1.1.21+unknown`, extraction/hash/fixture gates, 309 Lean jobs). FR3 and ddfc
  arms are accepted only as finite compiled-CEK `TESTED` evidence. NOTE-012's
  Sweep ledger slice has accepted positive owner-Sweep and Modify-control rows:
  retained bodies recompute the documented transaction ids; root/owner live
  censuses show the advanced state and exact refund, legitimate request consumed,
  refused garbage still unspent, and both collateral inputs intact. NOTE-113
  corrected the earlier false empty-address statement. Original command/result
  pairs for both refusal submissions and the insufficient-budget attempt are
  byte-identically retained under `blaster/ledger/t87b-txs/receipts/`, but the
  original pipes masked the numeric submit exit. Raw stderr and non-admission are
  retained; final refusal-receipt admission stays blocked to the missing numeric
  exit extent, with no rerun authorized. Fresh owner full runs at `b14d2c7` and
  `1c9b3f3` passed extraction/hash/fixture checks and 309 Lean jobs; receipts are
  under `owner-sol-20260912/evidence/`. Snapshot Burning is independently
  owner-accepted from the bound End body at tx `9d3dd211` (exact mint -1,
  Burning redeemer/witness, state consumed, ownerless same-shape refusal) and the
  ddfc End CEK pair is green. The census-schema repair and its root control are
  accepted narrowly. The first NOTE-119 register repair at `cc56497` is rejected
  pending NOTE-019: provenance was not compared; quantifier/operator/nonempty
  reverse-direction prose was not source-bound; the real row path could not
  admit an approved new identity; mutable pins could be co-edited; and the
  invented-pair control accidentally wrote 193 register rows rather than a
  192-for-192 replacement. Root independently re-derived the honest clean
  `0100012` baseline as 192 identities / 109 manifest-bound with all metadata
  equal (`handoffs/source-derived-lean192-root*.json`). Preserve that baseline;
  final tuple growth/rebind and tuple-gated rows remain explicitly open;
- board remains `188/188/11`. No release, merge, successor or backlog work is
  authorized.

Fixed worker order under NOTE-110/112/120: t80e finishes the live rival witness, then
implements the bounded compositor registration tranche against an explicitly
provisional snapshot without waiting for the gate repair or final epic-17 bytes;
it later rebinds to the exact accepted tuple, and its P2/P3/P8 repairs remain
sequential. Producer `ff0cbd3` is not released and must not be frozen: empty
Modify and surplus-action checks are still being repaired by epic 17 and exact
hashes will change. The five-field ddfc rival remains historical. t87b owns the
NOTE-019 source-bound register repair on the same seat. Do not interrupt either
current leg or add a seat.

Parent-frozen registration gate v1 is under
`owner-sol-20260912/gates/consumer-registration-v1.{contract.md,sh,manifest.md}`.
Root NOTE-111 executed two report-only controls and proved it mechanically
false-green; v1 is retained unchanged and `NOT ACCEPTABLE`. Do not dispatch it to
an implementation worker or relabel any implementation seat as its auditor.

Corrected owner gate v2 is retained under
`owner-sol-20260912/gates/consumer-registration-v2.{contract.md,sh,manifest.md}`.
It makes candidate output preparation-only, closes/measures its raw inventory,
owns fixed outcomes, and delegates compilation/parameter application, CEK,
ledger submission/query and mutation decisions only to a seal-hashed parent
verifier. Both exact root controls were re-executed and rejected after their
fake runners ran because the declared evidence did not exist. v2 remains
`BLOCKED-PRE-SEAL`: the accepted producer integration base and parent verifier
do not exist yet, so this is not an acceptance basis and grants no E0/E1/E4
credit.

Owned executable gate v3 is the current prospective gate under
`owner-sol-20260912/gates/consumer-registration-v3.{contract.md,sh,manifest.md}`
plus `consumer-registration-parent-verifier-v1.py`. It parent-rebuilds and
measures seven exact artifacts; separates invoked from enforcing failures;
executes a Variant-C discriminator; owns the CEK/ledger semantic relations,
submissions, txid output queries and source mutation/restoration; and retains
every command stream. A well-formed fabricated candidate passed sealed preflight
and ran its candidate preparer, then was rejected on the parent-rebuilt compiled
digest before CEK/ledger credit. An earlier-semantics fake evaluator was rejected
by the 256-byte canary. Static checks are green. v3 remains
`BLOCKED-PRE-SEAL` with baseline exit 1 and zero candidate execution because the
final accepted producer seal is absent. Do not install `ff0cbd3`; root inspection
and a fresh real positive remain required. Gate work does not delay the fixed
implementation order.
