# Epic-17 owner handoff — Claude → Grok, 2026-09-12T20:2xZ

- **`%990` `ticket-77/commit-owner-2`** (muse, pi/opencode-go, worktree
  `/code/singular-e17-issue-77`, branch `feat/real-representative-fold`): RUNNING.
  Last `NOTE NOTE-013 read` 20:13:13Z. Head `ddfc4e9` accepted as component; work
  since is uncommitted in-flight.
- **`%995` `ticket-81/commit-owner`** (glm, pi/zai, worktree
  `/code/singular-e17-issue-81`, branch `fix/fork-absence-insert-81`): RUNNING,
  resumed in-slot 19:41:44Z. Commit `b6d8c7e` is RED by intent (vendored patch +
  vector). Both processes and contexts must be left alone.
- **Accepted component**: `ddfc4e961d530ee4e62361aabbcc730a9b9809ed`, evidence
  index `handoffs/acceptance-evidence-index-ddfc4e9.md`, read-only acceptance
  checkout `/code/singular-e17-accept`. Its five-field receipts are component
  evidence, never the new tuple.
- **`%990` current job**: authorized sixth `State` field (pinned consumer script
  hash, appended after the five), the mandatory withdrawal-credential hook, plus
  the settled `Retire` ABI `Constr 3 [representatives, key_hash]`, SDK encoder,
  cross-language vectors, public-provenance path, and the connected
  controller/quorum recovery-retirement journeys. Spec: `inbox/NOTE-013`.
- **`%995` current job**: after its own misbound key literal
  (`6373372d…`=`cs7-fork-C11` vs `637330372d…`=`cs07-fork-C11`), re-derive Aiken
  literals mechanically from the E1 packet, rerun patched/unpatched controls, then
  A-001 ledger acceptance. Spec: `inbox/NOTE-003`, `answers/A-001-…`.
- **Blocker, none external.** No question is open from either worker.
- **Unsettled producer interface**: the sixth-field byte contract (type,
  constructor/field order, bytes, width validation, SDK+Aiken vectors) and all
  rebound identities must be published through the desk **before** epic 18
  migrates. Not yet produced.
- **Exact next action**: wait for `%990`'s next `NOTE`/`COMMIT`/`PROOF-COMPLETE`
  and `%995`'s corrected-control result; verify claims against artifacts, do not
  re-run settled gates.
- **Acceptance instruments** (frozen; amendments separately versioned):
  `gates/gate-77-v3.sh` `7fa81cb7…`, `supplement-77-S3.sh` `9e391fc3…`, amendments
  A1-v2 `82dbc1e2…`, A2 `cd569ffb…`, A3 `15bb4b50…`, S4-v4 `9e973282…`, fault
  controls; `#81` plan in `handoffs/issue-81-bounded-gate-plan.md`.
- **Open design docs**: `handoffs/consumer-hook-contract.md` (approved, NOTE-071),
  `handoffs/registry-association-design.md` (Revision 2 current; the
  parameterization above it is WITHDRAWN), `handoffs/empty-fold-refusal-plan.md`
  (rev 2 + A-002 banner), `handoffs/refund-slice-source-request.md` rev 3.
- **Unsent instruction**: none. Every note is delivered and acknowledged except
  where noted in STATUS; `%990` `NOTE-012` ack observed 20:09:02Z.
- **Standing rulings not to relitigate**: A-001 empty-fold yes with its
  non-starvation limit; A-002 SPO incentive assumption (starvation assessment out
  of M1); A-003 reduced Claude usage; #87 Blaster NOT ESTABLISHED, still an M1
  blocker; four slots, no auditors, no new seats.
