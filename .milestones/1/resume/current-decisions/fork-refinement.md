# Q-003 addenda read in full: the abstract design remains the authority

Root read the complete Q-003 and its investigation addendum on 2026-09-12. The accepted inclusion-path Fork is useful partial evidence; retain it and the absence-path failure separately. It does not establish that the builder can insert an absent key for the failing shape. Do not close the overall interoperability outcome by moving that defect out of the denominator.

Neither pinned library is the semantic authority. Lean's omission of ProofStep constructors does not by itself prove a product ambiguity: a concrete Merkle encoding may be a representation refinement of the already modeled registry operations. The immediate next action is to bind the failing concrete key/root/request to the full abstract Insert/fold preconditions and expected resulting map. Check that the generated absence proof represents the same pre-state and action. If the required abstract transition is unambiguous and the concrete implementation cannot realize it, classify it as an implementation/refinement defect. Do not ask the user to choose a library merely because two implementations disagree.

Produce the minimal mapping from existing evidence first. No repeat of the completed probe campaign or speculative multi-step search is requested. If it exposes a real ambiguity in the abstract operation, its representation contract, or consumer requirements, escalate that exact Given/When/Then with the competing observable outcomes. If the mapping settles the intended behavior, return the concrete local repair scope and required discriminating test to root for cross-epic source ownership. No upstream edit, dependency refresh or proof-shape exclusion is authorized by this answer. Keep #68/#81 and the failed absence obligation visible while continuing settled work.

At your next sweep, replace the stale RESUME.md snapshot: it still names retired #68/#69 work as the next action and omits the delivered #80 foundation, current #70 candidate, open coverage debt, Q-004 routing and resolved permissionless hold. Include your actual launch, child roster, retained worktrees, exact next action and pending user decisions. Root will copy your self-authored fragment into the milestone resurrection snapshot. This is recovery metadata, not a new implementation or audit campaign.
