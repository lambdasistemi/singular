# Q002 applied identity handoff is ready

Root executed the narrow Haskell producer against exact5bd SDK source and its Nix-built blueprint. Complete reviewable handoff: `/tmp/projects/singular/milestone-1/handoffs/blaster-applied-identity-5bd/README.md`, with source, build logs, producer, representation-specific bytes, hashes and digests in that directory. Read it and answer t87 Q-002 through your own answer file. No second producer build is needed.

Applied-List[] state hash: ce7615f6ba4de80dfa9b9c6aef680666472ba4ed7e640ff55aad7c6e. Parameter Data is List[]. Both single-CBOR and naked Flat program bytes are supplied with unambiguous filenames. Raw compiledCode matches the worker's actual snapshot blueprint byte-for-byte. Independent Python V3-prefixed Blake2b224 agrees with the ledger APIs; bare hash and unapplied hash differ.

The application path calls unchanged SDK Blueprint.applyPreviousPolicies directly. Hashing executes the exact ledger API sequence used by Internal.computeScriptHash/scriptHashBytes, not the full Internal module; this is explicit in the handoff and provenance. No product or accepted Lean source was edited. Initial helper build failures are retained, successful build/run exit0 is recorded. Do not describe this as a full SDK, ledger or invariant gate.

Have t87 consume the verified artifacts to bind the historical fixture credential and check imported applied-program equivalence to its intended Apply(body,List[]) term. Q002 credential request is answered by these bytes; remaining TxInfo/reachability/live-protocol prerequisites must still be independently established before upgrading I-1. Final f3 is parameterless and this historical identity must not be substituted for it. Continue the full previously authorized scope, no placeholder waiver or new worker.

Root also reran the ORIGINAL mistranscribed hash assertion against the new tuple: exit1 at the intended abcOk guard, while corrected frozen6a730f1 HashVectors exits0. Evidence `/tmp/singular-hashcheck-wiTcKO`, both exact sources and outputs preserved. This is fresh narrow oracle discrimination, separate from the worker's runner control and product mutants.
