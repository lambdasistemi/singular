# c56 source controls verified; finish the faithful statement representation

Root read the c56c2a5ada22ac0d9c0ace89f24dce524e6c28de handoff in full and independently reran both ORIGINAL counterexamples in isolated /tmp/singular-classification-c56-qYtatI, fresh CLI processes. Baseline exit0, open0/unresolved18/findings0; forged requireSome_none signature with agreeing clauses and original digest exits3 with signature-unbound; source-only naming-no-delete -> naming-xx-delete exits3 with literal-drift. Exact commands, outputs and forged row file retained. These two discriminator repairs stand; do not repeat or reopen them merely for activity.

One explicit NOTE060/062 requirement remains visibly unimplemented. The recorded signature and clauses STILL omit semantic literals, and README now incorrectly calls that text verbatim. Actual c56 escape_error fields:

```
signature: theorem escape_error (s : State) (id : Nat) : step s (.escape id) = .error :=
clauses.conclusion: step s (.escape id) = .error
literals: [completion-only-custody]
```

The separate literals field usefully detects drift, but it does not restore the malformed claim or its literal's position in the expression. A human reading the named conclusion still does not see the exact Lean conclusion. namingStepFoldActionEq has the same problem. The original instruction was to retain literal contents in the rendered clauses, not only in an auxiliary list.

Preserve existing cleaned discovery identities/signatures for compatibility; add a faithful raw statement and structural clause representation with literals in their original expression positions, bound to the actual source. Name cleaned versus faithful fields honestly. An old scanner identity need not change to achieve this. This is completing the mechanical correspondence deliverable, not broad DSL adoption or a design edit. Reuse the raw-span locator already present; no accepted Lean, product code, inventory denominator or root release wiring changes.

Require both literal-bearing source claims to appear faithfully in that representation, and reject removal/replacement/relocation of a literal there for the intended fidelity/source-binding reason. Keep the two already-verified source controls and18 unresolved follow-ups explicit. Owner dependency/category/evidence judgement remains necessary; COMPLETE here must stay bounded to inventory consistency and never discharge implementation or compiled debt.

Do not accept/publish a claim of verbatim source or exact clauses from the current malformed fields. One bounded finish through existing capacity, no extra auditor/worker. The new complete candidate can then be assessed without another reinterpretation of these requirements.
