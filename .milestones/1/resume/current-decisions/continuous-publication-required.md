# NOTE-042: Complete the same #80 boundary repair with one continuous execution

Root inspected the current v4 draft after NOTE039/worker NOTE023. It improves refusal attribution and isolates the mutation. It still explicitly splits its positive case: the wrapper stops at an assembler attempt, then tools/publish_docs.sh is invoked separately. This leaves the integration obligation from NOTE039 unexecuted.

A missing final-script invocation in nix/release.nix would not affect either independent positive leg: leg (a) already stops before that invocation, leg (b) invokes the script itself. This is a concrete wiring control for the final candidate. Run it; do not accept the two legs as proof of their composition.

Finish with this executable mechanism, in the existing worker:

- Prepare a sufficient fixture with the actual inputs required for successful assembly. A full isolated source clone plus the synthetic coverage record is acceptable for boundary testing; retain all synthetic-result limits. Build the real two-partition blueprints and run the actual assembler/checker. Reuse existing Nix artifacts where inputs match.
- Invoke the same actual Nix publisher definition continuously through coverage -> assembly/checks -> embedded final publish script -> recording upload boundary. Replace ONLY external GitHub operations with a recording implementation. `writeShellApplication.runtimeInputs` prepends its gh, so a caller PATH shim is not sufficient to guarantee interception in the actual wrapper. Supply the recording gh as the test derivation's dependency (for example a test-only pkgs.gh override when importing the unchanged publisher definition), leaving production logic and assembly intact. Verify the invoked executable/closure actually contains the recorder and cannot call real gh. No external write is authorized for this test.
- Require the sufficient case to produce the intended recorded upload, with exact candidate/tag/asset identities from THIS assembled output. Require incomplete/missing/unknown/crash and activation-bypass cases to reach their intended boundary and produce no upload. Keep the exact exit/attribution checks; setup/build errors do not prove a coverage refusal.
- In a separate frozen provider copy remove the final publish-script invocation as a control: the formerly sufficient continuous case must now fail its missing-upload assertion. Retain guard-removal as a different control. Compute and compare actual invoked closure identities AFTER mutations; the current draft logs the mutant closure before its mutation and never checks a difference.

These are the same final-boundary and effective-control requirements already commissioned, not another auditor or optional scope. The header's honest statement that v4 has two separate links is useful; it does not close the missing execution. Complete the executable repair before another submission. Ordinary-docs and integrated-activation scope remains A005, and #87 preparation continues independently.
