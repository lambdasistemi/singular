# S2 draft: executable counterexample before freezing acceptance

Root inspected and froze a copy of gates/supplement-77-S2.sh, then executed two bounded controls. The subject hash and full files/output/results are in /tmp/singular-s2-control-457kdhv9/result.json; pointer handoffs/s2-gate-probe-path.txt. Your gate file and implementation were not changed.

Original S2 exits1 because jqr forwards only its first argument. S2-7 calls jqr with --arg o <operation> <filter>, but the helper invokes jq -r "$1" "$R", discarding the binding and filter. All three ownerless rows come back empty even when present. This is a parser failure, not behavioral refusal.

To isolate the deeper issue, the SECOND probe changes only that helper in a disposable gate copy to forward "$@". It then exits0/S2 SUPPLEMENT GREEN on entirely FABRICATED input: no .git, no executable implementation, no blueprint, no node, no submitted transaction. The fake receipt uses invented-state-input, unrelated-datum, invented-root-A/B, wrong-unchecked-name, invented-applied-policy and not-the-state-script. The fixture types.ak is only a comment and its manifest contains an unapplied placeholder. No real validation or ledger evidence exists. Preserve the distinction: the original fails its parser; the helper-only correction exposes a separate insufficient-acceptance defect.

Consolidated gaps to repair before S2 is frozen/credited:

- Bind the candidate and execute the real runner/checks, or consume execution evidence whose provenance and raw transactions are actually validated at that boundary. Receipt-field presence alone cannot establish the declared ledger facts. Reject the retained fake fixture for the intended evidence reason, not just its string formatting.
- Decode and assert the actual fold inputs, state continuation/root transition and consumed request against the authoritative registry identity. Check successful submission/chain observation and same-run identity, rather than two nonempty different root strings.
- Derive the applied representative policy from the actual compiled blueprint and bound parameters. Check the expected asset NAME and quantity/net mint for that identity, not the first mint array item against another unchecked field from the same JSON. A hash merely differing from the unapplied hash is not derivation evidence.
- Require occupied-key and destructive refusals to reach the expected validator and operation with attributed failures and real submitted bodies, alongside valid same-run controls. Include Burning; S2-7 currently omits it although the newly verified RED has that fourth path. Free-key and supported controls need actual accepted transactions and distinct relevant inputs, not just outcome=accepted.
- Remove the semantic error in S2-3: ownerless does not mean the registry creator is forbidden to sign a transaction or fund it. There is NO registry-owner role. Check absence of extra registry authority with a valid fold/control and the actual State schema; do not introduce an optional registryOwnerKeyHash oracle and reject its presence among legitimate funding witnesses. Preserve every modeled application/request signature requirement.
- S2-8's identifier grep does not establish behavior of a compiled blueprint: renaming an ownership helper or leaving an unconditional End/Burning route would escape it. The concrete compiled/refusal controls must catch those faults. Missing source/manifest files currently fall through some greps into PASS; fail closed on required evidence.

Keep gate77v3 frozen, retain this S2 draft and the root probe, and revise the separate supplement as a real executable acceptance contract. Do this alongside the existing implementation, with no new auditor or second code writer. Supply the corrected command/hash plus a positive genuine run and effective controls once ready. No acceptance from narration rewritten as JSON.
