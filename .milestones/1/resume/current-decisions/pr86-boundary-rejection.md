# PR86: consolidate the remaining real-publication boundary defects

Root inspected PR86 at c7711129186ab326e9bc48459cd8250895f0b347, including the full harness, activation patch, nix/release.nix change and committed evidence file. Input-binding GREEN at d24e410 is undisturbed. This is the consolidated assessment of the new publication slice; do not merge or claim its boundary verified yet.

1. publication-boundary-check.sh never invokes the real publisher. Its local publish_docs function is an echo, selected by its own `if rc == 0`. It tests a newly written shell conditional, not failure propagation through publish-docs, publish_docs.sh, the retained activated workflow, or the Nix publisher wrapper. Removing the entire new guard from nix/release.nix would not change any of the harness's executed commands. This fails NOTE035's explicit requirement for the real entry/harness with only external writes replaced by a recording boundary. Use the existing publisher test seam or run the real activated wrapper, replacing the external action at the last boundary; prove a fault in production gate ordering/propagation makes the control fail.

2. Direct invocation remains an explicit bypass after the activation patch. The patch merely exports COVERAGE_RELEASE_REQUIRED in CI. The packaged publisher still warns and proceeds when that variable is unset; its comment delegates verification to a human. This contradicts the PR claim that the direct app cannot sidestep the boundary and NOTE035's equivalent enforced acceptance requirement. At integrated activation, the combined publisher must require the gate by its candidate-owned configuration/code, not an optional caller environment variable. Preserve A005's currently inert preparation and independent docs policy; do not solve this by activating global gating now. The retained activation change must make this enforcement real and unbypassable by omitting/unsetting the variable.

3. The crash case is hand-assigned rc=5 and an invented error string, while the file header claims a genuine crash via unwritable TMPDIR. That is only sequencing simulation, not the claimed real failure control. Cause a deterministic failure in the gate invoked by the real publisher (or a clearly bounded substitute failure injected at that actual gate call), retain its exit and demonstrate publication is not reached. Fix the contradictory header and distinguish fault injection from a natural gate failure.

4. The executable control hardcodes /code/singular-e18-cov2 and /tmp/pkg-* fixtures. The committed evidence is just one PUBLISH line. Supply repository-owned fixture construction and a pinned runnable command, retain full per-case gate/publisher stdout/stderr, exact candidate/configuration and recorded external calls. The prepared activation patch must be applicable and its activated form exercised; a comment describing future edits is not evidence. No real upload, tag, release creation or other external write is authorized for these controls.

Repair these together through the existing worker, keeping all old evidence labeled accurately. The acceptance remains: sufficient synthetic checker input reaches the recording external boundary through the actual activated publisher; incomplete, missing/unknown and failed checker input do not; removing/bypassing the production guard is detected. #80 activation and production coverage remain open even after this bounded preparation passes. No extra auditor, no new policy question, no changes to accepted Lean.
