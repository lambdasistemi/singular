# Bind the classification to actual Lean statements and definitions

Root verified frozen candidate 2aee840126f34d145e1601f89718086f77b226fe in isolated clone /tmp/singular-classification-review-fOooOT. Your structural splitter repair stands: it independently re-splits, rather than relying on round-trip reassembly alone. Do not reopen the settled 17-row repair. Two separate source-binding counterexamples now change the acceptance action.

Evidence: probe.py, forged-row.json, forged-classification.json, results.json and cli-results.json in that clone. The latter retains actual fresh-process CLI commands, stdout/stderr and exits using project-pinned Python /nix/store/7gl4khq26h625mpqzkiiqsify3hclar1-python3-3.13.9/bin/python3. No worker/product worktree was mutated. The source mutation was restored after evidence capture; the script and retained outputs reproduce it.

1. Replace the recorded Singular.requireSome_none signature with `theorem requireSome_none {α} (reason : String) : True :=` and make all three clause fields agree, retaining its original digest, identity, disposition and rationale. Fresh CLI: exit 1, open=8, findings=0, INCOMPLETE, identical to baseline. check.py compares the supplied digest to discovery, but never binds row.signature to that digest/live declaration. Internal clause consistency accepts a different statement.
2. In the isolated source only, change the NamingLemmas namingStepFoldActionEq refusal literal from `naming-no-delete` to `naming-xx-delete`, with the classification unchanged. Fresh CLI: exit 1, open=8, findings=0, INCOMPLETE. This is not a cache artifact: each CLI is a new process. The scanner blanks semantic string literals; a prose caveat does not bind their contents. This row is currently a proposed non-open naming-transition classification.

These are missing consistency rejections, NOT evidence of COMPLETE or a bypass of the existing global Lean-tree guard. No semantic Lean change is authorized. Keep the old scanner identities and all192=109+83 rows; add complementary faithful raw-statement/source and relevant-definition binding in the new classification artifacts instead of rewriting the accepted design or silently changing existing inventory identities. The required declaration/definition digests must actually be compared to live content. The rendered clauses must retain literal contents as part of the claim. Lexical limitations stay explicit. Frozen revision metadata must not be a decorative field.

Have one consolidated source-binding repair cover the statement text, semantic literals and relevant definitions used by the rationale. Demonstrate baseline acceptance-of-consistency and intended FAIL-CLOSED rejection of both original counterexamples through the actual CLI, plus a relevant-definition drift control. A definition's mere existence and an 80-character rationale are not semantic review; your judgement of dependencies, categories and the eight open rows remains necessary. Do not drop or exempt declarations because no current textual use was found, and do not alter Lean proof dependencies solely to justify classification. The implementation tests and Blaster debt remain separate and unchanged.

Continue through the existing authorized capacity and owner hierarchy, with no auditor or extra concurrent worker. Preserve both candidates and results. No repeat of Aiken, devnet or publication gates is needed for this bounded classification repair. Acknowledge once and report the repaired frozen candidate with the actual discriminator results.
