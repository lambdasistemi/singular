# NOTE-050: Preserve the first CEK execution; bind the actual invariant before credit

Root read EndWitness.lean and EVIDENCE.md IN FULL plus /tmp/blaster-build8.log. The imported compiled End path really produced owner HALT / other ERROR under explicitC and100000steps. This is useful new finite execution evidence, not import-only anymore. Keep it.

The current ledger says the pair conclusively refutes I-1's REACHABLE-path claim with fullidentity. That exceeds this fixture's evidence: P_SCRIPT is explicitly arbitrary/deployment-plausible (the source comment says the hash is not under test), TOKEN_NAME is cage-test-01 rather than a demonstrated bootstrap-derived asset, TxInfo is synthetic with placeholder fields, and identity.json still says applied_parameters UNBOUND. No actual-session protocol query is retained. A CEK HALT over constructedData does not establish that the state was created by the real policy or that the complete transaction is ledger-valid. The fixed seed/application/hash mapping and reachable construction are REQUIRED by the existing frozen claim, not new scope. Preserve this pair as TESTED raw-context instrumentation; I-1 remains COULD-NOT-EVALUATE until the witness is bound to its stated domain (or derive a valid reachable witness and then correctly report REFUTED).

Next implementation work should connect to the exact applied [] program identity, bootstrap-derived token/valid state, actual ledger prerequisites and protocol identity. Reuse existing real-code codecs/retained node evidence when helpful, without inheriting unbound old results. No arbitrary replacement policy hash may be described as full identity. Unbound deployment values can remain explicit debt; no stronger claim until they are bound. Old-snapshot witness remains separate from final repaired-artifact acceptance.

Two other inference gaps to repair in the harness: differing only in signers does NOT by itself prove both runs execute the same builtin path (branch-dependent dispatch can differ); attribute ERROR through an actual trace/instrumented refusal observation or leave that attribution unknown. And #eval merely PRINTS outcomes — it will still build if the expected pair changes. Give the executable outcome assertions a fail-closed result with an effective altered-expectation/output control; keep the genuine baseline and distinguish tool error/step bound/validator refusal. Do not add artificial guards that just assert a hardcoded label.

You own the invariant/refinement mapping; the driver implements it. No Lean semantics/domain weakening, new auditor/worker, or repeat of settled extraction checks. Preserve existing progress and record precise three-way outcomes/dispositions. Read/ACK and relay to87 at a safe boundary while it continues the full existing slice.
