# Unblock the full compiled fold work without shrinking its invariant

Root inspected the actual pinned Evaluate.lean in full: 17cee18a2058790bca36282d82c19146587fb2d1 lacks Blake2b_256 dispatch and returns none through its catch-all. This supports a tooling-support finding; it does not by itself identify the first failure of any particular synthetic Update fixture. Preserve that attribution limit.

There is a concrete upstream support path, so do not close slice1 as complete on Rejected-only execution or stop the whole work at this pin. Root fetched primary files at exact PlutusCoreBlaster ed3126b6a2f5cc32bd151fdefb26e66dcf514888 into /tmp/projects/singular/milestone-1/handoffs/blaster-crypto-support. Its Evaluate.lean dispatches Blake2b_256 to Crypto.blake2b_256; Crypto/Hash/Basic.lean provides an opaque definition with an executable Cryptograph.Blake2b body. It declares Lean4.24.0 and its committed lake-manifest pins Lean-blaster c576289cb55412b359411d6c89568f95af50730c. Source inspection only: compatibility, hash correctness, symbolic treatment and actual Singular execution are not yet verified. Current CardanoLedgerApiBlaster5dab3c43f042b8735b6d067223baaa8d32ed28a1 instead records PlutusCore4ef4860 and Blaster59db213; do not assume arbitrary latest pins compose.

Authorized next technical action through the existing #87 driver: preserve the committed old-pin results, perform an isolated compatibility probe using exact verification-tool revisions that actually provide hashing, then bind any working tuple explicitly in a new candidate. This authorizes necessary Blaster dependency changes only, not Aiken/product locks, accepted Lean semantics, a new worker, or a waiver. Start with the smallest coherent tuple found in committed manifests; inspect conflicting follows/overrides instead of accepting accidental Lake resolution. Demonstrate valid known hash vectors and malformed-argument refusal against an independent reference, explicit target variant, then the actual Update path and its discriminating mutant. Report opaque-hash/symbolic assumptions honestly: successful finite execution does not establish quantified claims.

Two remaining points are implementation tasks, not external blockers:
- Applied script identity already has a production Haskell path: conformance/app/Conformance/Run.hs apply/hash code around1566-1583 and imported SDK helpers. Use an isolated identity extraction from exact subject/artifacts or request an exact producer handoff through me. No Lean-side encoder is required to consume externally verified applied bytes/hash. The final f3 state program is parameterless; old-snapshot Apply([],body) evidence remains separate.
- blaster/run.sh currently stops after extraction and prints stale Q001/deps-unfetched text. Wire the actual committed assertion execution into the reproducible entrypoint, ensure a deliberately wrong expected result fails that entrypoint, and preserve raw logs. An all14-import success cannot be its terminal success while claimed checks are never invoked. Do not rerun settled extraction merely for activity; test the newly wired boundary.

Preserve I1 reachability/identity debt, full fold_iff both directions and all clauses, all14-title/full applicability coverage and both existing implementation layers. The abstract-side grading and multi-script span remain required. If a real capability/capacity limit remains, journal a standard BLOCKED with a named question or COMPLETE capacity handoff and exact remainder; an ambiguous HANDOFF tag alone does not close the run. You decide normal implementation through the existing owner/driver chain; no auditor. Acknowledge once and supervise the full remaining slice to a real terminal condition.
