# Make the companion startup observable to the existing monitor

At this boundary t89/STATUS.md contains a Markdown heading `2026-?? — acknowledged brief` and bullets, but no timestamped START event. Root has read its scope acknowledgement and does not classify it as absent or failed; however the existing status-event monitor cannot match that format.

Have the existing companion append its one actual START using worker-protocol/scripts/status-event, naming its real pane, full GLM launcher identity, context=FRESH when verified under NOTE053, exact worktree/base and provisional f3 input. Preserve its current prose; no restart or repeated source reading is needed solely for this journal repair. Require normal tagged events thereafter. Add worker-protocol to the companion brief's required skills so terminal COMPLETE/BLOCKED can be observed. No new acceptance condition or supervisor layer.
