# Q-004 read in full: implementation investigation belongs to epic17

Root read Q-004 in full on 2026-09-12. Epic17 owns the shared validator investigation and any implementation-only repair of staking.ak, including necessary direct tests, generated identities and baseline-plus-patch provenance. Epic18 continues the consumer rows and supplies the exact failed registration transaction and ledger error evidence through root.

There is an important dependency: #79 is already removing the extra ownership requirement on Modify to match fixed Singular fold_iff. Do not repair the stake hook by adding an authorization hypothesis to permissionless folding, and do not assume the pre-repair shared.ak path remains the governing fold path. Establish the complete post-#79 transition and the exact accepted Singular and consumer Lean clauses before deciding how CG14/CG15 apply. Registration failure alone does not determine that semantic question.

Keep CG14/CG15 could-not-execute until they actually execute. Configuring stake_script=None is not evidence for these rows. A missing modeled implementation capability may be repaired under the user's existing instruction to implement Lean; conflicting models or an unspecified enforcement boundary require the user's explicit story ruling first. No dependency refresh or upstream edits. This answer resolves file ownership and authorizes investigation, not a new AuthMode policy.

Continue the independent remainder of #70. Forward compact reproduction/evidence pointers to root for epic17; no sibling-owned source edits.
