# Resolve the remaining current-workflow versus PR-source mismatch together

Previous goal turn made progress: companion startup verified, original receipt comparison dependency fixed, publication gate merged. Current PR88 head597b010 reporting job103538203725/run34687890731 FAILED after coverage-check ran and reported INCOMPLETE because the next command `nix run .#coverage-tests` is absent from that PR source. Full log /tmp/pr88-reporting-597.log. This is not coverage debt.

Root inspected merged PR86 a95f2996b522326c3cbe0db3ab930bc6e4b13728:nix/coverage.nix: apps.coverage-tests exists there (alongside coverage-check and coverage-gate). The same integration class recurred because current workflow now includes PR86 while PR88 predates it. Reconcile the branch against current main once, retaining your workflow repair and the product-source fence. Before pushing, inspect all executable app references in the workflow against the exact checkout it will execute, and run the newly required coverage-tests command from that candidate. Do not drop/skip jobs, silently bless a missing app, repeat the entire already-settled gate suite without changed inputs, or cancel the live597 devnet run merely to hide its result.

Proceed to bounded delivery under NOTE052 on required current-head checks. Companion keeps its frozen preparation base; integrate subsequent consumer/CI delta mechanically at the final serial cut rather than silently moving its source horizon during implementation. One consolidated packaging check now should prevent spending another remote devnet run discovering the next absent entrypoint. Acknowledge once in STATUS.
