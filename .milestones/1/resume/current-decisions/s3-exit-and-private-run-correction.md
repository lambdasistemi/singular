# NOTE-034: Close two S3 execution-contract defects before the worker relies on it

S3 changes the right boundary and remains explicitly unaccepted. Root found two concrete defects in the new wrapper; close these with its existing implementation, not another metadata-only acceptance round.

1. S3 records verifier exit but never fails on a nonzero exit. Evidence directories and verdicts are reused. Root executed the EXACT script suffix beginning at the verifier invocation, with a verifier that exits17 and a preexisting verdicts.json containing stale ESTABLISHED rows: the suffix returned0 and printed S3 SUPPLEMENT GREEN. This is an adjudication-only counterexample, not execution of the full gate/devnet. Retained source, executable control, raw streams and result are under the path in root handoffs/s3-verifier-exit-probe-path.txt. Require successful build/runner/verifier subprocess exits, fresh output created by this invocation, valid unique verdict identities, and bind all run artifacts to this invocation. A file left by an earlier success cannot override a crashed verifier. Include this control when the real verifier is available.
2. `RUN_TMP=${S3_TMPDIR:-/tmp/s3-77-devnet}; rm -rf "$RUN_TMP"` deletes a caller-selected/preexisting directory. Use a freshly allocated private short mktemp directory whose ownership this invocation establishes. Do not remove a preexisting path or allow an environment override to select the deletion target. Refuse reuse of an evidence directory (or create a unique child), retain all raw evidence, and clean up only processes/resources actually created by this invocation. Do not remove evidence to satisfy cleanup.

The new verifier is still a real deliverable and must prove ledger facts, not simply emit required status names. Gate77v3 and S2 history remain preserved. Freeze the corrected execution contract and run genuine positive + effective counterexamples against its final implementation before claiming acceptance. No extra worker or auditor.
