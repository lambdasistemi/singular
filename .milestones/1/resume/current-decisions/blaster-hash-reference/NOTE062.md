# Classify the duplicate claim without a Lean edit or user decision

Root read A-OPEN-eight-rows-ruling.md in full and the actual declarations. Singular.escape_error at Lemmas.lean:321 and Singular.Statements.escape_refused at Statements.lean:79 have the same inputs `(s : State) (id : Nat)` and identical conclusion `step s (.escape id) = .error "completion-only-custody"`. Both prove it by rfl. Statements imports Singular.Lemmas. There is no semantic conflict or ambiguity to send to the user here.

The proposed choice between deleting the declaration and rewriting the proof is unnecessary. Keep BOTH accepted declarations and their proof bodies unchanged. Classify escape_error as a duplicate statement of the named public obligation, with explicit exact-clause correspondence, both stable identities, both source/definition bindings, and honest separate debt. Use an existing suitable disposition with rationale, or add an explicit metadata-only duplicate-statement category if needed. This is acceptance metadata under the current mandate, not a design ruling.

code-the-design already allows grouping/sharing a scenario when each precise claim is represented and checked; grouping does not drop identities or clauses. The eventual property and production-boundary checks may be indexed by both declarations only if they actually assert their identical full claim. This grants no execution credit now. Do not manufacture a user decision, new Lean dependency, removal or new product requirement to make classification finish. Full inventory remains192=109+83, and classification consistency is still blocked on NOTE060 repair.

Two corrections to the A-OPEN explanation: a literal-stripped `.error` is an incomplete/malformed statement representation, not a faithfully typed theorem saying some unspecified error; and the count of one affected signature is not complete — NOTE060 already demonstrates namingStepFoldActionEq's missing `naming-no-delete` literal, in addition to escape_error's `completion-only-custody`. Inspect whole declaration spans, not a fixed few lines. Both must retain literal contents after the repair.

The other seven statement-based dispositions may proceed, but their Lean proofs cannot discharge required implementation-layer debt. Pure algebraic claims need appropriate real API/composed-operation correspondence rather than invented ledger actions; leave any missing applicability/refinement evidence explicitly unresolved. Preserve all identities and debt. No new worker, auditor, semantic edit or test-scope waiver.
