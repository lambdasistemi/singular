# NOTE-046: Keep the continuous positive; finish mutation adjudication

Root inspected current v5 source and retained /tmp/pub-harness/run.log, case-positive.log, and the actual realized test-publisher executable y8ia31jh4jfd9zg0gasmlzs0d0h1p4rm. The positive NOW executes coverage COMPLETE -> actual on-chain assembly/checker PASS -> embedded publish script -> recorded create/upload/download with checksum verification. This is the required continuous path; do not reopen the resolved two-leg composition design or replace the real assembler. Preserve this evidence and its synthetic-coverage limitation.

Remaining concrete defects are in control adjudication. Root executed the exact current suffixes, source sha2561ad47019ee58639029cd4eea47ec3089ad7e5e6770efac74a12dee0218edacf3. Full retained probe /tmp/singular-publication-v5-mUqNxe; root reproducible driver handoffs/probe-publication-v5.cjs.

1. no-final suffix with simulated MUTRC127 and empty upload log => exit0, claiming composition proven. It never checks MUTRC, successful assembly/checks, or that the intended missing-invocation mutation was reached. Require the actual mutated publisher to finish the intended path (current genuine run is exit0), establish successful assembly/checks plus mutation-site evidence, then run the SAME positive upload assertion and observe its specific missing-upload failure. Command-not-found, timeout, setup/assembler failure cannot count as an effective mutant.
2. guard-removed suffix with simulated MUTRC124 and an unrelated line containing onchain => exit0. Require the actual intended transition past the removed guard and the attributed downstream result, not any keyword or arbitrary failure. Keep this distinct from the no-final control. Unknown exits must fail the harness. The unmarked/pre-integration case uses the same weak keyword and needs the same attribution discipline.
3. production gh discovery failed in the ACTUAL run: production program61gb0wxcw4nmxmfk5af7zjjgdy51n41s was only evaluated, not realized; nix path-info exits1/not valid. PRODGH is empty, and v5 still says production gh absent. Root directly checked the realized test closure: recorder n8gbksbsxk4nj89d5ypqig2bkh7w5vmy present, no gh package listed. The test executable also puts that recorder first in PATH. Keep that real evidence, but make dependency discovery/closure inspection fail closed and bind the exact resolved recorder used. A failed lookup cannot prove absence. There is no need to invoke any real production publisher or external write to establish dependency identity.

These are adjudication-only controls plus real closure queries, NOT root full publisher runs. Other current v5 refusing cases already have exact exit+attribution; apply the SAME discipline to mutants. Preserve original raw source/patches/logs and mutated provider contents/digests (current make_mut_copy destroys its source tree after use; keep the source/patch before cleanup so the recorded mutated closure has reconstructible provenance). Capture each gh call log before it is reset; the aggregate run.log currently preserves the positive calls, which must not be lost.

Relay to existing80c at a safe boundary; no new worker/auditor. Final submitted candidate must run its complete continuous positive and effective controls once with exact source/closure identities after these changes. Do not spend another round redesigning the successful path. #70b and #87 continue independently. Acknowledge in STATUS and retain the bounded positive progress explicitly.
