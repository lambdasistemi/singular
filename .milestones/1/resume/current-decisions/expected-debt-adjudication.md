# NOTE-045: The current expected-debt assertion accepts two unexplained failures

Root inspected clean candidate1d98d51afa168a2bc0fd0e15ab33993a706eee31 and executed its exact CI expected-debt adjudication suffix in isolation. Evidence: /tmp/singular-expected-debt-control-1F5pLb (source workflow, original/executed suffix, fixture, per-case stdout/stderr and result.json); reproducible driver at root handoffs/probe-expected-debt.cjs. Only substitutions were the installed jq launcher instead of nix run and private temporary paths. This is a fabricated suffix-level control, NOT a full workflow or ledger execution.

Baseline fixture with expected row set/verdicts, run_rc1 and held/failing accounting => exit0, as intended. Change ONLY run_rc to127 => exit0 and GREEN. Change ONLY CL01 verdict to could-not-execute (run_rc1) => exit0 and GREEN. CL01 here is the session capacity/measurement row, not cleanup; the probe's local variable named cleanup is just a misleading fixture-field label and has no product meaning.

The code tests run_rc !=0, not the known handled session-debt status, and requires CL01 presence but omits CL01 from per-row verdict checks. This contradicts A-001's requirement to reject build/setup/crash/unknown failures and to account for the actual expected run outcome. Main.hs catches exceptions and exits1; bind that intended result and the complete terminal/session evidence, do not accept arbitrary nonzero. Validate CL01's intended bounded verdict as well, preserving its full-inventory debt and refusing missing/unknown/divergent measurement evidence. Make the receipt directory invocation-fresh so stale records cannot supplement a partial failed run.

Relay this bounded correction to the SAME70b worker at a safe boundary. Do not interrupt its live fresh devnet run solely to edit CI; retain and consume that run's actual receipts/result. Repair adjudication with the actual retained counterexamples and genuine positive result; no rework of the already-green row/control write policy without a new defect. The required result is regression CI with EXACT declared expected debt and no unrelated failure, never conformance completion or closed held stories. Other lanes continue. Acknowledge and report the repaired candidate with controls; no extra auditor or worker.
