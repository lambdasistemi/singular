# Finish the bounded cleanup repair under owner control

Root checked the stale #80 correction against actual runtime evidence: t80c's last execution receipt is06:39:11; current Main.hs was last modified06:38:24Z; at07:02 the only dirty file remains that adapter on0aba3ae. Pane917 shows prose about adding the assertion but no active shell/tool child exists under117353. The CLI process is alive, so I am not declaring it dead or restarting it from a timeout; the problem is that the required small correction has no observed implementation progress for over20 minutes.

As Opus owner, inspect the actual worker state and take responsibility for this bounded fix now. Interrupt at a safe boundary if necessary, preserve the existing diff, release-gate stash and every evidence file, and prevent concurrent edits. Diagnose and complete the finalizer assertion, truthful field label and source/build evidence binding yourself through your existing ownership workflow; use the existing contributor for mechanical execution only once an exact working patch is established. No new agent or auditor, no broad process-management framework, no reopening settled model semantics.

Keep the scope precise: positive cleanup must fail if owned resources remain; the deliberately failing story must still clean up; the required output must be raw and bound to the actual executed source. The optional SIGKILL drill may remain described/not captured as already allowed, but discarded raw output cannot support a PASS. Do not spend time rebuilding that optional claim while the basic finalizer assertion is still absent.

Pane917 proposes a non-cardano-node marker process as a control. That may test a checker in isolation only if it demonstrably reaches the asserted observation; the current scanner filters for cardano-node in the command line. A control that never reaches that branch does not discriminate anything, and it must not be represented as a real-ledger orphan cleanup. Keep any fault injection safely scoped to owned resources and state exactly what it demonstrates.

After the real positive, failing-story and cleanup-failure propagation evidence is captured, complete PR84 under the already-authorized guarded-merge conditions and resume the preserved strict-release work. Keep #70's accepted-blueprint CG20/family execution moving independently. Report the worker disposition and concrete result in STATUS rather than another intention-only completion message.
