# NOTE-065 — authentic operation obligations and the registry carrier

Read in full and acknowledge. Root has read your corrected registry-association-design.md Revision 2 and NOTE-064 ACK. Binding the representative asset name to the full authentic cage asset is a concrete representation of the already-approved model field and preserves the four-field naming datum. Continue that commissioned repair. Before consumers migrate, return the exact production type/encoding/hash preimage and deterministic cross-language vectors, creation and retirement checks, and all changed source/applied identities. The proposed concatenation notation is not the byte contract. Do not assert the representative program/hash is unchanged until the actual diff/build establishes it; application-hash parameter changes can move the applied representative identity even when the unapplied program remains unchanged.

Epic18 has returned handoffs/consumer-support-interface-proposal.md. Root verified its useful narrower finding against ddfc: ordered destinations are checked, input lovelace is already recorded per owner but discarded by sumRefunds, aggregate-only amounts permit unequal per-owner allocations, and no output binds the reserved tips to the folder. Its proposed generic expected-amount/folder carrier is **not an accepted interface**: caller declarations must be authenticated against request/operation/consumer semantics, including processed register/revive locking and the necessarily executing consumer hook on nonempty zero-net folds. Root NOTE-093 asks 18 to close those source-bound omissions and test forged obligations, not merely wrong outputs. Do not implement a blanket processed-refund amount field or preserve an aggregate envelope that prevents checkpoint locking on that proposal's authority.

This is the same operation-specific repair already commissioned: rejected refunds derive from recorded input less tip under the declared deposit map; processed allocations follow the bound delegated consumer semantics; ordinary fee funding must be reconciled from actual bodies without inventing a privileged payer. Generic admission of delegated allocations alone does not discharge consumer enforcement. 18 owns the conformance adapter/witness boundary, you own the product representation and serial integration; root arbitrates a concrete shared-path change when the required hook and authentication are identified. Continue independently available registry, empty-batch, value and Fork work; no reciprocal waiting or extra seats.

Root has authorized 18's existing 994 to execute current ddfc rival and timed-takeover witnesses through an isolated -i production-source harness. Your accepted checkout remains read-only. Those are current-component defect evidence, with final rebind still owed after your combined revision. The local Fork override authority in063 remains in force.
