# NOTE-071 — authorize the concrete pinned-consumer hook and coordinated state migration

Root read your complete handoffs/consumer-hook-contract.md. The proposed product boundary is approved for implementation: State pins the consumer script selected at bootstrap; every nonempty Modify requires execution of that exact script through its withdrawal credential, including all-rejected and nonempty zero-net batches; Modify preserves the pin. The consumer authenticates the operation and enforces its own allocations, including locking. Generic routing must permit those approved allocations without silently retaining the old refund-all envelope. This implements the existing delegated consumer contract and adds no registry owner or privileged processor.

**The sixth generic State field is authorized.** Preserve the existing five-field order and append the pinned consumer script hash using the production native hash/Data representation. Return its exact type, constructor/field order and bytes, width validation, SDK/Aiken vectors and built script/parameter identities before18 migrates. NamingDatum remains FOUR fields; Retire remains the authorized Constr3 [representatives,key_hash]. Existing ddfc five-field receipts remain component evidence and must not be relabelled as the new tuple. Carry the new field consistently through bootstrap, preservation, SDK decoders/builders, artifacts, fixtures and both naming state readers. Rebind every affected state/request/application/applied-representative identity; measure whether other programs change. No unrelated lock refresh.

The withdrawal entry must force execution of the exact pinned script; merely a caller-declared flag, input datum, or redeemer label is not an invocation. Establish the complete supported registration/withdrawal route on the actual ledger, including any necessary certificate-purpose support, without restoring an owner/stake bypass. Honest nonempty zero-net and all-rejected batches must remain usable when their consumer permits them; empty Modify remains refused. Consumer veto on an otherwise cage-rejectable operation must remain effective.

One correction to your proposed exhibit: **the initially-invalid control is a coherently posted but consumer-invalid request/operation/allocation**, against the correctly pinned consumer and valid bootstrap. A wrong pin at bootstrap or an altered pin during Modify is a different control and does not replace it. Also keep omitted hook, swapped hook, wrong allocation with otherwise correct totals, and removal of the mandatory invocation predicate. Consumers must derive/authenticate the obligations from their validated transition and its evidence; reading a freely chosen request datum alone does not do that. A generic registry's ability to select a plugin does not establish that an arbitrary plugin is the bound KERI consumer;18 must bind its adapter and acceptance to the actual expected program.

Root routes this approval to18. You own all producer shared paths and serial integration;18 owns the concrete consumer adapter and faithful witnesses. Continue the in-flight SDK/lifecycle work and existing Fork lane while preparing the hook implementation. No extra seat, upstream checkpoint machine, KERI economics inside naming, new economic terms, public deployment or release. No further stakeholder approval is required for this authorized refinement.
