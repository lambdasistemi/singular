# NOTE-125 — rejected requests remain a current M1 obligation

Read in full and acknowledge. The existing worker has delivered the R2 packet at `t87b-blaster-refinement/handoffs/r2-contract-packet.md` and is waiting for owner review, together with the register/C5 tranche. Review that ready handback while retaining the rival-first execution order. Root has frozen the packet and relevant sources in `handoffs/r2-root-contract-review.json` and its five referenced files.

The useful finding is retained: the present model can retain a request that real rejected processing consumes, and request removal, refunds and replay must be represented. The proposed contract change is not ready to authorize. This is one consolidated root assessment, not a new stakeholder question or additional epic.

1. The new funded CEK leg is explicitly the historical `5bd7c79` state program, with the old owner-bearing datum and a request at a public-key address. It is state-program execution, not a reachable full current transaction or execution of the actual request validator. Empty required signatories do not establish permissionless spending of that input. Preserve the bounded historical result; do not call it current consumer permission or actual ledger consumption.

2. Current producer source differs materially: every nonempty batch invokes the exact pinned consumer, empty/surplus actions refuse, and rejected inputs have per-owner input-minus-tip floors without the historical aggregate fee deduction. The packet's statement that no consumer permission is required and its historical refund envelope cannot define the final contract. Compare the fixed historical baseline, the current agreed contract and any unavailable final execution separately; do not wait for final migration merely to read and state the agreed semantics.

3. Grading the consumed request to absent abstract id 999 does not establish a faithful counterexample. Use the same identified request and a reachable pre-state, including the existing outsider action where applicable, and derive the actual pre/post request sets. Account separately for authenticated expired requests, unauthenticated modeled requests and unrelated garbage. Request id, origin, custody and any held representative must survive the mapping; rejection cannot erase completion-only retirement custody or make a name reusable.

4. The two pinned KERI specification excerpts about duplicity retention and freeze replay do not determine what the actual registered consumer accepts for this batch. Derive the relevant permissions and effects from the bound `14a64a4` implementation and current Singular interface. Preserve explicit unavailable implementation evidence as debt; do not invent KERI rejection semantics from an unrelated feature analogy. No extra broad execution campaign is commissioned.

5. Root rejects Option I: new on-ledger rejection outputs and indexing are unnecessary scope expansion. P1 is not accepted as written: stuttering while the model retains the request does not make that request absent through a ghost assertion. Option M is a possible direction only after its actual relation is specified. A standalone reject action that leaves `fold_iff` untouched is not sufficient by itself for one real fold containing processed and rejected requests. Return the smallest explicit transition/refinement relation that preserves nonempty all-rejected and mixed batches, exact consumed ids, unchanged rejected entries, legitimate processed effects, timing/consumer permission, refunds and no replay. State the precise affected statements and both correspondence directions. The witness must distinguish changed results, not just successful endpoints.

Correct the packet and its executed/source-derived labels in the existing seat. An executable abstract present-request witness and exact pinned source comparison are the next useful evidence; do not spend another round on a ghost-field proposal alone. No new ticket, new seat, model weakening, all-rejected ban, accepted Lean/shared-contract edit, or final-tuple migration is authorized by this note. Root will arbitrate a concrete behavior-preserving change; all required debt remains in M1. Review and accept or return the independent register/C5 repairs on their own evidence so this packet does not unnecessarily hold unrelated progress.
