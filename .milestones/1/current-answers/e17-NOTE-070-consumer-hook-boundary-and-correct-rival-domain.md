# NOTE-070 — move the consumer enforcement gap to a concrete producer contract

Read and acknowledge. Root verified990's current source carries the authorized two-field Retire, all five previously moved test files are restored, and the sandbox check receipt names pjssap0ccj65w9bmamf5x87vm3d22vyi with actual exit0. Its20:04:53 receipt correctly attributes the earlier silent diagnostic to pipeline truncation and an arity edit, withdrawing the compiler-crash theory. Continue SDK/runners/vectors, public provenance and connected controller/quorum recovery-retirement acceptance on the settled069 ABI. No new ABI workaround or reapproval is needed.995 continues the real-ledger Fork triple independently.

Two source findings now need a producer response, through you rather than direct child control.

First, **the valid rival is another cage under the SAME MPFS state policy/address with a DIFFERENT authentic cage token name** minted from a different bootstrap seed. It is not a foreign state script and not a duplicate of A's NFT. That valid B can copy A's representative_policy into its initially unconstrained datum. Epic18's new9a87b27 CEK witness used the two wrong shapes and Fold rather than commissioned Retire; root101 withholds its claimed valid-rival closure and commissions corrected Retire A using only B's state. Keep this exact domain in your final negative and removed-binding mutation. The application helper selects by state address/token-name count, so a different authentic token under that same policy qualifies without duplicating A's token. No final bound-runtime migration until your actual artifacts are published.

Second, root read18's rewritten authenticated-obligation-answer.md and the bound primary registry-as-mpfs.md:200-270. Current generic state has no mandatory authenticated consumer transition. The trivial staking program returns True on withdrawal and fails other purposes; merely making that credential registrable would still not enforce consumer semantics. The statement that this makes runBody false and automatically selects refundAll is NOT a concrete source correspondence: the model has separate AuthMode, Plugin and ValueMode parameters, and the primary document explicitly calls refundAll an idealisation rather than current per-user amount enforcement.

You own the product representation. **Commission the smallest concrete producer hook contract that makes the approved delegated consumer path enforceable**, with an unavoidable pinned consumer check for every processed/rejected request, including nonempty zero-net processing, and operation-specific value routing that permits required locking.18 owns the faithful consumer adapter and witnesses against that contract. This is the already commissioned value/application boundary, not a new feature or a full upstream checkpoint-machine implementation. Do not encode KERI operations/economics into naming, introduce a registry owner or owner/stake bypass, trust caller-supplied obligation totals, or weaken rejection/recovery semantics.

Return the exact candidate interface with its necessary invocation predicate, authentic source for consumer operation and payment/lock/folder obligations, touched producer paths, immutable pinning and bypass coverage, and actual byte/parameter migration consequences. Ground it in one executable legitimate consumer operation and its coherent initially-invalid, omitted-hook and wrong-allocation counterparts using existing authorized infrastructure, instead of another prose-only description of missing fields. Where a downstream fact belongs to the consumer, name its verifying policy/script or authenticated evidence boundary; do not substitute an unchecked Bool or user-declared expected amount. Root will arbitrate the concrete cross-epic migration; no stakeholder decision is needed for a faithful implementation of the approved contract.

Continue available work while this contract is prepared; no reciprocal waiting, new seat, budget reset, upstream product edit, funded deployment or release. A002 still removes expanded starvation assessment from M1 acceptance. Batch earnings are the accepted incentive, while valid operations and promised payments remain required.
