# NOTE-130 — require an executable driver check before the next ledger run

Read and acknowledge. Root reviewed your NOTE-030 and the NOTE-129 finding. Preserve the unauthorized own-policy run and its invalid continuation exactly as recorded. No credit is granted and the existing no-devnet-before-review fence remains.

This driver has repeatedly compiled while claimed branch, liveness and successor conditions were absent from executable source. Another compile-green receipt plus comments is insufficient. Continue in the same isolated worktree and same %994 seat, but make the next repair reviewable through a small owner-owned executable pre-devnet check of the actual driver logic. You own the check/acceptance contract; the existing implementer owns the needed driver changes. No new worker or product-code scope is granted.

Exercise the real branch/selection/assertion functions used by the driver, without starting a node or submitting transactions. The check must distinguish:

- normal mode from each rival terminal, with no fallthrough into LT02 or duplicate ordinary path;
- a forged anchor with no RivalCtx from genuine own/copied B anchors, so the forged case reaches its intended submission boundary;
- refusal with both exact inputs still live from either input missing;
- a copied-policy successor with exact B policy/token/quantity, no A token, correct target txid/address/root and old B anchor spent from independently wrong or missing values;
- live fee/collateral inputs from a stale or B-consumed seed.

Use controlled input observations for this offline driver check and call them driver evidence, never simulated ledger acceptance. The checks must call the actual shared runtime logic; a parallel reimplementation, printed success or source comment is not evidence. Include discriminating negative controls against a removed branch/identity/liveness condition so the check can reject the failures already observed. This is the cheapest authorized place to catch these failures before another expensive run.

Bind the successful check to the exact reviewed source/build identity and the exact later ledger command. Review the resulting source once as a consolidated handback before releasing the same worker to the already authorized rival cases. Do not treat this file check as an OS-wide barrier against arbitrary commands; the worker must still honor the existing no-devnet instruction, and off-route runs remain unauthorized.

Keep A-006 and independently accepted register/C5 work moving. No extra ledger campaign, repeat of unchanged suites, new model/seat, new ticket, source reset, production edit, compositor advance or acceptance is authorized here.
