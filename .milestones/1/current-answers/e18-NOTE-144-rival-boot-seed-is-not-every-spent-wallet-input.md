# NOTE-144 — preserve the released run; repair the actual boot-seed assumption afterward

Root read the completed own-policy leg of the sole NOTE143 command: authentic B boots and is anchored, then the driver exits1 at `rival boot: unexpected multiple genesis seeds consumed`, before the target transaction. Frozen source/log receipt: `/tmp/projects/singular/milestone-1/handoffs/rival-root-multiinput-boot-review.json`. This is setup failure, not an own-policy refusal or registry-substitution result.

Do not abort, edit or repeat the already-released command. Let its remaining copied/forged legs finish and preserve all outputs/exits. Your owner review should classify each actually reached stage separately.

The source cause is concrete: `runRivalSetup` takes **every** genesis-wallet input that disappears across boot and demands exactly one. Actual `bootTokenImpl` in `lib/Cardano/MPFS/Cage/TxBuilder/Boot.hs` locates `cageSeed cfg` and selects `seedUtxo` **plus another wallet UTxO when available**. A consumed funding input is not a second registry seed. The returned `cfgB` already binds the selected seed used by the real builder.

After the current command is terminal and reviewed, the same existing %994 worker is authorized for a bounded offline driver/evidence repair: bind B's exact configured seed to the actual signed boot input/mint and derived B token; verify seed consumed and B distinct from A. Preserve all other consumed funding inputs as spent observations, and let the existing current-liveness selection prune them. Do not guess the seed from wallet-difference cardinality, use a caller certificate, discard valid live funding, or substitute a new B fixture.

Exercise a focused seed-plus-fee-input control through the actual shared/runtime decision path: correct configured seed is retained as the seed identity while both spent inputs are unavailable for funding, and every valid live funding input stays selectable. A wrong-seed selection must fail. Preserve the existing22/5 decision denominator and old failure evidence; new focused controls are named separately.

Also make the already-required retention concrete in the repaired driver. The current Main only emits a target txid and summary assertions; `connectedFoldTx` has no raw CBOR writer. Retain exact signed boot and target bodies, attempted record/anchor outrefs, and full observed pre/post UTxO data needed to independently read B's seed/NFT/address/root/value successor and refusal liveness. Do this in the existing isolated evidence path, with no new service/dependency/product change or ambient environment dump. Source assertions alone do not supply the missing bytes.

Because runtime source changes, rebuild and bind the revised actual executables and Nix wrapped source closure, run the applicable offline checks once on that revised candidate, and provide a new unexecuted exact command with fresh result-root protection. Complete your source/evidence review and send the frozen candidate through root before another ledger release. This note authorizes diagnosis and this bounded offline repair only: no second ledger attempt, auto retry, compositor, final producer migration, new seat/model/reset, accepted model/schema/product edits, commit/push/merge/release or epic acceptance. A006 and E17 continue independently.
