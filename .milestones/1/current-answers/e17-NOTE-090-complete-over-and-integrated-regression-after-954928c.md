# NOTE-090 — continue the remaining E17 outcome after 954928c

Root read your current RESUME, STATUS02:01:42 and the full `ticket-77/commit-owner-2/handoffs/note-024-026-public-reader.md`; verified clean commit `954928c7d3f0da866f00c972fc64b641ebb9982a` over e9d08c5. This is concrete progress. The latest recovered maintenance/retirement demonstrations remain bounded evidence, not epic acceptance.

The user goal is still **finish E17 and E18, then stop**. The current worker handback does not end E17's authorized work. Complete your consolidated review, then continue the remaining implementation using the existing implementer seat(s) and preserved context/worktrees; no new worker, auditor, model, restart or reset. You may rearm an existing parked implementer for substantive remaining E17 work after live identity/MCP checks and a durable ACK. Do not leave the epic parked solely because the completed #77 slice calls the required #74 work a different lane.

The next required outcome is the connected permanent-retirement journey: start with a genuinely claimed name, preserve the demonstrated recovery and controller/quorum custody routes, perform permissionless completion with the correct request/custody association and representative burn, observe registry Over, and prove withdrawal/reuse/resolution refusals from that same reached state. No direct-seeded Over, fixture-only burn, or waived consumer association. Preserve the correct processing/refunds/timing and six-field pinned-consumer contract. Stage and verify through the owned #74 scope; you own the precise slice ordering and may use the existing #81 implementer for genuinely independent regression work if warranted.

Resolve the integrated `lone_fork_insert_accepts_real_absence_witness` failure with its actual source/dependency provenance. Root read the retained postmerge failure: exclusion-root mismatch while inclusion and wrong-pre-root controls pass. The onchain flake declares that build/check/dev-shell all stage the same patched source. Retain the failing seed/source and prove whether the failing path actually staged the patch and whether the hardcoded witness matches retained producer evidence. A live E2E pass does not excuse a required failing regression; do not weaken/remove the test or relabel its meaning just to obtain green. Repair the actual defect and discriminate the wrong/unpatched case at the claimed layer.

Before final ABI delivery, correct the handoff's source mismatches: it says Recover redeemers are purpose-bound, but the reviewed reader's `recoverContinuation` still calls an untagged `findRecoverRedeemer`; it also writes the representative formula with repPolicy where the code uses the registry state policy. Preserve the source's honest bounded reader claims and derive the exact final full hashes/widths/order/vectors mechanically, rather than copying the abbreviated/typo-prone table. Final coherent producer bytes/tuple go through root before E18 migration. This is not migration/release/merge/epic-acceptance authority.

Refresh your own current resume with the actual UTC clock and current next action. Historical sections claiming uncommitted ddfc, unmerged old Fork1a6074f and old NOTE014 are superseded by the clean954928c state; do not hand successors contradictory current instructions. Keep all original evidence and histories intact. A006 and the separately released historical rival test continue in E18 independently.
