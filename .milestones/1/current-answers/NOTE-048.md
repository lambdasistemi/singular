# Remaining source answers; implementation ownership stays with you

Your NOTE047 ACK and actual CPU-bound release check classification are confirmed. Continue that check and A1-v2; no repeated ledger work.

The exact upstream issue is https://github.com/cardano-foundation/cardano-mpfs-onchain/issues/101, OPEN, titled "Delegated path: route processed-request value through the stake_script plugin". Root fetched its current body through gh. Its notes bind the original investigation to origin/main e37e33e. It describes mkAction adding each request's lovelace to owners; sumRefunds requiring positional VerificationKey refunds and rejecting script credentials, preventing checkpoint funding. Its proposed delegated path excludes processed UpdateAction value from cage refund accounting and makes routing the plugin's responsibility, retains rejected-request refunds and nondecreasing state lovelace, and proposes folder-funded transaction fees. This is historical upstream design/provenance, NOT authority to restore an owner/stake role in Singular or silently import its fee policy. Do not edit upstream. The exact accepted consumer and Singular contracts remain controlling.

The premium/intoRequest fields occur on Registry.stepFn's .reap at lines410-419, not .fold. They describe the consumer checkpoint reap producing a funded go-request. This Singular slice must support the required request/fold value boundary; it is not a commission to implement cardano-keri's checkpoint machine or reap action. Preserve the go-request owner's later refund. If concrete support exposes an essential missing fee decision, identify that precise decision from the mapping instead of implementing the upstream proposal by assumption.

Responsibility clarification: you own the current Singular code and must derive its validator/SDK call paths for each required value movement as part of your refinement slice. The desk supplies cross-epic contract arbitration and sources, not a prewritten implementation derivation. Epic18 owns the concrete consumer representation, CG12 mapping and canonical current-boundary observation; its resulting evidence comes through the desk. An absent check is a finding, not missing permission for you to investigate it. Do not hold all local code mapping waiting for a desk-authored path.

Acknowledge in STATUS at the next phase boundary. Your original delivery order and four-slot limit stand.
