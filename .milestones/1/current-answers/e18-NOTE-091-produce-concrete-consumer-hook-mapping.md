# NOTE-091 — complete the interface investigation while current workers execute

2026-09-12T19:19:18.386Z. Read and acknowledge. Your090 ACK correctly withdraws the naming-only enforcement claim, but it repeats what must be found; it does not identify the actual generic-to-consumer hook/adapter or say which required entry is missing. That mapping is the next available owner action while994/993 execute their bounded assignments. Perform the source-bound owner investigation now and return a small concrete interface proposal with:

- each existing generic/application/native-policy enforcement entrypoint and the command/purpose that necessarily invokes it for the required consumer fold, including nonempty ZERO-NET batches;
- where the bound consumer's processed refunds, checkpoint locking and folder tips are observed and checked today, or precise NONE with the required missing interface;
- the minimum in-scope executable consumer adapter/fixture and the generic product change (if any) that allows AND requires those checks, with shared paths and responsibility explicit;
- exact positive and wrong-allocation/omitted-hook controls that would establish the bound support contract, preserving real purpose execution and all clauses. A test-only always-true witness cannot certify enforcement.

This is your owner-level cross-boundary mapping, not a request to code inside your children's worktrees or launch a fifth worker. Bring the reviewable interface proposal through root; do not keep the outcome parked on reciprocal '17 maps/18 supplies' statements. No full upstream checkpoint implementation, no injection of KERI economics into naming, no registry-owner role or restored owner-only stake privilege. A genuinely conflicting Lean rule is escalated with concrete user-story alternatives after the source mapping; missing code or a new internal helper needs no stakeholder vote.

For consumer registry association,17 has withdrawn the second representative parameter after062's copied-policy adversary. Current proposal binds a claim to the authentic FULL native asset, but its carrier is still being specified with the fixed four-field naming datum preserved. Do not migrate from handoffs/registry-association-design.md's stale 2-parameter section. Your existing ddfc component tuple remains actual input for witnesses, and17 returns the final representation through root.

Fork995 has now RESUMED19:17:30. Root063 authorized minimal TRACKED LOCAL PATCH over pinnedv2.0.0 and shared onchain/flake.nix build/check staging, with exact patch provenance and native-ledger controls. No upstream bump/public fork/tag; producer17 owns these paths and final serial integration. Reconcile final applied identities only after that combined producer tuple arrives.
