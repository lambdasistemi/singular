# NOTE-124 — the parent rebuild must preserve production dependency assembly

Read in full and acknowledge. The six-program/source-relation repair is visible in the v3 verifier. Keep its review-only/pre-seal status and current worker order. This note closes a concrete future integration seam; it does not authorize migration before the final producer tuple.

Root read the actual preserved Fork candidate `f28563786dd4bf37312d6faa62deee3b7a06a9cf:onchain/flake.nix`. Production is not only the final `aiken build` command. Lines 94-98 apply the local lone-Fork patch to pinned MPF v2.0.0; `aikenPrelude` stages that patched dependency into `build/packages`, and both the blueprint derivation and checks run it before Aiken. The dev-shell hook stages the same closure. The patch and staging are required parts of the serial integration already owed by #17.

The current v3 `build_one` instead copies tracked files and invokes bare `aiken build` in the copy. `build/packages` is ignored, and no production dependency-staging step runs there. Copying the patch file does not apply it. Thus the current rebuild recipe does not yet establish the final producer's production bytes once that required patch is integrated; accepting whatever dependency Aiken resolves would lose the repaired behavior or produce a false mismatch against the proper seal. This is a source-level finding, not a claimed executed build difference. Frozen files/hashes are in `handoffs/registration-build-root-source-finding.json`.

Bind and execute the actual producer build route from the final pinned flake/lock and local patch, or a demonstrated byte-exact reproduction of that same dependency assembly. Include the staged dependency and compiler identities in the parent evidence. Apply the same production assembly to source-mutant rebuilds; do not silently compile mutations against a different MPF dependency, update expected hashes to an unpatched build, or rely on an ambient package cache. Retain the existing Fork positive/patch-removal evidence and complete the final integration identity/control checks at the normal rebind; no extra ledger campaign is requested merely for this observation.

The final producer handoff must provide its exact reproducible build/assembly and measured identities. Your sealed gate consumes that handoff after root release. No change to source ownership, existing seats, rival/compositor/wire order, full #87 requirements or the stop-after-both-epics boundary.
