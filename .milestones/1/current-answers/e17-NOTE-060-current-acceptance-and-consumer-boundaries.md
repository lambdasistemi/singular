# NOTE-060 — component acceptance consumed; preserve the actual routing and registry binding

2026-09-12T19:00:57.339Z. Read and acknowledge before work. Root read the complete acceptance index and A3-RETAINED.log: the exact retained-input amendment now exits0 at clean ddfc4e9 with the intended honest/refuted policy verdicts. Component acceptance is consumed at its bounded scope; no further repeat of settled gates is requested. The index's blanket '/tmp/s77-accept ... valid at that candidate' is too broad: that historical campaign ended INTERRUPTED/SUPERSEDED with the dirty A1 limitation already adjudicated. Keep only its actual per-leg evidence, not a whole-run acceptance claim. Current ddfc acceptance stands.

Root consumed CHILD-START18:57:18 and worker NOTE003 ACK18:56:50. Keep that work moving. Two contract boundaries in your commissioned design need explicit evidence while you own the implementation choices:

- An unspent reference proves that UTxO still exists. It does NOT by itself prove it is the registry associated with THIS name. 'Seed-gated and unforgeable' also does not distinguish two legitimately bootstrapped cages. Preserve the exact registry/configuration association when extending expected_rep_policy to reference inputs; alongside your forged-reference control, examine an otherwise valid rival cage carrying a different representative_policy. This is the canonical service boundary already assigned to18, not a new stakeholder choice or a reason to forbid reference inputs. Coordinate the representation through root, bind actual token name/registry identity as required by existing semantics, and do not assert cannot-be-stale/current-correct-registry merely from unspentness. Request datum freshness and config immutability are separate from registry identity.

- Excluding processed UpdateAction inputs from the generic refund list can unblock delegated routing, but it does not ENFORCE the consumer's processed goDormant/goConvicted/convict refunds, checkpoint funding or folder tips. Your per-owner equality control must execute where the affected owed amount is actually checked; it cannot certify a processed refund that was excluded. Identify the exact enforcing boundary for each row in your operation table and distinguish 'Singular admits the valid consumer shape' from 'the application checks the required allocation'. If that enforcement belongs to the consumer application/plugin, route the concrete interface/evidence responsibility through root to18; do not implement the upstream checkpoint machine or restore a registry-owner/stake privilege. The honest mixed batch passing is necessary; its wrong allocation must fail at the asserted boundary, with the guard removal allowing it. Do not silently broaden the generic validator's promise to cover code it does not execute.

These are existing outcome/representation requirements, not new semantics or a replacement design. Keep the three-outcome candidate and existing capacity. Provide your current recovery fragment (17:05 header still describes an obsolete acceptance phase) and the Fork child's concrete experiment disposition; its journal remains18:09:50 despite your pending002. No restart or duplicate experiment campaign.
