# NOTE-139: v2 returned; make the future ledger command concrete

Read this note and receipt in full and ACK. The same worker returned NOTE038 at 01:27:44 with v2 GREEN and stopped at 01:27:48. Preserve that genuine bounded progress and every earlier setup/lexical failure. Complete the already acknowledged NOTE138 repairs in the same worker before any ledger release.

The completed command handback is not the required exact official-app invocation. `handoffs/rival-ledger-command.sh`, SHA256 `8920802181204e1647f81c74428f5a22db85f304eac72be9522b4e04d21aa6b6`, uses three `nix develop --command cabal run retirement-rows -- ${RIVAL_RUN_ARGS:-}` calls. It sets neither NAMING_BLUEPRINT nor MPFS_BLUEPRINT; Main requires both environment variables before starting the run. The existing Nix retirement-rows app wraps the executable with the pinned cardano-node PATH, while this command bypasses that app. Extra unspecified CLI words do not bind those required artifacts.

Root receipt `handoffs/rival-root-command-review.json` preserves the exact command, gate output, Main and flake source identities. No command or ledger variant was executed. The v2 gate checks this file only for existence, executable bit, shell syntax and hash; its GREEN therefore does not establish that the future run is complete or follows the commissioned official route.

Return a concrete, self-contained three-variant command using the existing wrapped Nix application built from the exact reviewed isolated driver source. Bind the actual naming/MPFS blueprint paths and digests, wrapper/driver identity, required environment, and separate retained outputs for own-policy, copied-policy and forged-anchor. Build and inspect the required artifacts without starting a node. Remove the RIVAL_RUN_ARGS placeholder and ambient dependency assumptions. Preserve the actual Nix source inputs, including newly added shared/check files, and distinguish the v2 cabal binaries from the actual wrapped executable instead of assuming their byte identities are equal. Record any required tool environment for the gate; the observed python3 setup failure and -O0/list-bin distinction remain evidence.

Combine this with exact failed-subject parsing/budget discrimination and the actual A NFT policy+token check from NOTE138, then return the consolidated source/build/control/command handback. Keep the accepted 22/5 checker and v2 evidence; no unchanged suite rerun merely for narration. The later ledger command remains unexecuted until its existing explicit release. No product/model/schema change, compositor advance, new worker/model, reset, commit/push/merge/release or epic acceptance is authorized here.
