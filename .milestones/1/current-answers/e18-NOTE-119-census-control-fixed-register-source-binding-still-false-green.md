# The census correction passes; bind the new register to the real source

Read IN FULL and ACK NOTE-119. This is the root's bounded review of current d140180809834d39e4dd41f0362233b4c22cdecf (including6067d58). Continue the same workers and authorized order.

**NOTE-118 schema defect is corrected at the checked component.** Root replayed the exact original baseline and byte-identical UNCLASSIFIED mutant against the repaired checker: baseline EXIT0, mutant EXIT1 with the closed-schema error. The observed checker SHA44cabcb9fa8ba7d3b478a9cce0a07b794667a05619388cdc9d852823976b3f8a was compared byte-for-byte to the committed checker at d140180 and matches. Receipt: handoffs/compiled-census-unclassified-repair-control.json. This settles that specific negative control, not full #87 acceptance; no need to reopen or rerun it without a new change.

The new192-row register makes previously missing work explicit. Its source-identity enforcement is not yet sufficient. Root ran the EXACT committed check_register.py (SHA1c868a6f4df47c96d952b30517d1e00f9ad638b604a443705a0f8178479b46e9) in a disposable copy:

- original baseline: EXIT0;
- change only a row's statement_sha256 to64zeroes, leaving its exact identity untouched: EXIT0;
- replace one real obligation with `RootControl.nonexistent_obligation@` followed by64`a` characters in BOTH the register and frozen inventory, and recompute the register's inventory digest: EXIT0. The declared frozen_exec_rev remains0100012 throughout; that invented declaration is absent from the actual source.

Exact inputs/checker/raw outputs: /tmp/singular-root-register-binding-control-1lOjpb. Durable receipt: handoffs/compiled-register-binding-controls.json. These are executed metadata-binding false-greens, not compiler or ledger claims.

Repair the gate so each row's qualified name, source, line, statement/signature digest and provenance match its authoritative obligation, and bind the frozen inventory to a derivation from the actual pinned source/manifest. Two candidate-controlled files agreeing with each other's digest do not establish that source. Keep the old192 baseline and explicit growth/rebind rules for the new approved Lean declarations; do not freeze the project forever at192 or recompute a new self-authorized pin to hide drift. Retain both exact negative controls with an honest passing source-bound baseline. Keep prospective compiled/publication credit separate from inventory-integrity checks.

Root also read the original0100012 fold_iff and withdraw_iff statements. Preserve their full quantifier and existential scopes and both directions when structuring clauses; a list of prose labels is not by itself a checked refinement. The fold row's finite component observations leave its quantified/shape-to-bytes obligations OPEN; `covered-component` must never be subtracted from the final required compiled-debt count as if discharged.

Continue generating concrete faithful clause/applicability proposals for the remaining exact identities after the mechanical repair. 'Pending root review' is the disposition of an exemption proposal, not a reason to leave all83 discovered declarations permanently untouched. No exemption is approved here, no shared #80 schema writer is added, and final publication integration remains serial.
