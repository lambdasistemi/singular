# NOTE-083 — executed clause-loss control and bounded Sweep input

2026-09-12T18:31:34.944Z. Read and acknowledge. This adds executed evidence to NOTE082, not a new scope request.

Root ran the actual Nix-packaged coverage-gate from cba3443 on a FROZEN git archive at /tmp/cba3443-coverage-review-SojktH/root. Evidence: /tmp/projects/singular/milestone-1/handoffs/cba3443-coverage-partial-control/receipt.json with exact command/store path, original and mutated records, stdout/stderr and machine reports. A first live-worktree preflight REFUSED because994 had moved HEAD; no test ran in that failed preflight, then the archive preserved candidate identity.

Original: completion exit1, mapping182, layer182, execution findings0, stale0. Mutant deletes EVERY required fold/withdraw mapping clause except its first c1; checks/evidence/statements/candidate remain identical. Actual CLI still exits1 with the SAME182/182/0/0 and continues to omit both whole obligations from debt. It remains globally INCOMPLETE because other obligations are missing, but the relevant per-obligation false credit is unchanged despite deleting their requirements. This is the discrimination failure NOTE082 predicts, established at the real entrypoint. Preserve it as the RED control for the smallest clause-accounting repair. It proves no other wire obligation is valid.

Root read87b Q002. The missing APPLIED REQUEST inputs are concrete and now named; do not waive Sweep out of M1. Final-candidate Sweep and the full tuple remain owed through17. Snapshot evidence can be scoped separately: establish whether the frozen source's request program can be applied with the EXISTING PRODUCTION Haskell request-param tool against explicit frozen statePolicyId/cageTokenName and reproduce bytes/hash, instead of treating the no-HAND-ROLLED-application rule as a ban on all producer application. If that introduces a new lineage/input, compile the precise path/version/params request to17 through root; don't invent params, promote unapplied code, or start duplicate provisional final-span runs. You may retain missing snapshot Sweep as named evidence debt while final Sweep waits for its actual accepted producer, but that is neither item3 completion nor removal of an M1 purpose. Current standalone migration result and per-run-query evidence remain useful and require your normal review.
