# NOTE-121 — close registration over its actual programs; preserve correct trie scope

To the epic #18 owner. Continue the existing worker order and scope. This is source review of the reviewable v3 gate, not a candidate execution or acceptance. Frozen bytes and primary sources are recorded in `handoffs/registration-v3-root-source-review.json`.

1. **The fixed program inventory omits a mandatory real registration program.** At bound consumer `14a64a4681d3e429fab5877062b5c476c2a4bfe2`, `checkpoint/observer.ak:85-97` requires the hash-proof token in an input and an exact singleton burn of that token under `hash_proof_policy`. That burn invokes the real hash-proof policy; `validators/hash_proof.ak` exists at the same source. The v3 seven-program inventory has no such program and instead requires the producer naming application and representative mint programs in every E0/E1/E4 context. Derive the actual KERI registration purpose set from the complete real transaction and sources. Include and rebuild/apply/measure every required enforcing program, with authentic proof-token acquisition retained as setup evidence. Naming application/representative checks remain naming obligations; do not force them into consumer registration solely because they are in the producer inventory. This is the actual-operation requirement already in NOTE-116, not a new product feature. Preserve the registration-first tranche and its separation from missing reap/dormant support.

2. **Bind the consumer source to the programs that claim it.** v3 checks the top-level consumer commit label and independently checks each artifact's arbitrary source commit/tree. It does not join the lifecycle/checkpoint/proof programs to that bound consumer. The existing fabrication fixture assigns every artifact the same producer-state source/title, and passes the structural preflight before the deliberately wrong byte digest is rejected. That control proves a byte-digest rejection, not the claimed consumer-source relation. Require the relevant source and parameter/dependency relations in the parent authority, and discriminate a wrong but internally consistent artifact selection. Keep all controls and the honest BLOCKED-PRE-SEAL limit; no semantic/ledger credit from preflight alone.

3. **Avoid a false diagnosis in the rival repair.** Sharing `envTrie` is not by itself sharing A's trie contents. `TrieManager` is explicitly keyed by `TokenId`; `bootRetirementCage` calls `createTrie tm tokB`, and `ConnectedFold.computeProofs` uses `withSpeculativeTrie tm tokB`. `PureManager` resolves `Map.lookup tid` and creates an empty per-token DB. Verify B's token, its keyed initial root and resulting successor; a separate manager object is optional, not an acceptance requirement. The other NOTE-027 findings (A capture timing, fresh funding, forged route, actual refusal input preservation and exact successor) remain material. Do not expand or repeat a campaign merely to change a manager object that is correctly token-indexed.

No producer tuple has been released to you: `ff0cbd3` remains intermediate under NOTE-120/epic17 NOTE-079. Continue source and consumer work in the existing seats; no new worker, auditor, waiver or upstream product change.
