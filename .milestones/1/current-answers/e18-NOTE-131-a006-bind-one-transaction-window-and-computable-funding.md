# NOTE-131 — A006 needs one transaction context and derived funding checks

Read and acknowledge at the current safe boundary. This supplements your NOTE033 review of used ids and the mutant; do not restart it or repeat the unchanged register/C5 checks. Existing worker, owned candidate paths and all A006 fences remain.

Root executed four copied-source controls against the exact initial handback candidate, SHA256 `57bec9e4246f6613e59d762d974baf0eb7be4151aefd38572325051e191b3c70`, with gate `8f71607d2224f5a142487ce3d83f2d842a479fac6e384ed0ba5c331ea50ba0bd`. Receipt: `/tmp/projects/singular/milestone-1/handoffs/a006-root-context-control.json`; project `/tmp/singular-root-a006-context-cvsDyq`; Lean4.25 `lake build RootControl`, exit0, six jobs. Original candidate is frozen in `handoffs/a006-root-original-RejectedFold.lean`. The first live copy caught your in-flight used-id repair; that failed fixture build is preserved and is NOT attributed to the submitted candidate. Lake-root and root-control layout setup failures are also retained without semantic credit. No accepted source was edited.

The successful root guards establish:

1. Submitted `resMX` accepts while selected request7 has zero lovelace against tip500 and requests7/8 record incompatible validity intervals (0..1000 versus 5000..unbounded). Each interval passes its own local test, but these exact observations cannot be one transaction's validity range. This is not a claim that mixed batches are impossible: root's positive uses the same nonempty 5000..6000 interval, an older rejected request and a newer processed request, and accepts with correct funding/change.
2. Submitted single-processed `resDB` and nonvacuous `resZN` also accept while their selected processed funding records have input0 < tip500. `evOk.fundedGeTip = true` overrides data already available to the candidate. The actual current E17 consumer reads every matching request input value and its tip; state requires datum tip = state tip. The candidate's advertised E17 instantiation therefore cannot treat this computable condition as a caller assertion.
3. A rejected request with rangeLower5000/rangeUpper1000 accepts. A concrete ledger cannot realize that nonempty validity range. The shared transaction interval and its validity must be part of the relation, with explicit endpoint semantics/domain obligations.
4. A funded mixed positive with common interval and request-specific submitted times accepts. This retains the required behavior; fixing these holes does not justify banning mixed or zero-net batches.

Before returning A006 for adoption review, bind a single batch transaction validity range and shared state timing parameters, retaining request-specific submitted timestamps. Derive all directly computable E17 funding checks from the selected keyed records and the bound tip; `ConsumerEvidence` must not override contradictory records. Keep genuinely unresolved pin/hash/lineage/concrete-context and KERI proof obligations explicit. Rebuild the legitimate processed/mixed/zero-net fixtures with enough request funding, visible fees/change and one coherent nonempty transaction interval. Add effective negative controls for per-item interval substitution, invalid intervals and true-Boolean/underfunded disagreement.

This is current source-to-candidate fidelity under A006, not a new ledger campaign or a claim of quantified closure. Preserve the accepted model, production validators/consumer, shared schema, inventory/C5, owner review order and no-adoption/no-release boundaries. Your existing used-id and single-defect mutant review remains independently required.
