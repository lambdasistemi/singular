# NOTE-038 — NOTE-150 reachable admission histories and orphan discriminator

Read in full and ACK in `STATUS.md`. Resume the same `%993` process, context,
uncommitted tree and A006 ownership at its declared review boundary. NOTE-037
was already durably read and recorded at `2026-09-13T03:31:00Z`; do not repeat
its unchanged proof routes or five-program control. This is the remaining
NOTE-035/036 admission/discriminator block, not a new campaign, model change or
permission to commit.

Root's full frozen review receipt is:

`/tmp/projects/singular/milestone-1/handoffs/a006-root-general-correspondence-review.json`

SHA256:
`14f99046e82e12e70911083562762455a7ff0ed09178b67c1f42759a2125096f`.

Its copied package is `/tmp/singular-root-a006-corr-KeijoX` under pinned Lean
4.25.0. The accepted model authority remains
`13231f58833b8feb57f4b0f9b1117bfcfba0c07d`; do not alter it.

## Preserve the proved general layer

Credit and retain the current candidate-internal general correspondence:

- `Corr.lean` SHA256
  `e677e457fd3392a5d9b81bfb8703069e1411a5ac62899d9d1a5bc7e5bb5daf7f`;
- arbitrary list lengths in both directions through `dirB_go`/`dirB_top` and
  `dirA_go`/`dirA_top`;
- full candidate result, frame/removal/refund content and the current
  custody/used bridges;
- standard axiom footprints only (`propext`, `Classical.choice`, `Quot.sound`),
  no `sorryAx`.

Do not rerun or rewrite this unchanged proof merely to reproduce root's
successful nine-job `lake build RootCorr`. Retain the first missing-dependency
package failure separately as setup-only/no credit. This proof is
candidate-internal and unadopted; it is not actual-program, CEK, KERI or
accepted-model correspondence.

## Remove reachability credit from the exact old prestates

Root's nine-job `lake build RootWitness` proves with unchanged
`reachable_inv`:

```lean
not Reachable RejectedFoldGate.sZNBase
not Reachable RejectedFoldGate.sCBase
not Reachable AdmitTrace.sRel
```

Each exact state has representative supply `1` for key `42` while its registry
entry is not `Active`; every accepted reachable state is `WellFormed`.
Preserve the old fixtures, their finite candidate/action-kind results and the
root refutations as diagnostic evidence. Remove every claim that their
insert-then-delete commentary, custody state or release state constitutes an
admitted history. Do not weaken `reachable_inv`, change the accepted model, or
patch these prestates into apparent validity.

## Build actual admitted histories

Construct executable Lean histories beginning at `Reachable.initial`. Chain
only successful accepted `Singular.step` results, carrying each produced state
into the next step, until you obtain the exact candidate base states needed
for:

1. a legitimate mixed processed/rejected batch;
2. a legitimate nonempty zero-net user outcome; and
3. legitimate retirement custody, including the funded positive required by
   NOTE-036.

The history must expose its concrete actions, required actors/inputs and each
successful result. Do not directly patch `used`, insert an application, seed a
final registry, fabricate a retained representative, or start from an
arbitrary final-shaped record. Bind candidate funding and custody observations
to the exact requests admitted by that history, then run the candidate and the
existing full relation on those exact resulting states. If an old fixture's
exact state is impossible, retain that refutation and replace its claimed
positive with a genuinely admitted history that demonstrates the same user
outcome. Generic `Delete` stays generic; this grants no naming-Delete semantics
or scope relaxation.

## Integrate the distinct unselected-orphan control

Exercise the actual candidate and relation with a valid selected rejected
request `8` plus a distinct unselected, unspent custody request `99`:

- with the exact funding binding for `99` present, the legitimate positive
  must accept and relate;
- changing only that fact by omitting funding `99` must return exactly
  `custody-unassociated`, and current `FullCorr` must be unable to relate the
  orphan state.

This is separate from the older `R-custody-unassociated`, which replaces the
selected held request's binding and would also expose a custody-unbound defect
if the entry guard vanished. Retain that old bounded case, but do not
substitute it for this unselected-orphan discriminator.

The new control must compile and execute through the real candidate/relation,
with true exits and result-specific assertions. Add a behavioral mutation or
equivalent negative showing the control goes red if either the candidate guard
or the corresponding relation premise is erased. A caller Boolean, source
grep, missing import/module, failed build or unrelated rejection is not
discrimination.

## Handoff boundary

Keep NOTE-037's production disagreement explicit: preserving unspent custody
in A006 is not agreement with the current producer and does not close the
retirement-completion bridge. Grok/E17 owns that repair. Actual Aiken bounds,
both implementation layers, KERI instantiation, authentic producer custody and
separate completion remain open.

Stop uncommitted for consolidated owner/root review. Hand back exact source
identities, complete commands, raw outputs and true exits, the three original
unreachability dispositions, the admitted histories and their candidate commutation,
the exact funded/orphan pair and mutation discrimination, plus remaining debt.

No new seat/reset/model, unchanged proof campaign, accepted-model or schema
adoption, register/C5 rerun, final tuple migration, node/ledger replay,
commit, push, merge, release or epic acceptance. `%994` continues independently
under its existing offline/no-second-ledger fence.
