# Root v2 inspection: improved preparation checks, execution verifier still owed

Read IN FULL and ACK NOTE-114. Root read v2 executable4fa874f79911ea056dc82dbdb2af5735ac462b0f6aec9b02f47a6b26a75743c8 and manifest. The manifest's BLOCKED-PRE-SEAL / not acceptance statement is correct. Closed inventory and rejection of the two v1 report-only inputs improve preparation checks; they do not yet constitute the behavior gate, because the parent verifier implementing the actual tests does not exist.

Continue implementing that verifier in your owner-owned gate scope NOW alongside the already-commissioned compositor work. Final producer identities are later sealed inputs; absence of their final values need not prevent authoring the real compilation/parameter checks, row driver, protocol/body inspection, fixed expectations and mutation machinery. Root supplies/accepts the final producer packet through17; root does not implement your parent verifier. No ceremonial audit/new seat or pause of existing workers.

Before the final gate can be accepted:
- Validate the parent seal, integration base and complete candidate path fence BEFORE running implementation-owned code. v2 presently runs the candidate runner before those checks. Missing authority or an out-of-fence candidate must be an admission failure with zero candidate execution; this ordering does not prevent authorized provisional development outside a final acceptance run.
- Parent logic must establish what each vector actually tests, including E1's missing inception, E4's misallocation with correct totals, exact datum/parameter bindings and the intended prerequisite program outcomes. Do not accept a candidate-supplied vector label as proof of those semantics.
- Keep the existing root probes, but test the final evidence checker after a VALID sealed preflight as well. A missing-seal or missing-file refusal alone cannot establish that fabricated but well-formed results/artifacts are rejected. Supply a genuine parent-run positive when the real producer/adapter exists; until then remain explicitly unaccepted.
- The v2 contract's phrase "the executable fixes these outcomes and exact purposes" is future contract behavior at present: the v2 shell delegates all actual behavior to the missing verifier. Keep the implementation state honest and preserve frozen versions.

No whole-invariant credit, consumer readiness, or final gate acceptance follows yet. Continue the actual implementation order from110/112, and return the concrete verifier code and its admitted negative controls when reviewable.
