# NOTE-123 — resolve the rejected-request mapping as an existing M1 outcome

Read in full and acknowledge. Continue the current register/C5 repair and rival source correction. This note concerns the already recorded R2 refinement gap; it commissions no new epic, worker, public question or broader feature.

The existing M1 outcome includes valid nonempty all-rejected processing when the consumer permits it, with the required refunds, unchanged registry contents and no replay of consumed requests. Empty processing is separately refused. `MAPPING.md` says the compiled Rejected path consumes a request and refunds it, while the abstract empty-fold keeps it, and calls resolution a future user story. That description cannot defer a required current behavior beyond M1 or discharge it by an empty-fold grading. It is especially incompatible with the approved nonempty guard if real consumed work is silently mapped to zero items.

The actual model also has an `outsider` action that puts an unauthenticated request into state; therefore a blanket claim that every rejected request lies outside the modeled/reachable domain needs evidence. Determine the precise relation, rather than assuming either a design contradiction or an acceptable exclusion.

After the current C5/source-binding boundary, bring root a concrete R2 contract packet: the exact reachable setup (or demonstrated unreachability), request origin/type/timing/consumer permission, actual consumed inputs and refunds, pre/post abstract state under the proposed grading, the exact model statement and clauses affected, and the behavior-preserving repair proposed. Compare the relevant generic Singular and bound cardano-keri rejection semantics from their pinned sources. Separate garbage outside the modeled registry from a modeled request that a valid nonempty batch consumes and refunds. If projection/stuttering is proposed, define it and account for the request-consumption/replay and nonempty obligations; do not simply erase them from the comparison.

Do not change the accepted Lean behavior or restrict legitimate all-rejected processing merely to make a proof pass. If a conservative model extension or an implementation correction is needed, present its exact scope and effect for root arbitration before changing that shared contract. Keep R2 and every affected invariant as explicit M1 debt until the actual relationship is established. The existing stakeholder story and settled rulings are sufficient to investigate; no stakeholder decision is pending merely because the mapping is unfinished.

Root will decide the cross-epic contract correction from that concrete evidence. Continue within the existing seats and paths; source binding, full applicability, both equivalence directions, whole quantified obligations, final-artifact rebind and all other M1 acceptance remain required.
