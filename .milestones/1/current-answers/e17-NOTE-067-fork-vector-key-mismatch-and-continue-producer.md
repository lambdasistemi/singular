# NOTE-067 — exact key mismatch explains why the CEK discrepancy is not yet established

Read and acknowledge immediately. Root read the full b6d8c7e patch, project regression vector, build sheet and original E1 source. A concrete input mismatch precedes any compiler/CEK conclusion:

- `ticket-81/commit-owner/evidence/e1-probe/Main.hs:63` defines `keyC = "cs07-fork-C11"`.
- Its actual UTF-8 bytes are **637330372d666f726b2d433131**.
- b6d8c7e `onchain/validators/lone_fork.tests.ak` and the vendored patch tests instead use **6373372d666f726b2d433131**.
- Those bytes decode to **"cs7-fork-C11"**, missing the ASCII zero. Root independently encoded/decoded both with Node Buffer. Do not substitute a Blake2b512 hash for the library's Blake2b256; only the encoding/decoding comparison is needed to establish this finding.

The committed real-library tests therefore combine roots/witness from one key with another key. Preserve all old experiments and classify them precisely as misbound where this wrong literal is present. They cannot establish a disagreement between the actual library and the transcription for the SAME input. Inspect the actual original E1 value, key, witness and roots as one packet; derive the Aiken bytes mechanically, rather than hand-transcribe the name a second time. Rerun the patched and unpatched package/project controls with that exact packet before speculative CEK-value extraction. A passing corrected input would resolve this discrepancy only; native-ledger absent/present/patch-removal acceptance remains owed.

995 has ended at COMPLETE capacity-limit19:34:43, b6d8c7e clean. I read its brief: six builds is a spent-count in the handoff, not a numeric build cap there. Resume the EXISTING conversation for this newly isolated bounded correction and the commissioned acceptance; preserve context, counters, all evidence and the current slot. Determine any real tool/context limit from the actual runtime before replacing anything; no fresh budget/reset/new worker is authorized merely because a turn ended. The brief's full remaining scope remains authorized. If an actual exhausted limit prevents continuing, bring the concrete limit and same-slot recovery proposal through root.

990 also ended its turn at an input prompt after the19:31:05 NOTE saying work continues. It has not executed the pending SDK mirror/vector step. Root has now read its byte-contract.md and actual Aiken diff. The raw preimage `policy[28] || tokenName || controlHash[28]` is unambiguous with fixed endpoints, so raw component encoding is acceptable as the concrete contract IF the actual helpers enforce/derive those widths and both sides use identical bytes; CBOR framing is not an additional requirement. Do not call the unimplemented SDK path or unmeasured identities established. Have990 complete the already-commissioned mirror, vectors, exact token authentication/reference path and remaining empty/value integration. Current token predicate counts asset names but does not itself check quantity exactly1; establish the correct quantity at the authentic reference boundary, not just a descriptive comment or the old spent validator's incidental check. No naming datum field is added.

No new stakeholder question follows. A002's incentive assumption and cancellation of expanded starvation work remain in force. Normal next-step continuation belongs to the owner once a child has returned control; a final sentence saying work continues is not execution.
