# NOTE-138: bind the runtime refusal subject and the actual A token

Read this note and its receipt in full, ACK in the epic journal, and fold the findings into the same worker's current offline review. Preserve any command already in flight; do not restart it or create a new seat.

The draft Main now contains all five shared calls and the check executable is in cabal. This is concrete integration progress. Root reviewed the actual dispatch, anchor, successor, refusal and funding sites, frozen at `handoffs/rival-root-wiring-observed-Main.hs`, SHA256 `6f57c91a8a75f3d011f44ec76e0d78cee1d89e5192fa9f7efa0b9ad5dfa83445`.

1. The typed refusal branch still accepts `PlutusFailure` plus `isInfixOf (envAppHex env) (show reason)`. This does not identify which script failed. Root compiled/executed that copied predicate against the retained actual consumer failure whose named failed script is e6145504...; both the real subject and the wrong application hash dfd33801... return True. Receipt `handoffs/rival-root-wiring-attribution-review.json`, full source/raw outputs `/tmp/singular-root-rival-attribution-1Usi8C`, retained raw failure SHA256 `7d3d5ac97a03a19b4330a17176901323690355fa4efc24baddc61bfb0b06456e`. This is executed predicate evidence using a real message, not a new rival ledger run. Parse and compare the exact named failed script field, distinguish budget exhaustion from semantic refusal, and retain a focused control that accepts the genuine subject and rejects the same raw failure attributed to the wrong application. The existing E17 failure-attribution repair/fixtures provide concrete wording; no dependency refresh or producer migration is needed.

2. The successor field `sfATokenExcluded` currently derives from `aPolicies = [envAppPolicy env, policyID (envRepPolicy env)]`. Those are application/representative policies, not A's authentic state NFT. Derive this field from the exact A state asset identity captured before B's boot, comparing policy AND token name. A and B share the state policy, so rejecting that entire policy would also reject legitimate B. The other exact-B/one-asset guards may jointly exclude A already; this finding is that the separately named runtime fact does not observe what it claims. Preserve the exact B policy/token/quantity, full target TxId, old-anchor consumption and root checks.

Keep the accepted direct checker and frozen 22/5 denominator, the current actual Main/cabal/command/v2 block, all prior failed attempts and source identities. Complete source/behavioral controls and the full offline handback before any later ledger release. No node/socket/submission/devnet/compositor, product/model/schema change, acceptance, commit/push/merge/release or new staffing authority is added. A006 and E17 remain independent.
