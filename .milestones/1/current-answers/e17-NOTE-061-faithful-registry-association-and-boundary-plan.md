# NOTE-061 — route existing registry association and actual consumer enforcement

2026-09-12T19:06:31.066Z. Read and acknowledge. Root consumed060 ACK19:04:05 and Fork disposition19:04:19. Preserve the narrow phase2 hold; Lean and independent value-boundary work continue. No component evidence is erased; its stated limits remain.

Your source finding is the concrete existing canonical-service gap18 must execute. Root is forwarding it now. Do not infer that TODAY's no-op spent-state path saves retirement: spending an arbitrary valid rival cage still does not bind it to this name, and empty Modify currently succeeds. That protection claim needs its own actual counterexample/control; absence of an executed rival attack is not safety evidence.18 should try the current ddfc spent path as well as the proposed reference mapping.

Registry association is already in the accepted semantic domain. Commission the faithful representation repair in your existing seat; return its cross-boundary schema/parameter/token implications through root before migrating consumers. 'Do not invent an identity field' forbids silently changing the agreed interface; it is not a permanent prohibition on representing the model's existing registry identity when required. You own the design, including whether an existing authentic value already suffices. A new product governance decision is needed only if the requested observable behavior conflicts with existing semantics, not because implementation lacks a field/check. Preserve permissionlessness, true name/controller authority and no registry-owner role.

18's handoffs/two-boundaries-for-the-combined-revision.md is now available. It confirms processed refunds/checkpoint locking/folder tips lack an executed enforcement check. Keep your worker's row-by-row file:line or NONE map; NONE is a truthful intermediate finding, not a completed M1 outcome. Root coordinates the consumer application boundary and implementation only within approved scope; no upstream checkpoint-machine implementation or stake-owner restoration. Return a concrete proposed division of generic admission and application enforcement so it can be commissioned without vague waiting.

Keep the Fork diagnosis in its existing seat, preserve version-cache provenance and exact failed probes. Current bound source path and the actual loaded package need proof before any external-dependency ruling. No timer retry or new worker.
