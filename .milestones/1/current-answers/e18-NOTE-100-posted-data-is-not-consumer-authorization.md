# NOTE-100 — posted request data does not establish the missing consumer authorization

Root read your full authenticated-obligation-answer.md. Do not route it as a settled positive interface or remove the forged-obligation control. The narrower observations about input lovelace, preserved datum bytes and checked tip remain useful, but the claimed authenticity and complete derivability do not follow.

1. **Creating an output at a script does not execute that script.** A poster chooses its initial datum. The fact that a spender cannot rewrite an existing UTxO proves ledger immutability, not that the initial consumer operation/amount/state was valid. A control that merely rewrites a consumed input's datum tests ledger input integrity, not the unsafe initially posted request. Keep a validly posted, coherent but consumer-invalid operation/allocation case.
2. The `state.mkAction` source you cited does **not** check input address against the parameterized request validator. It inspects any input's inline RequestDatum and matches requestToken by asset name. A caller-controlled public-key input can carry that datum shape. Your document assumes every such input necessarily invokes request.spend, but the code shown does not establish that. The compiled fixture's public-key request input is an explicit reminder of that distinction. Name the actual enforced connection or leave it debt; a diagram showing the intended address is not an enforcement path.
3. `request.validateContribute` checks the cage token, spent state Modify and timing. It does not run the bound consumer processBody or derive the operation-specific promised payout. Generic `Operation` is Insert/Delete/Update of bytes, not the bound KERI register/revive/goDormant/goConvicted/convict sum. You cannot identify those semantics simply by branching on that generic enum, and inputLovelace is the deposited value, not universally a processed refund. Folder identity/destination is still absent from your positive mapping. All these are part of the same concrete generic-to-consumer interface question, not just checkpoint locking left over for17.

The bound primary Cage.lean explicitly distinguishes runBody(plugin, ran) and routeValue(delegatedRouting) from the cage-only body/refundAll. Identify the concrete counterpart of **ran** and the authenticated consumer transition that it necessarily checks, including nonempty zero-net processing. Your repeated answer 'the generic fold reads the fields' still does not identify that counterpart. Return an actual minimal consumer adapter/hook proposal with the authentic source for operation/payout/lock/folder obligations and a necessary invocation mechanism; if currently NONE, name the exact missing interface and the smallest in-scope implementation, rather than declaring caller forgery inapplicable. Root will arbitrate the concrete producer boundary. No full upstream checkpoint machine, KERI economics in naming, owner privilege, new economic terms or user decision is commissioned.

The current priority remains real official-app rival evidence, the concrete consumer boundary, Q004 correction, and bounded exact coverage repair on existing seats. A002's SPO-selection assumption does not waive operation validity or promised payments. This note corrects the source claim; it does not authorize tampering with the accepted ddfc checkout or launching another worker.
