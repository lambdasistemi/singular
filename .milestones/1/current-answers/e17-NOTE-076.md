# Root urgent semantic correction: remove the universal cage lock; integrate the delegated hook coherently

Read IN FULL and ACK NOTE-076; route at the next safe worker tool boundary BEFORE further fee-test/builder edits bless the draft predicate. Preserve990's current changes and evidence. No reset, new seat or Fork merge into dirty work.

Root read the CURRENT dirty onchain/validators/state.ak (observed SHA256 59a7f4d2b8ddfec8807f04ab4012c263cbf8e9c443fef4ec42b5bc9ac8f78ee6; exact source and diff retained in root handoffs/producer-cage-lock-observed-state.ak/.diff, source-finding JSON). mkAction now applies to EVERY UpdateAction:

    locked + inputLovelace - stateTip

and validModify requires:

    lovelace(stateOutput) == lovelace(stateInput) + lockedTotal

This is NOT the approved consumer routing. It forces every processed bond into the generic cage, including register/revive (which must lock D in the NEW CHECKPOINT) and go/convict (which must refund Mr to the requester). There is no operation discriminator here. It also leaves tips merely implicit. The previous raw refund-all envelope was wrong; a lock-everything-in-the-cage envelope is wrong for the same reason.

Concrete source-level counterexample, not a claimed ledger execution: prior cage3M, register request10M with tip2M and checkpoint bond8M. The correct consumer flow leaves cage3M and puts8M into the checkpoint, plus2M to folder and separately funded fees. Your current predicate demands cage11M and therefore refuses that legitimate flow unless the funder donates ANOTHER8M. This is an extra lock, not a permitted overpayment convenience. A processed go request similarly cannot refund its promised Mr without an extra donation.

Primary authority already read by the outgoing owner: root071 explicitly says the consumer owns allocations INCLUDING LOCKING and generic routing must permit them; handoffs/refund-slice-source-request.md rev3 says register/revive lock into the checkpoint, go/convict refund, and stepFn pays batch.length*tip to the folder. Naming and the generic cage do not absorb those economics. Retire ABI/registry association/reference quantity1 remain unchanged.

Disposition: DO NOT carry the unconditional processed cage-lock requirement into any accepted intermediate or final candidate, and DO NOT rewrite the three failing positive tests merely to encode it. Preserve the failed run/diff and correct the semantic draft. Rejected-request obligations retain their correct authenticated amounts; do not invent a fee share, script-owner role or tip guarantee from arithmetic alone.

Update the work sequence to target root071's actual final boundary directly: the sixth-field pinned withdrawal hook plus generic routing that permits consumer-owned allocations must be coherent with the validator/builders/tests BEFORE final connected journeys and identities are accepted. The earlier014/016 order is superseded where it makes an obsolete five-field fee-map-green commit a prerequisite to the hook. Keep the already-good Retire/reference/vector changes; no need to throw away unrelated implementation. Exact generic bytes/vectors/built identities still go through root before18 migrates. No consumer operation/economics hardcoded into naming;18 owns the actual consumer compositor.

Return a short source-bound corrected boundary and actual next step; then implement through990 without another stakeholder decision or approval request. No whole-suite rerun until a meaningful code change warrants it.
