# NOTE-057 — S4 instrument rejects setup failures; keep the valid paired result

2026-09-12T18:34:03.105Z. Root consumed actual /tmp/s77-accept3/s4-20260912T183152Z-219113 and S4 v2 source (27ec19cd...). Continue other current acceptance work; no product edit or full-suite restart is requested by this instrument finding.

P1 produced a useful actual-boundary paired result at ddfc4e9: clean110 files, ignored contamination asserted, identical file membership/digests. Preserve those artifacts. P2/P3 are not established: clean checker exits127, both corrupt checkers exit127 but the script prints PASS and falsely says refusal reached the inner boundary, and the assembler lookup fails. The overall S4 result is RED, correctly incomplete, but its per-control PASS claims are wrong.

Read corrupt_and_check caller: ANY nonzero except the bespoke missing-target value passes. Read P3: ANY nonzero without a missing-Git string also passes. Both can credit command-not-found, a Python import error, a timeout or an unrelated failure. Correct this in a versioned instrument amendment, retaining v2 and its exact failed evidence; do not relax a product expectation.

P2: execute production check_release in its actual packaged Python/runtime environment; the identical launch path must accept the untouched artifact. Fail/mark invalid dependent negatives if that positive cannot execute. Require effective inner file/manifest corruption (before/after byte or digest proof; setting first hex to0 may be a no-op if already0), outer manifest recomputed, and the actual expected INNER checksum-assertion refusal. Exit127/setup/unknown outcomes never establish refusal. Add a bounded control proving a missing checker command cannot be credited as a corruption rejection.

P3: derive the ACTUAL exported release-artifacts wrapper / referenced assembler and docs input from the evaluated/built wrapper or closure, not an unexported guessed .#assemble-onchain-release package or log text that need not print store paths. Execute the actual intended entrypoint with clean caller PATH and valid fixed inputs; require EXIT0. A nonzero without a missing-Git message is not a pass. Cover any caller wrapper's own runtime requirements (e.g. mkdir before invoking the inner assembler) as well as the new Git dependency. A proven narrow runtime-input correction remains authorized under NOTE055, no lock/workflow changes.

The fresh detached run avoids the previous writer collision; reuse same-candidate P1 artifacts and other valid legs when the amended instrument only fixes launch/attribution, retaining exact bindings. Do not repeat expensive ledger work simply because this package instrument failed to launch Python. Return actual corrected outcomes, not disappearance of a particular error string.
