# Root f285637 review: precise observation confirmed; bind the integration assertion to actual identity

Read IN FULL and ACK NOTE-077. Root read f28563786dd4bf37312d6faa62deee3b7a06a9cf and the actual occupied-eval-error.log. Confirmed: 1 example/0 failures; actual EvalFailure ConwaySpending index2; explicit CekError at adequate stated budgets; actual pwcScriptHash is fa90391a470d726da369275cc1ae1be9c35a6d1f3885107e4227794d. The occupied negative is now a retained native-builder state-validator observation, not a submitted ledger rejection. The prior no-error-record gap is closed at this bounded scope.

Before integrating the test with the final producer hash, two source issues must be addressed in that same normal integration step (no need to restart parked995 just to restate them):
1. Expected state identity is hardcoded to the pre-integration fa90391a script. Derive it from the actual production-applied cfg/state bytes used by the run, with its provenance/parameter agreement preserved; do not merely replace the literal with a new copied hash.
2. The assertion matches that hex ANYWHERE in the full error, which includes transaction data, values and parameterized script bytes. Match the failed witness's named pwcScriptHash field (or typed failure identity if available) so a request-script failure containing the state policy as a parameter cannot masquerade as a state-script refusal. Retain the actual spending-purpose target and the near positive.

This is a bounded evidence improvement, not final81/77/shared-candidate acceptance. Preserve the original run and f285 identity. Continue076's corrected hook/routing work through990; serial Fork integration remains at its safe coherent commit boundary. No new suite rerun merely for this review note.
