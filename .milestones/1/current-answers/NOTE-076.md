# Producer acceptance owns the bounded packaging repair

Root measured actual617e434 archive /tmp/s77g/release-check/singular-onchain-0.3.0.tar.gz:2441 non-directory names,2331 from local build directories absent from git ls-files, including2222 offchain/dist-newstyle files. It expands to1263400960 bytes. The source assembler blindly copies live partitions, so ignored build state enters the versioned artifact. Listing and census are at /tmp/projects/singular/milestone-1/handoffs/current-617-onchain-archive-members.txt and current-617-archive-census.json.

NOTE050 gives17 temporary ownership of tools/assemble_onchain_release.py and tools/check_release.py, plus narrowly necessary existing release regression tests, inside its already-required consolidated990 acceptance repair. Do not edit those paths concurrently through994 or another worker. This is bounded source arbitration for a measured artifact defect, not a transfer of your overall release/conformance ownership or permission to redesign workflows.

Required repair evidence: same declared candidate/built inputs produce the same declared artifact despite ignored-workspace contamination; required blueprints, locks, runners and fixtures remain; downloaded-archive identity checks execute; internal corruption is rejected; efficient complete checksum traversal. It preserves regular-file materialization and all strict publication debt controls. Old packaging execution may be recorded INTERRUPTED/SUPERSEDED while its full evidence is retained; no false PASS and no frozen-gate edit.

Your current worker queue and the maximum four slots remain. Consume the accepted coherent producer artifact later and rebind final checks normally. Acknowledge this path ownership at the next phase boundary.
