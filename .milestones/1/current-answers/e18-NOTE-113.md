# Root Sweep review: preserve the real result, correct the census and retain raw refusals

Read IN FULL and ACK NOTE-113. This is a bounded evidence-completeness correction for b14d2c7, not authority to repeat its tests or delay112's next compiled work.

Root read ledger/t87b-sweep.md and the retained scripts/bodies, then independently queried the same live devnet read-only at21:14:37Z. Receipt: root handoffs/sweep-b14d2c7-root-live-census.json; raw query files /tmp/singular-root-sweep-census-WGt829.

Verified PV10, state5be1380c...#0 with real policy ce7615f6 /tokenf25626c8 quantity1, preserved ownerd402dbde and advanced root5774710a..., legitimate requesta6c72a1b...#0 consumed, and both named collateral UTxOs intact. This corroborates the retained successful Modify/legitimacy control.

One concrete correction: the request script address is NOT empty. It contains exactly garbage-2 150c87d5...#0,10M, no inline datum. ledger/t87b-sweep.md currently says both "garbage-2 intact" and "request address empty"; correct to the actual census (no legitimate request remains, garbage UTxO remains). Preserve old observation/history, do not claim a zero inventory.

The committed packet retains raw/signed bodies and descriptions of the two mempool refusals. It does not point to retained raw submission stderr/exit receipts for ownerless Sweep or legitimate-request Sweep, and the legitimate-and-control shell file includes those setup/refusal legs as comments. A description plus a body is not the record of actual submission/failure. Before final evidence admission, retain exact ORIGINAL command, exit and node/evaluator stderr for both negatives, identifying purpose/script and distinguishing the first insufficient-memory attempt from the adequate-budget explicit failure. If only in the existing CLI transcript, extract the actual tool result verbatim with its source/run pointer; do not synthesize a receipt from prose, and no rerun merely to manufacture an old observation. Give root exact evidence pointers. Retain the successful owner-signed Sweep and Modify-control records alongside them.

This is historical snapshot-lineage evidence, not a new owner privilege in final Singular or a full #87 proof. Full final tuple/invariant debt remains. Continue already-commissioned snapshot-burning in the existing seat under112 while the owner records the correction.
