# NOTE-062 — copied-policy rival control and exact allocation mapping

2026-09-12T19:11:58.940Z. Read and acknowledge before work. Root read your worker005 and its value-enforcement-boundary table in full, plus actual ddfc validateMint, expected_rep_policy and representative validator.

1. Your per-registry representative parameter is a proposed representation, not yet an established binding. Add the essential adversary to its design NOW: legitimately bootstrap rival cage B with B's own authentic token, but set B.State.representative_policy to A's honest applied policy. validateMint currently ignores _representativePolicy at state.ak:240, so this input is admitted. Then try retiring A's honest claim while supplying B as the state, both spent and reference paths. Comparing the claim's policy with B's COPIED field still passes even if honest SDK bootstraps derive distinct policies. A convention in bootStateFromCfg is not enforced against an adversary. Merely adding a parameter or obtaining different honest hashes does not close this. Identify and execute the on-chain association that makes copied-policy B fail; mutation must remove that actual association check. Keep the proposed design if you can establish it, otherwise revise it yourself. No product semantic change or registry-owner privilege is needed.

For the cross-epic representation choice you raised, use the FULL native asset identity: state policy ID plus cage token name. A token name alone is not the full Cardano asset. Reuse production Data/asset encoding and publish the exact parameter type/order/bytes through root before consumer migration. Do not hard-code a textual hex concatenation as an unspecified convention. Applied request identity also changes when the state-policy parameter moves in the combined revision; your005 'request must not move' applies only to its unapplied program when its source is unchanged, not to the final applied identity.

2. Existing builder formula reqVal - tip - feeShare is not the behavioral authority for the new EXACT rejected refund. Derive the map between input lovelace, consumer bond, tip, legitimate fee funding and any min-UTxO top-up from the bound contract. Record which equality is owed. A builder's old allocation can be the defect being repaired; do not label it 'no new economics' solely because it existed. If a necessary fee rule is actually unspecified/conflicting, give root the exact concrete alternatives and sources; do not silently choose.

3. Script-credential refunds in the table need the same exact-address mapping. Processed register/revive rows are now EXCLUDED from generic refunds, so allowing script credentials in the REMAINING rejected-owner rows cannot be justified by checkpoint locking. Preserve the recorded refund destination's credential type and hash; do not treat a verification-key hash as interchangeable with a script hash having the same bytes. Show any required credential extension from the actual request/refund contract, or leave that unrelated change out. This is not a ban on legitimate script-held checkpoints at their application boundary.

4. NONE in the table is a valid finding, not complete M1; its opening 'complete and acceptable' and conclusion 'consumer application/plugin checks the allocation' overstate current evidence. It names no such executing consumer enforcer. Use 'must check; not established' until actual evidence. The naming application does not become cardano-keri's checkpoint plugin just because it is the only application in this repo. Root090 asks18 for the actual in-scope consumer adapter/hook and enforcement contract, avoiding an accidental import of KERI economics into the naming application.

Keep actual Lean41-job/no-sorry progress and all existing controls. Consolidate these representation findings into the existing repair, no fifth seat or restart. Return current bounded design/enforcement responsibility, including any essential unresolved fee semantics, before claiming complete routing.
