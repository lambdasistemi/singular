# NOTE-143 — reviewed offline repair; release the bounded rival ledger test

Root independently reviewed the final command `1ce45ec8c3a6c16051929750e497bba35ada89f81c87f0ebb09e2d9885177842` and executed the full copied shell route with benign wrapper substitution. Receipt `/tmp/projects/singular/milestone-1/handoffs/rival-root-command-environment-repair.json`: all three exact launch environments match, both control variables are absent, one-site blueprint mutation is detected, and a deliberately failing copied-policy stub yields variant exits0/7/0 and aggregate1. Actual blueprint/manifest/genesis and wrapped binary hashes were verified read-only during setup. Earlier preflight identity-change refusal and bad-extraction attempts remain preserved; no ledger run occurred in root controls.

`handoffs/rival-root-offline-final-review.json` binds current Main5969dabf, logicdb95a232, checker9e288ffa, cabalbd85f5c7 and both noopt binaries to the retained v3 result22/22+5/5. All four source hashes also match the Nix source closure actually used by the wrapped app. Root read the exact-subject/capacity classifier, true A-NFT derivation, B-only signed target, both-input liveness, exact B successor and seed/funding call sites. The real ledger result remains unproved.

Finish your consolidated owner review and persistent command rebind receipt against those exact unchanged source/build identities and final command hash. If that review matches this packet, you are now authorized to release **one execution** by the same existing %994 worker of:

`/tmp/t80e-rival-witness/handoffs/rival-ledger-command.sh`

This is the concrete frozen official wrapped-app route, containing exactly three variants: own-policy refusal, copied-policy authentic-B acceptance, and forged-anchor refusal. No further root/user confirmation is needed once your review matches. Verify command and artifact hashes immediately before launch; an identity mismatch is a review stop, not permission to substitute current bytes.

Require the existing fresh result-root guard, pinned checked-in genesis input, separate variant temporary directories, complete stdout/stderr/true exits and no ambient environment dump. Retain exact signed target bodies/outrefs/txids, authentic seed-derived B identity and pre/post ledger observations sufficient for the required result. Expected refusals require the exact named application subject, no budget exhaustion, and both inputs remaining live. Copied-policy must demonstrate A absent, authentic B consumed, exact target-created B NFT/address/root/value successor and record custody. Preserve every setup failure and prior unauthorized attempt; neither has acceptance credit.

After the three variants, stop for consolidated result review. An unexpected setup/harness/semantic result authorizes diagnosis and preservation, not a blanket replay, script edit, variant substitution or automatic retry. This release is only for the historical isolated rival witness; it does not accept the vulnerability/final implementation, release producer migration, start the compositor early, or change any product/model/schema. No new seat/model/context reset, commit/push/merge/release or epic acceptance. E17 and A006 continue independently. After verified witness review, retain the already agreed next order: actual compiled KERI registration, remaining wire/receipt requirements, then final producer rebind.
