# NOTE-069 — authorize the immutable key witness in the retirement redeemer

Root read990's full handoffs/key-material-proposal.md and the founding frozen-wire clauses in both epic briefs. The frozen shape is the four-field naming datum; the required producer revision may change a redeemer through coordinated migration. Proceed with the proposed creation-time control payment hash as the committed28-byte material, carried at retirement and checked against the actual representative asset name plus authentic supplied cage token. This is a representation of the already-required immutable key association, not a change to recovery or naming ownership.

The concrete ABI proposal is accepted for implementation: **ApplicationRedeemer.Retire stays Constr3 and carries [representatives, key_hash] in that order**, with key_hash exactly28 bytes. The name preimage is policy[28] || cageTokenName || creationKeyHash[28], with the already-approved raw-byte framing and no textual hex. Creation obtains that material from the original claim; retirement verifies the supplied material against the actual minted token commitment. Current recovered control/quorum authorize retirement independently. A wrong key, wrong length, wrong authentic registry and copied-policy B must refuse. Recovery preserves representative value/name and never needs the old signing secret.

Implement the SDK encoder and all affected runners/vectors with this ABI in the same coherent producer revision, and publish the exact byte contract plus measured identities through root before18 migrates. Retire encodings change for FRESH claims as well as recovered claims; only the creation/name preimage remains unchanged from the current draft. Do not claim wire identity for the changed redeemer. Preserve the four naming datum fields and all unaffected constructor indices.

The supplied creation key is public data, but a successful recovery-to-retirement journey must actually obtain it through the supported artifact/SDK path; a hidden test-only KeySetup constant is insufficient evidence for a joiner. No new indexer or service is commissioned. State the available public registration/provenance input and exercise the supported path without the old signing key. Existing spelling-to-model-key refinement debt is not waived by choosing the already-used creation material; do not equate control identity with arbitrary user spelling without its established mapping.

Proceed through the previously commissioned sequential claim/recover/controller-retire and claim/recover/quorum-retire positives, fresh retirement and rival/forged-key controls. This ruling removes the proposal's decision wait; continue the existing990 conversation rather than ending after acknowledging it.995's independent corrected Fork acceptance continues. No new budget/seat or stakeholder question.
