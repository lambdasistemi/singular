# NOTE-134 — recover the existing rival worker instead of another identical tail handoff

Root verified the actual %994 process is still Pi0.84.4, PID4182517/startTicks149071436, provider zai/model glm-5.3-flash/thinking max. It is terminal at the latest response with no child command. The UI reports84.9% of a1.0M context, automatic compaction enabled. This is not an observation timeout or a dead process. Multiple completed turns now stop before the same one-file check is compile-clean, repeatedly citing context tail. Your NOTE035 and the full frozen22+5 contract remain the next implementation task.

Change the recovery mechanism now. Use the existing Pi native `/compact [instructions]` in this SAME session/model/process, after preserving a precise current checkpoint and verifying no command is in flight. This is explicit permission for native context compaction, NOT a worker reset, `/new`, branch switch, process restart, new seat/model, fresh counter or loss of history. Do not launch a replacement.

Root checked the installed primary docs:

- `/nix/store/iwgllsbcsalpqms30i4xn56dcpg99zdl-pi-0.84.4/libexec/pi/README.md`, Compaction: manual command is supported; full history remains in the JSONL file.
- The same package `docs/compaction.md`: native compaction appends a CompactionEntry, retains recent messages, rebuilds only the active prompt context, and includes summarization usage in session totals where available.
- `context-compiler/SKILL.md`: where a fresh process is unavailable, checkpoint a compiled packet and use a verified native compaction boundary before work. Here fresh processes remain forbidden; use only compaction.

You own the checkpoint/transport. Before compacting, record the exact native session/history identity, existing PID/start time/model, current source hashes, retained failed attempts and current counters/usage observations. Preserve/export the append-only raw native history so compaction cannot masquerade as deletion. Compile a short authoritative resume packet for the single offline-check artifact: real current source interfaces, frozen IDs/JSON structure, exact baseline-versus-mutant oracle requirements, compile/run command, forbidden surfaces and true completion criterion. Do not carry the whole historical brief as the next active mandate.

Verify the native compaction entry and same process/session/history continuity before rearming the one-file work. Then require a durable worker ACK of the compact packet and continued implementation to the actual compile/run boundary, not another declaration that context is nearly full. Check actual UI/context evidence if it repeats. Preserve source, repository/index, worktrees, logs and counters; no native model/config change, no devnet/node/submission, no Main/cabal/command/v2 advance before the already required review. If the native compaction fails, retain the failure and report the concrete result; do not substitute reset/restart.

A006 and E17 continue independently. This recovery targets the repeated failure to finish the existing artifact; it changes no M1 acceptance requirement or model staffing.
