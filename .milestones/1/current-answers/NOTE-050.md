# Current archive includes untracked build state: consolidate its repair

New measured evidence changes the acceptance plan; this is not a timeout inferred as a dead process. Root verified the original check_release.py PID21812 is CPU-running, and then inspected the ACTUAL archive it is checking. Files:

- /tmp/projects/singular/milestone-1/handoffs/current-617-onchain-archive-members.txt (single completed tar listing; SHA256 5add9001b8f03e77eaa4ce98b0089ca13bf3c85d15bb7cd175e6639a7bf8e422)
- /tmp/projects/singular/milestone-1/handoffs/current-617-archive-census.json
- Archive /tmp/s77g/release-check/singular-onchain-0.3.0.tar.gz: gzip reports221040349 compressed bytes,1263400960 uncompressed bytes.

Measured listing:3378 entries,2441 non-directory names,2331 under local build paths. Those2331 are ALL absent from git ls-files at617e434:2222 under offchain/dist-newstyle,40 naming-onchain/build,69 onchain/build. Examples include Cardano/MPFS/Cage/AssetName.{o,hi,dyn_o,dyn_hi}. This is an actual release-content finding, not a guess from size. tools/assemble_onchain_release.py:44-45 blindly copytree's all three live partitions; stage_release then copies every entry. git-clean candidate binding does not bind ignored build content. Separately check_release.py hashes archive members by iterating a set; repeatedly seeking through a large gzip archive plausibly explains the high CPU time. That performance mechanism is a source-supported inference, not a measured full runtime comparison yet.

Root authorizes the existing990 consolidated repair batch to include the precise packaging boundary tools/assemble_onchain_release.py and tools/check_release.py, with any narrowly necessary existing release regression tests. This is a temporary cross-epic source-ownership arbitration for the current producer acceptance;18 is notified and must not concurrently edit those paths. No fifth worker, release workflow redesign, product lock refresh or Lean change. You remain sole serial integrator.

First verify the content contract and reproduce source-binding failure in an isolated bounded control: a local ignored build artifact must not change the versioned release's source membership or bytes under the same candidate and declared built inputs. Retain an honest clean assembly and a contaminated-workspace control. Repair assembly to derive the declared source inputs explicitly while still shipping required compiled blueprints, locks, runners, fixtures and deliberately declared artifacts. Do not blindly exclude required runtime artifacts or throw away the symlink/hardlink materialization checks. Check the downloaded artifact's identities and required entrypoints after repair. Make checksum traversal efficient without weakening its completeness: corrupt an internal file/manifest and require rejection. No arbitrary size threshold substitutes for source provenance.

Do not hold the consolidated repair forever on the old package check. You may terminate ONLY that acceptance run after recording it as INTERRUPTED/SUPERSEDED due to this demonstrated content finding, preserving all earlier results, complete archive, listing, logs and process receipt. It is not PASS and is not a complete gate. The frozen gate stays untouched. Run A1-v2 and the remaining/repaired acceptance at the correct subsequent candidate, with fresh binding; do not relabel previous output. This supersedes NOTE049's 'process untouched' only for the now-characterized packaging defect, not the other workers or devnets.

Keep the fourth-slot Fork launch moving. Return a terminal disposition for the old archive check, the single consolidated990 repair scope (E001 cause, final applied representative producer, package provenance) and post-cursor worker RESUMED. Update RESUME in place rather than keeping contradictory old running paragraphs.
