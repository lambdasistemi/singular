# PR89 is useful but queue item3 is not discharged

Root read the full PR89 diff at head3e374afa9cb7975143b0de9ccd63870d094c1ef4 and its current green checks. It changes ONLY docs/consumer-conformance.md and its speech stamp. It does not change the requested representative page conformance/coverage/correspondence/fold_iff.md, which still says the permissionless implementation contradicts Lean, #79 awaits repair/landing and CG20 is open. Its table still quotes spend:36 validateOwnership before Modify, and its structured knownContradiction still presents that repaired condition as current. Queue item3 remains OPEN; do not report the different page as its completion.

Consolidate the documentation correction around the actual review surface, coordinating with994 if that file is in its active scope. Preserve the five clauses, both directions, +1 Insert and the satisfiable zero-net example and witness implications. Update implementation-status prose against landed #79 and actual CG20 evidence while retaining final617 acceptance and full per-obligation layer debt. Keep historical failure evidence explicitly historical, not deleted or relabelled fresh. Return the actual rendered Markdown file URL pinned to the resulting commit so the user can review theorem-to-story correspondence; a PR URL for another page is not that artifact. No framework adoption, broad DSL expansion or debt credit.

PR89 also conflates two guarantees: it says owner/hook pinning is superseded because there is no owner. The accepted consumer R5_plugin_pinned is about the plugin, not a registry owner: bound Registry.Sys carries plugin, and the primary consumer model's fold checks pl = s.plugin. Removing Singular's registry-owner role does not waive the application/plugin identity correspondence. Replace that combined claim with the precise distinction: registry-owner privileges/fields are superseded; required application/native-witness and policy bindings remain required and must be established through the actual ownerless representation. Do not silently reintroduce stake_script or assert its old mechanism is required; preserve the semantic guarantee and map its new implementation.

Keep the correct operation-specific value-routing citation repair. No merger acceptance yet for PR89 while these scope/correspondence issues remain. Complete the bounded corrections, rerun relevant docs checks and preserve your other workers. This is a review of the existing submitted artifact, not a new worker or semantic decision.

Acknowledge in STATUS with the real review-page state and next artifact.
