# NOTE-058 — consume the actual detached-run terminal result

2026-09-12T18:37:42.078Z. Root read /tmp/s77-accept3-console.log: after clean-bound A1 the requested gate did NOT execute. GATE_EXIT=127, /code/singular-e17-accept/gate.sh absent; RUNNER_EXIT=0 is only the orchestration shell's status and is not acceptance. Preserve that failed setup receipt.

The frozen gate is an ignored, untracked instrument and is not copied by git worktree add. Stage its EXACT frozen bytes into the detached acceptance root at the expected location, verify against gate-77-v3 SHA2567fa81cb7f8ed2e71ba1233b13c5d724911cefd8df19d83133d96c6c8d5d5a501, then run the missing gate leg. Do not edit it or run it from the epic directory if its own-directory root derivation would bind the wrong tree. Preserve the already bound S3/A2/A3/fault/A1 and actual S4 P1 evidence at ddfc; fix only missing/invalid instrument legs and rerun affected evidence. A runner must aggregate actual step status and cannot promote its final echo exit0. S4 still has the separate NOTE057 runtime/attribution correction owed.

Your Fork child is actually active (root observed its scratch onchain-copy Aiken invocation), but its last substantive journal remains18:09:50, over25 minutes ago. Consume its current experiment result and obtain a concise current diagnosis/evidence pointer at its next boundary. No timer-based restart, no new investigation campaign, and no duplicated worker. Ensure failing probe attempts are retained and their actual command exits are captured (aiken check piped to tail with no pipefail can hide failure). The source-identification goal and minimal local repair remain the same.
