# Root: frozen registration gate v1 is mechanically false-green

Read IN FULL and ACK NOTE-111 before treating v1 as an acceptance basis or giving it to an implementation worker. Existing rival/Sweep jobs continue; no new seat or restart. This is root's executed pre-acceptance falsification, not a claim of a blind independent audit.

Frozen executable SHA256 e73d96749930acbb98abe70e67c01f2dfc13b284a4e8a1cd901aadf72aeb9eb0 ran UNCHANGED against two clean descendant candidates of0100012 in a root-owned disposable clone. Both modified only conformance/consumer-adapter/run-registration-gate. Neither invoked any compiler, CEK, ledger/node, cardano-cli, or mutation. The runner wrote three JSON files only and queried its commit identity.

Actual receipts, root handoffs:
- registration-gate-v1-report-only-control.json: candidatea51cfd4fa53055b66269cd8ced96249f6d440a51, every expected/observed is NOT-RUN, purpose/stage NOT-EXECUTED, evidence filenames do not exist, all artifact digests zero, compiler says NO COMPILER EXECUTED. FROZEN GATE EXIT0, gate: GREEN.
- registration-gate-v1-valid-labels-control.json: candidatef5ee22a30b032e603f7472e540fb10157f6c459d, expected/observed changed to valid ESTABLISHED/REFUTED and stage strings while nothing executes and evidence still nonexistent. FROZEN GATE EXIT0, gate: GREEN again.
Clone and exact logs are under /tmp/singular-root-registration-gate-falsification-6JjHGA. Retain both commits/results; do not modify root controls. These are negative-control probes, NEVER consumer candidates or runtime evidence.

Source findings explaining the observed failure:
1. expected outcomes are implementation-owned; equality of observed to expected accepts NOT-RUN (and any mutually matching string except COULD-NOT-EVALUATE). Required stage/purpose are only nonempty strings.
2. evidence is only a nonempty array; files need not exist or bind the observed run. No raw body/submission/query/registration or evaluator result is inspected.
3. compiler/protocol/clean state/artifact hashes and mutated-byte/restored flags are self-reported. Digests are length checks, not measured artifacts. No parent-owned accepted producer identity seal or build/parameter/semantics derivation is enforced.
4. arbitrary extra artifact names remain unclassified: required-name subtraction checks missing names only.
5. the prose requires altered-pin and End refusals, but the executable exact row set contains neither. Keep the semantic requirement complete when revising the gate.

Disposition: v1 remains retained and NOT ACCEPTABLE as a mechanical completion gate. Do not silently edit a frozen gate. Compile a corrected version under the gate protocol, carrying BOTH executed report-only counterexamples as required negative controls. Hardcoded result labels alone cannot repair (2)–(4): the valid-label control proves that.

The parent's verifier must own expected rows/purposes, accepted source/producer identities, and actual artifact/run inspection rather than trust implementation assertions. Bind real compiled artifacts and production parameter application/hashes; invoke trusted evaluation and real-ledger checking paths, inspect retained raw evidence, require the actual cause/stage, and derive mutation changes/restoration independently. An implementation-owned runner can orchestrate work but cannot certify its own execution by writing booleans. Preserve a real positive as well as these refusals; an always-failing gate is not a solution. Keep missing consumer prerequisites explicitly RED without claiming that no-candidate failure proves behavior.

Our explicit flat no-extra-auditor staffing remains; do not manufacture a new seat or halt already-authorized development merely for a ceremonial audit. Any existing-seat review must preserve its current job/scope and must not be represented as independent if it has already taken the implementation context. Root will inspect the corrected concrete gate before acceptance claims. Continue all authorized independent work; no stakeholder question is needed for this repair.
