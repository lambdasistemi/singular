# NOTE-146 — a concrete premise mismatch in the in-flight A006 relation

Keep the existing worker and its in-flight proof work. Root statically reviewed current `Corr.lean`/`RejectedFold.lean`; frozen identities are in `/tmp/projects/singular/milestone-1/handoffs/a006-root-custody-hypothesis-review.json`. This is a source/premise finding, not an executed counterexample or completed theorem review.

`candFoldInner` requires **every custody requestId to occur in funding**, before item processing, and rejects `custody-unassociated` otherwise. Current `BatchHyps` has uniqueness, selected-item used membership and custody-unspent premises, but omits this association. `FullCorr` adds only the selected ordered batch, output exhaustion and `sumInputs` over removed IDs. Those clauses do not constrain an unrelated, unselected orphan custody entry.

Before claiming `dirB_top`, check the concrete shape: an otherwise valid selected rejected request plus an extra unspent custody binding whose requestId has no funding record. The current relation appears to admit it while the runtime entry guard rejects it. Have the same worker validate the shape and complete the independent relation's justified hypotheses and general directions. Preserve the actual association guard; do not weaken/delete it or add an axiom/sorry/caller certificate to finish the proof. No need to restart the current proof or repeat unchanged focused timing/funding suites.

This is within NOTE135/035's already-required full correspondence. General two-way full-state correspondence, frame/used/funding claims, exact admitted mixed/zero-net/custody history and separate completion accounting remain open until demonstrated. Finite admission-kind examples remain bounded evidence and cannot substitute for the required exact reachable history or actual-program correspondence. No accepted-model/production/shared-schema adoption, new seat/reset, migration, release or epic acceptance is authorized. Rival NOTE048 and E17 continue independently.
