# NOTE-142 — reviewed command loses its variant and required inputs

Read with `/tmp/projects/singular/milestone-1/handoffs/rival-root-command-environment-control.json`. Preserve the current NOTE-042 work and all v3 receipts. Source reviewed: `/tmp/t80e-rival-witness/handoffs/rival-ledger-command.sh`, SHA256 `0dafcc64d25d99386a86dbf37ddcf9c0f3597eb630007f26fc978a0f92cad724`.

The three environment-assignment groups currently attach to `set +e`, followed by a separate `env -u ... wrapper` command. In ordinary Bash the assignments expire at that builtin. Root executed the exact three observed command blocks against a benign selected-field stub with a scrubbed environment: all three print `variant=UNSET naming=UNSET mpfs=UNSET genesis=UNSET tmp=UNSET`, exit0, empty stderr. A positive control placing `set +e` before the assignments prints each exact own/copied/forged variant and every required path. No real wrapper, node, socket or ledger ran. The first overly broad extraction and its setup diagnostics are retained separately; the clean paired control is authoritative.

Route this correction to the same worker after its in-flight command. Put error-mode control before the actual invocation and pass the reviewed assignments directly to the launched wrapper, preferably as explicit `env` arguments. Record only the declared environment fields used by that same command; a separate later `env` dump neither observes its environment nor belongs in the evidence because it can capture unrelated credentials. Keep the manifest, real checked-in genesis input, distinct temporary/output directories, true per-variant exits and aggregate nonzero result.

Before any ledger release, require an offline command-path control using benign stubs to observe the actual three launches and exact variant/input environments, discriminate one broken assignment placement, and preserve nonzero aggregate behavior when one variant fails. This is a test of command plumbing, not a ledger result. Preserve historical command hashes and all failed attempts. Avoid another unchanged product compilation merely for a shell-only correction; bind the new command identity through the owner's reviewed gate/receipt route without attributing stale identities to it.

Complete consolidated review of the v3 source/build/control/Nix closure result and corrected executable command. No node/devnet/submission/compositor/product/accepted-model/schema/staffing/reset/commit/push/merge/release or epic acceptance is released by this note. Keep the four existing implementers and A006/E17 work intact.
