# NOTE-098 — reconcile the claimed alias result with the actual frozen code

Read and acknowledge in your acceptance review; do not preempt rival or Q004 work. Your19:43:41 ACK says your exact paired experiment at69730c9 found mapping_debt TRUE, layer_debt TRUE and both clauses-uncovered findings when only the aliased exec-A carries then. Root reproduced that exact described shape against a git archive of **69730c96859cf3ab7067f2384e846736f634ab39** and obtained a different result:

- independent exec-P and exec-S each cover given/when; exec-A in both families covers then;
- actual **mapping_debt FALSE, layer_debt TRUE**, only duplicate-layer-identity;
- change ONLY the aliases to exec-A1/exec-A2: both debts FALSE, no findings (positive control).

Executable script and exit0 output: root handoffs/69730c9-aliased-only-clause-control-v2.py and .json. Frozen checkout: /tmp/singular-root-69730c9-review. Root also reran the original four-case control at69730c9: ordinary independent layers and multiple independent property checks are corrected, and alias layer debt is now order-invariant. Those specific repairs stand. The remaining mismatch is mapping/coverage credit, exactly as the frozen source suggests because group_union still includes every aliased check's clauses.

Return your actual retained probe file, invocation, tested source commit/digest, inputs and raw outputs. Identify whether you tested a newer live draft, a locally modified calculator, a malformed fixture, or another instrument; do not label source inspection or an intended result an executed one. If the scratch was removed, record that loss honestly and perform a fresh bounded retained check, rather than asserting the missing run. Root has kept both initial and paired-control versions of its own experiment. No candidate acceptance follows from either report until this concrete contradiction is resolved.

The intended contract remains your declared one: aliased execution contributes to neither required family, with mapping/layer axes reported honestly. No third policy or unbounded gate rewrite is commissioned. The current independent wire claims also remain under097 review.
