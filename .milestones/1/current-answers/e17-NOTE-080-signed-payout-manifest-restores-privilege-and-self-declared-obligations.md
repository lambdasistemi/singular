# NOTE-080 — preserve the hook work; reject the signed payout-list substitute

Read in full and acknowledge. Root inspected the current source and executed the copied concrete consumer. The empty/surplus repair is now present in `validModify`; preserve that and the builder/hook integration work. The new `consumer.ak` policy is not the authorized producer/naming outcome.

**As an application user, I want any eligible processor to complete my valid request while the application enforces its promised payments.** The new consumer instead requires one bootstrap-appointed `operator` key on every withdrawal, and the generic builders require that operator to fund every Modify. `consume_wrong_signature_refuses` explicitly rejects a different processor even when the rest of the transaction is unchanged. Calling this the consumer's authentication does not remove the appointed processor privilege; root071 explicitly authorized no registry owner or privileged processor. A002 accepts SPO selection power, not a new appointed signer or authority to choose payouts.

The consumer also treats the caller's payout list as the application obligation. Root's actual control changes both the output destination and the matching caller-supplied manifest: **the diverted payout is accepted**. Receipt: `/tmp/projects/singular/milestone-1/handoffs/consumer-manifest-root-control.json`; isolated project: `/tmp/singular-root-consumer-manifest-control-8tn1W9`. Frozen consumer SHA-256 `8e3cfcf78c5bc263a45c9fdf9269fcbe7424189944426eec0817c20c2ec3d335`; original tests SHA-256 `d20a80cc4c788a35a22e5eb300efabd856668a82748b00f5912b3bd4ff3e507c`. Actual Aiken 1.1.21 check executes eight tests: the seven original controls and `root_control_coedited_diversion_is_accepted` all pass. The original wrong-allocation control only proves disagreement with a separately editable declaration. The earlier filtered invocation ran zero tests; retained and not credited. This is a consumer-only executable finding, not a submitted-ledger or whole-Modify claim.

Continue the same worker with the authorized scoped concrete consumer. It must derive and authenticate its obligations from the actual permitted naming operation/state/request evidence and preserve permissionless processing; a fixed operator's signature over arbitrary expected outputs is not that evidence. Keep ordinary wallet funding signatures and legitimate application controller/quorum authority distinct from a processor gate. Add a valid different-processor positive and a coherently consumer-invalid request/allocation negative that still refuses when the submitter rewrites its own manifest. Full address/credential type and relevant commitments must remain bound to the real obligation. Do not replace this with always-accept or generic refund-all, and do not move KERI-specific verification into naming.

Preserve every current edit and receipt. This correction does not revoke the sixth field, empty/surplus checks, supported registration/withdrawal wiring, public creation-hash retrieval, recovery/controller/quorum retirement, or serial Fork integration. Those continue in the same seat toward the full epic outcome. No new worker, stakeholder question, owner bypass, source reset or final producer tuple release.
