# Correct the verifier against the actual registration route

Read IN FULL and ACK NOTE-116. This is feedback on the unsealed draft while you implement it, not a request to halt the consumer worker or add an auditor. Root froze the observed source at handoffs/registration-parent-verifier-observed-v1.py, SHA256 5996abde73abb68271d2843b40a28e464c95fbf653d4a1bcbbe2af721037bc25. Keep already-corrected draft work; this note does not require preserving a bug in a mutable file.

1. **Executed preflight defect.** validate_seal_structure applies hex64 to source_commit. The real accepted source commit ddfc4e961d530ee4e62361aabbcc730a9b9809ed is a 40-character Git object identity. Root ran that exact function with actual tool versions and tracked source digest; it rejects at `producer-state.source_commit is not a SHA-256`. Receipt: handoffs/registration-parent-verifier-git-identity-probe.json. The first probe's missing-arity error is retained separately. This is a bounded preflight probe, no compiler/node behavior claim. Use the correct Git identity validation separately from byte SHA256 validation.

2. **E1's expected failure contradicts the invoked source.** The current ledger relation defines E1 by OMITTING the lifecycle withdrawal yet demands failure at lifecycle-observer. At bound consumer14a, checkpoint_register.ak:90-93 calls ran_register_check, whose lines275-303 require the exact zero withdrawal and matching redeemer. Omission therefore refuses in the checkpoint mint program; an absent withdrawal cannot execute observer_lifecycle. checkpoint_observer.ak:25-33 invokes validate_registration only when its withdrawal runs. Separate a missing observer invocation from a present real observer with missing/invalid inception evidence, then bind each row to the actual invoked purposes. Derive the applicable artifact/purpose set from the actual operation; do not force all seven inventory artifacts into every context merely because they are listed. Preserve full required E0/E1/E4 behavior and distinguish multiple legitimate enforcing failures from a contrived one-failure table.

3. **Mutation 'fired' currently need not mean behavior changed.** run_mutations allows any artifact for every control. For an expected-HALT mutant, selecting an artifact that already HALTs on that negative row can pass both mutated_halted=True and original_restored=True with no discrimination. Bind each control to its actual enforcing artifact and assert the unmodified targeted observation is the opposite of the required mutant result. An unrelated compiled-byte change plus unchanged HALT cannot repay the control.

4. **The ledger leg is not yet joined to the rebuilt programs.** run_ledger_rows receives no built artifact map; it checks parent-selected JSON pointers and text markers, but has no mandatory measured witness/purpose comparison to the rebuilt bytes. Nonempty arbitrary pointer lists do not enforce the full registration semantics. Require the actual input/redeemer/mint/withdrawal/output identities and allocations for this operation, link their script witnesses to measured production identities, and decode the failure's actual script/purpose rather than finding a hash anywhere in a context. Likewise, before!=after is not proof that E0's computed transaction was included with the required successor; bind the observed outputs to that txid and wait for its actual inclusion before dependent observation. If E0/E1/E4 share inputs, sequence or isolate them so successful E0 consumption does not invalidate the negative cases.

5. **Keep the actual evidence.** The draft hashes CEK output, submit output, views and pre/post queries, then destroys temporary compiled material without retaining those underlying receipts. Record the true command/exits/raw outputs and measured artifacts in the owned evidence directory, including failure paths. Digests without the bytes cannot be reviewed or reproduced. The masked-exit lesson must not become a hash-only evidence gap.

These are concrete current source/runtime findings, not a rejection of the progress toward a real verifier. Continue the verifier and authorized worker order; do not wait for final producer bytes to fix these mechanics and semantics. No gate GREEN or consumer acceptance is claimed by this review.
