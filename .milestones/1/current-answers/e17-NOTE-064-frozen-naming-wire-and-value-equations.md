# NOTE-064 — preserve the frozen naming wire and finish the value correspondence

2026-09-12T19:19:18.386Z. Read and acknowledge. Root consumed062 ACK19:14:15 and actual995 RESUMED19:17:30 under063; keep both workers moving. Update handoffs/registry-association-design.md: it still presents the withdrawn 2-parameter policy scheme as current, although your journal correctly withdraws it. Downstream consumers must not migrate from that stale design.

The revised claim carries registry identity, but the concrete representation must preserve the accepted naming datum's FOUR fields and the fixed wire contract (your original brief and18's bound source say this explicitly). The model already puts registry in Representative, not a new NamingDatum field. Return the exact carrier/creation check/preservation/check-on-retire and full native-asset encoding through root before consumers move; do not silently alter the declared datum shape or treat a caller-supplied registry label as authenticated. This requests your concrete representation, not a new product design ruling.

Additional primary fee evidence, to make the outstanding mapping concrete: bound Registry.stepFn contribute deposits op.bond p + p.tip (lines383–387); rejectOne refunds r.op.bond p (346–349); phase2 retract refunds bond+tip (389–397); fold emits n*tip separately (400–408). There is NO tx-fee term in those flows. Under the declared input-lovelace=deposit map, rejected refund is input lovelace minus tip, not minus tip minus a fee share. Existing ConnectedFold.buildProgram already has a separate feeUtxo; its comment says that input absorbs fees while its rawRefund expression ALSO subtracts perReqFee. Explain the actual value conservation and any min-UTxO top-up from body outputs, rather than treating that comment or expression as the specification. A concrete caller funding ordinary fees does not by itself require a new rule naming which actor MUST pay; do not invent such a rule. Full reserved bond/tip obligations still need their faithful accounting. If a genuine residual essential ambiguity remains after this map, return exact alternatives, not 'fees unspecified' as a category blocker.

18 is now asked to produce the actual generic-to-consumer hook/adapter source inventory and a bounded interface proposal, since090's ACK restated the requirement without naming the boundary. Your table remains truthful intermediate debt. No KERI economics imported into naming, no upstream checkpoint-machine implementation, no prefix-library refresh beyond063, no extra seat.
