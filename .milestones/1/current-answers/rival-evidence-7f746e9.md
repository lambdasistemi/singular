# Slice-1 evidence ledger (three-way outcomes only)

Commit `5bd7c79dd64fd9c7ec872f1c20ab2f5e075366ad`. Snapshot — not definitive.

## Mechanical path

| Check | Outcome | Evidence |
|---|---|---|
| Title inventory v1 (14 titles, both partitions, from actual builds) | GREEN (mechanical) | `inventory.json`; build logs at run time via `run.sh` |
| Extraction by explicit title (no fallback, drift-checked, untracked-titles RED) | GREEN (mechanical) | `extract.py` + `generated/manifest.json` at run time; both falsification controls re-proven RED after the no-trailing-newline decoder fix |
| Identity triple | **COMPLETE for snapshot** | commit ✓ · toolchain ✓ (build-reported `v1.1.21+unknown`) · variant ✓ (`defaultFunSemanticsVariantC`, explicit, A-001, PV10-bound) |
| Lean import path (14 `#import_uplc` decode to real `Program`s + `targetSemVar` elaborates) | GREEN (mechanical) | Re-verified on the rebound tuple (incremental `lake build` exit 0); CardanoLedgerApiBlaster fetch+build confirmed at its pin. |
| Pinned hash correctness (`blake2b_256` vector + malformed refusal) | TESTED + GREEN GUARDS (kept) | `HashVectors.lean`: CEK-computed `blake2b_256("abc")` equals the mechanically generated reference (`abcOk` guard green); evaluator does not produce the retained mistranscription (`wrongRefuted` green, F-002); integer-arg call refuses (`refused` green). Variant explicit C. Assumption: `blake2b_256` is opaque-with-executable-body — finite CEK executes the body (this check); SMT would see it uninterpreted (no `#blaster`-over-hashes claim made). INDEPENDENT REPRODUCTION (NOTE-006, narrow): owner ran frozen `6a730f1` file (source hash `03f0d244…`, byte-identical verified) against pinned oleans (hashes recorded) → exit 0 empty output; wrong-expectation variant exit 1 with `false` (independent red-proof). Retained at `/tmp/singular-hashcheck-wiTcKO/`. Scope: hash assertions only — not runner/Update/identity/reachability/invariant. |

## Compiled-behavior assertions

| Obligation | Outcome | Disposition | Note |
|---|---|---|---|
| Observed-ledger CEK mirror (`ObservedF3Modify.lean`) | HALT green (kept) | TESTED | Same datum/redeemer/token/policy as tx `5fdbd5e7…` with observed amounts (30M in, 27999500 refund, 2M fee), observed range window, observed txids, empty signatories — including the refund path the synthetic fixture skipped. Ledger verdict == CEK verdict on the witnessed domain. |
| f3 I-1: owner-authority absence (bounded, provisional f3a68b1) | `ESTABLISHED (bounded — see note)` | TESTED | Executed: ownerless mint/boot + ownerless fold ACCEPTED on ledger; End-shaped spend refused (CEK + ledger, explicit logic); mint-burn refused (CEK + ledger, source-confirmed by-design); no `validateOwnership`/owner field in types/programs (f3 source read); signer-triples identical on all f3 paths. NOT covered (stays debt): Migrating-arm execution, Sweep path, request/staking/naming purposes, full quantifier, release params, accepted-candidate rebind. Discrimination demonstrated at both layers (NOTE-012/013/014): CEK triples (`e59bd20`); ledger f3-mutant legs — ownerless REFUSED by explicit logic, signer-authorized ACCEPTED (`bbb92b3d…`, live state). Snapshot leg specified outstanding (applied-mint input, producer named). |
| Snapshot I-1: owner authority terminates (ledger) | `REFUTED` (ledger-grade) | TESTED | Owner-signed `End` ACCEPTED on ledger twice (registry #1 `c35bab9f`, registry #2 `9d3dd211` — state addresses empty after, tokens burned); ownerless `End` REFUSED by explicit logic on registry #2 (same UTxO both legs — airtight discrimination). The brief's expected RED, at full reachability. Q-003 answered by A-003 (no second artifact: mint IS spend, 1 distinct program verified on both blueprints). |
| I-1 ownerless, migration/Sweep/burning paths | `COULD-NOT-EVALUATE` (narrowed t87b) | UNPROVED | Migrating-allowlist + Sweep-CEK now TESTED (own rows below); REMAINS: Migrating-ownership leg (FR3, needs `previousPolicies=[oldPolicy]` applied params + predecessor setup), Sweep LEDGER leg (tx construction on a live session), burning paths. Stays RED until run. |
| Snapshot Migrating-allowlist refusal + Minting control (t87b) | TESTED (finite CEK, kept) | `MigrationWitness.lean` green `(HALT, ERROR, ERROR)`: `Minting(selfRef)` HALTs on the recorded `assetName` fixtures (scaffolding + `root(empty)`=zeros proven through the real one-program bytes — mint IS spend, no new identity); `Migrating` ERRORs at the FR1 allowlist with fixture-bound `previousPolicies=[]` (fails for every oldPolicy); malformed mint redeemer ERRORs (out-of-domain). Ownership leg (FR3) NOT reached — named debt. First-failure untraced (no trace API); attribution rests on the pair, stated in-file. |
| ddfc rival-cage witness, official app (t87b, NOTE-007/008/009) | `ESTABLISHED (bounded — see note)` | TESTED | `DdfcRival.lean` green guards at `ddfc4e9` (B-cage synthetic, official app shared). Fold diagnostics (11 legs, retained supporting): ordinary/forged B-own-address invisible or refused; slot-matched B both orders with honest controls. RETIRE legs (commissioned NOTE-009, valid rival = same script+address, B's seed-derived token): RR0 honest-A HALT; RR1 ordinary-B ERROR (expected=B ≠ A-rep); RR2a forged-count ERROR (no match); RR2b forged-name ERROR (resolves — helper never compares names — then quantity); RR3 copied-policy HALT (F-003). BOUNDS: CEK scope only (constructed contexts, not ledger-validity); singleton redeemer maps; synthetic inputs; finite execution; assumed bootstrap/spend/ledger validity stated in-file. |
| Snapshot Sweep legs on production-applied request bytes (t87b) | TESTED (finite CEK, kept) | `SweepWitness.lean` green `(HALT, ERROR, ERROR, ERROR)` + structural `true`: bytes produced by the PRODUCTION path (`applyRequestParams`, both reproduce gates green, `cardano-cli` agreement `eb247285…`, `PROVENANCE-request.md`; `check_fixtures.py` pins file digest + `REQ_SCRIPT` per run). Owner-signed garbage sweep HALTs; ownerless ERRORs (no ownerless seizure — the I-1 Sweep expectation); owner-signed sweep of a processable request ERRORs (protection, datum-only discrimination vs the HALT leg); malformed redeemer ERRORs (out-of-domain). Residuals stated in-file: self-address binding untested at CEK scope; first-failure untraced. |
| ddfc COMPONENT span (t87b) | `ESTABLISHED (bounded — see note)` | TESTED | `DdfcSpan.lean` green guards at `ddfc4e9`: rep structEq `true`; composed `(HALT,HALT,HALT)` — state `Modify` (five-field, policy preserved, root advanced), rep `Mint` (+1 named, redeemer-map bound), application insert-`Fold` (approval burn, same-record continuation, rep under expected policy); isolation HALTs (withdraw/mint-approval/retire); budget probe (identical HALT at 500k/1M/2M); map-order guards (both levels, all fixtures). Discrimination: foreign-mint `Fold` ERRORs at the mint-quantity check while the identical-shape honest leg HALTs — the E-001 `representative-policy` refusal witnessed (Q-004 resolved by A-004 ordering repair); preservation + malformed ERRORs (sound); T1/T2/T3 predicted ERRORs. BOUNDS: single composed shape, synthetic scope (fake pubkey request input, singleton redeemer map — full ledger/request-validator correspondence NOT claimed); finite execution, never quantified proof; MPF-root refinement stays R1-analog debt. |
| I-1 instrumentation: compiled-`End` CEK pair (credential-BOUND) | TESTED (finite execution, kept) | `EndWitness.lean` green on both tuples (old-pin `/tmp/blaster-build8.log`, rebound `/tmp/blaster-rebind-build2.log` + incrementals): owner-signed `End` HALTs; otherwise-identical other-signed `End` ERRORs; bisection (`/tmp/blaster-bisect-raw.log`) tracks owner-absence. Credential now the bound applied-`[]` hash; spent input sits at the exact script address of the program under test. NOT yet a reachable-path refutation (`TxInfo`/reachability/live-protocol open). |
| I-1 credential binding (A-002, historical snapshot) | BOUND (kept) | Applied program vendored (`fixtures/` + provenance, 11 digest checks); structural equivalence to intended `Apply(body, List[])` green; behavioral pair reproduced on the pre-applied import green. Corroborated (not inherited) by the CG20 receipt route. Final `f3` is parameterless — this identity never substitutes for it; `List []` never applied to `f3`. |
| Oracle discrimination C-03 (owner rerun, narrow, separate) | GREEN (kept, not mine) | Owner reran the ORIGINAL mistranscribed assertion vs the new tuple: exit 1 at `abcOk`; corrected frozen `6a730f1` exits 0 (`/tmp/singular-hashcheck-wiTcKO`, sources+outputs preserved). Proves the check tells right from wrong. Separate from C-01 (runner) and C-02 (product mutant). |
| I-1 End-path unbound debt (each blocks REFUTED) | — | BOUND since A-002: credential = applied-`[]` hash `ce7615f6…` (exact ledger `computeScriptHash` sequence, independently agreed, 11 digest checks PASS, `check_fixtures.py` per run). STILL OPEN: `TxInfo` synthetic with placeholder fields; `applied_parameters` UNBOUND for release (fixture-bound `[]` only); no live-session protocol query (PV10 bound is the base genesis file only). |
| I-2 fold, Rejected scope — permissionlessness existence | TESTED (finite CEK, kept) | Ownerless `Modify([Rejected])` (empty signatories) HALTs on the base artifact; mutant refuses it ownerless and accepts it owner-signed (sufficiency discrimination, Rejected scope). Component acceptance only: no abstract consume-without-logical op exists (R2 evolution gap — compiled consumes + refunds, abstract retains). |
| I-2 fold, Rejected scope — sufficiency discrimination (mutant C-02) | TESTED control FIRES (kept) | Throwaway `MutantControl.lean` (preserved at `/tmp/MutantControl.ran-lean`, removed from repo; mutant bytes `da844ef7…`, built aiken `v1.1.21+unknown` from base + reintroduced owner check, `/tmp/blaster-mutant/`): same ownerless fixture ERRORs (positive claim REFUTED on mutant, as required), same fixture owner-signed HALTs (mutant functional, owner-gated — not broken-everything). A necessity-only harness would pass the mutant. Log: `/tmp/blaster-mutant-run2.log` exit 0. |
| I-2 fold, `Update` scope — honest fold + mutant discrimination (new tuple) | TESTED (finite CEK, kept) | Ownerless `Update` (insert `0x6162`→`0x6364`, empty proof, empty signatories) HALTs on the base artifact (state-validator component acceptance with unminted logical — grades to an abstract sameNet-FAILURE input per R1, verified `sameNet [+1] [] = false`; full sufficiency needs the multi-script span); malformed ERRORs; owner-check mutant (`da844ef7…`) refuses the honest fixture while baseline accepts it (positive claim REFUTED on mutant, as required). Mutant re-verified against the rebound repo (`/tmp/blaster-mutant-repo.log` exit 0; throwaway out of repo). Old-pin `ERROR` record (dispatch-refusal era) preserved in git history with its elimination argument; first-failure untraced there, now obsolete for acceptance. |
| Live protocol query (read-only, sibling t80 devnet session) | QUERIED PV10 (kept) | `cardano-cli query protocol-parameters` against the running Conway-era devnet (epoch 300, synced 100%) returned `major:10, minor:0`; session genesis file agrees. Read-only; disturbs nothing. The s77 fault-seeded session was already gone (socket absent) — precisely why per-run queries are required, never assumed. Per-run query obligation stands for every decisive run. |
| Live protocol queries (t87b own + t87 sibling sessions, 2026-09-12) | QUERIED PV10 | `cardano-cli 11.2.1.0 query protocol-parameters` (read-only): own `/tmp/t87b-devnet` Conway epoch 213 slot 106984 synced 100% → `major:10, minor:0`; sibling `/tmp/t87-devnet` Conway epoch 398 slot 199005 synced 100% → `major:10, minor:0`. Binds variant-C (PV10+V3) to live sessions for the current decisive context. Sessions are ephemeral (the s77 socket already vanished once) — the per-run query obligation stands for every future decisive run. |
| f3 owner-indifference TRIPLES + snapshot discrimination (NOTE-010) | GREEN (kept; corrects an overclaim) | Prior pairs compared absent-vs-wrong and could not see an owner gate (would pass pre-repair; claim withdrawn, hole owned). Now triples `[]`/`[OTHER]`/`[OWNER]` on all f3 paths: former key no more privileged than a stranger. Discrimination: the same triple on the snapshot End path gives `(ERROR,ERROR,HALT)` — the `[OWNER]` leg differs, so the shape genuinely tests the repair. |
| f3 preparatory: import + mint→spend chain + End-refusal + honest fold | TESTED GREEN (kept, provisional antecedent — never final) | `F3Witness.lean` exit 0: 4-field datum programs import+decode; mint HALTs ±signers (no signature dependence); old `End` refused ±signers (no termination path either way); honest `Update` HALTs identically ownerless and signed (no owner-gate regression). Credential `e51b35f3…` independently verified (`blake2b-224(tag+cbor)`); state parameterless (applied==unapplied). f3a68b1 UNACCEPTED — every final claim rebinds to the accepted candidate. |
| Cross-lane script-hash binding C-04 (NOTE-008) | GREEN (kept) | `check_f3_hashes.py` (run by `run.sh`): per-title V3-tagged hashes generated from blueprint bytes, all 14 equal epic 17's handoff table — including parameterized titles (table denotes unapplied-code hashes; deployment identities for applied params stay open). Recorded hashes bind which script the bytes are; mismatch is a named failure, never a note. |
| Ledger-level owner-gate discrimination (f3 mutant, NOTE-010/013) | BOTH LEGS GREEN (kept) | Mutant adds `expect has(extra_signatories, <operator>)` to `Modify` (diff preserved as `ledger/mutant.patch`; bytes built via production aiken from f3a68b1 sources; hash `c8b5d452…`). Leg 1: baseline-accepted ownerless shape REFUSED on mutant (explicit logic). Leg 2: same + required-signer operator ACCEPTED (`bbb92b3d…`, new state live) — refusal is because of the signature requirement, not general damage. The ledger machinery catches exactly the gate class I-1 claims absent. |
| Harness fail-closed control C-01 (file level) | GREEN (mechanical, kept) | Altered-output control on the `End` pair: flipped refused run to owner-signed → guard failed exit 1 with exact mismatch (`/tmp/blaster-ctl-C01.log`); baseline restored green. Pins executed output, not a hardcoded label. |
| Abstract-side grading G1/G2/G3 (`blaster/abstract/`, recipe exit 0) | EXECUTED GREEN (kept) | `Model.lean` verbatim at recorded commit under its own 4.25.0 toolchain: G1 insert-instance `(true,true)` (all five conjuncts + `step` success; compiled counterpart UNEXECUTED — needs the multi-script tx); G2 empty fold `(true,true)` (model instance, UNLINKED as a CEK mirror per R2); G3 refusal `false` (abstract check fires). Anchors the abstract endpoints of the grading; the shape↔bytes refinement argument stays stated debt. |
| Reproducible entrypoint `run.sh` (gates + all committed assertions) | GREEN both directions (kept) | Clean run exit 0 (`/tmp/blaster-run-green2.log`); dirty tree exits 1; committed flipped expectation exits 1 with the `HashVectors` build failure propagating (TEMP control, reset away, reflog retains it). Proves the runner invokes the checks — a 14-import success alone is never its terminal claim. |

`COULD-NOT-EVALUATE` is always RED and blocks acceptance. Nothing above is presented as
completed #87. `NO-COUNTEREXAMPLE-FOUND` appears nowhere in this ledger.

## Findings (reported, not waived)

- **F-001 — documented-pin drift (our own repository).** `onchain/aiken.toml` documents
  compiler `v1.1.16`; the actual build at this commit reports `v1.1.21+unknown` (blueprint
  preamble + `script-identity.json` compiler field agree). This is the prose-vs-build drift
  the skill warns about, found live. Bound in `identity.json`; the build-reported version
  governs every record, never the prose.
- **Q-001 — commissioning E-label defect (preserved).** The frozen identity prescribed
  `defaultFunSemanticsVariantE`; the pinned toolchain implements only A/B/C. Resolved by
  A-001 (bind C, PV10-bound, primary-source justified). The blocked report stands as
  evidence of the defect.
- **F-002 — oracle mistranscription incident (mine, caught by owner cross-check
  2026-09-12).** Hand-transcribed `blake2b_256("abc")` reference bytes were wrong at ten
  positions; the `abcOk` guard correctly failed. Repair: bytes generated mechanically
  from python `hashlib` with parse-back self-verification (`/tmp/repair-hashvectors.py`);
  the mistranscription is retained verbatim as `WRONG_HASH` with an inequality control.
  Withdrawn in full: the dependency/environment/staleness investigation that chased this
  ghost (it found nothing — there was nothing to find). Standing rule demonstrated:
  generate-or-verify reference bytes mechanically; never eyeball hex or terminal glyphs.
  Third catch same day: snapshot boot #2 failed on a hand-copied token (validator
  correctly refused the mismatch); filler assert + mechanical re-derivation fixed it.
  Also caught by the same discipline: a hand-pasted script address (invalid bech32).
  Both recorded here so the pattern is visible, not hidden.

- **F-003 — copied-policy B-state authenticates A's retirement with no A-state
  present (t87b rival witness, reported 2026-09-12, NOT waived, NO verdict
  rendered in-lane).** `DdfcRival.lean` RR3 HALTs at `ddfc4e9`: B's input at
  the MPFS script address carrying exactly one B-token (seed-derived
  `assetName(B_SEED_TXID, 0)`, recorded standing) with A's `DDFC_REP` copied
  into its datum `representative_policy` fully authenticates an A-record
  retirement (A-claim with A-rep, custody transfer to the pinned script,
  controller signature, no continuation, no mint). Mechanism, each link
  executed: `expected_rep_policy` matches address + exactly-one-name and
  never compares WHICH name (RR2b shows an unwitnessed name resolving the
  same way); `validateMint` leaves `representative_policy` unconstrained
  (source-read, underscore binding), so the copy can exist at bootstrap;
  `Modify` preserves whatever it was given. NEARBY REFUSALS (not a vacuous
  acceptor): RR1 ordinary-B refuses at claim-quantity, RR2a forged-count
  refuses at no-match, T-double/triple-shape controls in the span fire.
  SCOPE (required): app-only CEK HALT on constructed contexts — B's
  bootstrap acceptance, state-spend validity of all inputs, and ALL ledger
  validity are assumed, listed in-file; all-purpose and ledger validity
  must be established independently. NOT a REFUTED of any frozen invariant
  (I-1 governs owner authority; no frozen row governs cross-cage
  association — nothing is stretched to cover it). Verdict (defect with
  repair vs accepted composition with documented boundary) belongs to the
  design owners; routed to the parent with this evidence.
