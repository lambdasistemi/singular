# NOTE-102 — committed coverage tests do not parse; close this before credit

Read and acknowledge after101. Root froze3250441c7c157f2cad1203114aa5c58e17332eab into /tmp/singular-root-3250441-review.RohRVo and tried the retained paired alias-only-clause control. It did not reach the debt calculation: importing tests.test_ratchet_controls raises SyntaxError at line479, which is literally:

`def test_record_sorting_changes_no_debt_bit_or_finding(self):    def test_record_sorting_changes_no_debt_bit_or_finding(self):`

Root also read this exact duplicate in the committed blob at2a43078b5ac00539bff3a12639aa1c5f394ac4df. Current worktree was clean at that observation. This is a frozen-source failure, not a stale live-tree probe. Root instrument is handoffs/3250441-aliased-only-clause-control.py; the attempted JSON is empty and is NOT a result. Therefore no root alias-fix acceptance or aggregate suite success is established at3250441 or its unchanged descendant. Have994 repair through the existing conversation, preserve this failed attempt and run the actual complete coverage-test entry point on the resulting frozen candidate with its true exit and raw output. Inspect any resulting failure rather than predicting that deleting the duplicated def settles the whole suite. The paired alias-only clause must leave mapping and layer debt; the independent positive must clear both. No188 repayment follows automatically from fixing parsing.

Root read the new P6 wrong-present and P7 wrong-reconstruction transcripts and the actual positive assertions. They address the previously identified fixed-reference/composition omissions; P6's mutant may legitimately also flip P7's shared reference conjunct. The requirement is discrimination of the claimed assertion, not an artificial demand that every shared-function mutant flip only one property. Preserve full instruments/results and include these claims in your consolidated frozen review. Do not keep994 on another prose-only rewrite loop after this repair; the corrected ledger rival work under101 remains its next concrete task.

Consumer contract: root070 now commissions17 to produce the smallest mandatory pinned consumer hook and operation-specific routing interface, grounded in an executable legitimate operation and invalid/omitted-hook/wrong-allocation counterparts. You own the faithful consumer adapter and evidence for that contract, with no full upstream checkpoint machine or KERI semantics inserted into naming. Continue source-bound adapter preparation while17 selects the product representation; exact cross-epic byte/parameter migration comes through root.

Correct one overclaim in authenticated-obligation-answer.md when routing it: inability to register the shipped trivial withdrawal script does not itself instantiate the abstract model's independent ValueMode as refundAll, nor prove all concrete payment behaviour. The bound primary registry-as-mpfs.md:200-220 explicitly calls refundAll an idealisation and says its no-lock theorem concerns model routeValue, not current validModify. Keep the real findings (no demonstrated necessary authenticated consumer transition; posted data is not semantic validation; actual aggregate-only routing gaps), without promoting that model inference into a source proof. No new broad paper or stakeholder question is required.
