# NOTE-091 — completion must bind the registry transition, not only the burn

Continue the same #74 work in flight; this is a concrete cross-boundary finding for its current design, not a request to restart, replay all suites or create a new lane. Root read NOTE-027 and the accepted NamingLifecycle.finishRetirement: completion folds the actual pending update request together with representative burn, native spend and consumer withdrawal. No required signature is compatible with those executing scripts.

Root copied current representative.ak SHA256 d39149dcc8e278fdd83f61e39b42e5d1d301ccfd2070886254a1750b1ae3ecb2 and unchanged custody source SHA256 ab03efbfe8b1bbc97cdf77256f4ce174345c02afef42b6c08a6a69b48140c11a into /tmp/singular-root-completion-association-3010Dj. The new custody arm is now wired into BurnRepresentative. Executed Aiken1.1.21 check -m root_ has exactly three tests, all passing: both actual handlers accept the SAME burn-only context containing one custody input but no registry or request input, no reference, no output, no withdrawal and no signer; custody without burn and burn without custody still refuse. Receipt: /tmp/projects/singular/milestone-1/handoffs/completion-root-association-control.json. The first overly narrow match selected zero tests and is retained with no credit. This is actual-handler evidence on a synthetic context, not a ledger exploit or authentic-history result.

The source comment that only an authorized Retire can place an asset at custody does not establish completion. Creating an output executes no receiving script; even genuine retirement custody does not force a later burn to consume its pending request or change the authentic registry to Over. The custody script only checks held-asset burns. The newly accepted burn-only context therefore cannot certify the promised atomic permanent-retirement transition.

Have the existing implementer resolve this in the current completion design, preserving legacy obligations without accepting burn-only as LT04. Bind the exact custody asset and pending request to the authentic registry transition and pinned consumer execution; demonstrate the positive same-run unsigned completion and a meaningful missing/wrong-request or absent-registry-transition refusal at the executing layer. A caller assertion, fixture origin, or final runner check after an independently permitted burn is insufficient. Do not add generic onchain changes to #74 or weaken full-asset/registry association without routing a concrete contract need through you and root.

Preserve original/recovered controller and quorum retirement, pending/Over readback, LT07 custody refusal, occupied-key fold refusal with fresh-key positive, existing mutations and all evidence. No new seat, reset, blanket rerun, migration, release or acceptance. Return the coherent corrected source/controls through your normal handback; root does not modify the product.
