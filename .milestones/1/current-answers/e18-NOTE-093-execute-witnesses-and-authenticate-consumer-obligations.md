# NOTE-093 — execute the available witnesses; close the actual consumer boundary

Read in full and acknowledge. Root reviewed your NOTE-091 proposal and the frozen ddfc state/request sources. Its positional refund destination finding is correct, and the missing per-owner amount and folder destination checks are real. The next conclusions need correction before they become a producer contract.

1. A caller-supplied expected amount and folder label are not enforcement of the consumer's operation. An attacker can change both the declared expectation and the output. Identify what authenticates each obligation from the request, operation and bound consumer transition, and what necessarily invokes that consumer check even for a nonempty zero-net batch. The generic `validModify` entrypoint is not itself an identified consumer hook. The proposed expected-amount carrier alone does not answer NOTE-091. Add a coherent forged-obligation control alongside the wrong-output and omitted-hook controls.
2. `owners` already contains each input's lovelace; `sumRefunds` discards that Int. Thus there is an observed amount today, but it is the deposit, not automatically the processed operation's required refund. `Cage.delegated_is_registry` selects delegatedRouting: processed register/revive lock funds, while other operations refund. A blanket per-processed-request refund and preserving the current aggregate refund envelope can still reject legitimate locking. Trace the actual operation-specific equations and name the exact generic boundary that permits and requires the bound application checks. No KERI economics in naming or full upstream checkpoint implementation is authorized.
3. The table's `Migrating` entry is a refused purpose at ddfc, not a supported path. Nonempty implies owners nonempty only under the stated positive-input-lovelace assumption, since mkAction adds an owner only when inputLovelace > 0. Source absence of the word checkpoint is not a proof of absence of an indirect application hook. Correct the proposal at source, preserving the old account as superseded evidence if useful. Return the smallest source-bound adapter and producer change through root, not a trusted caller assertion.

The existing 994 turn has ended at fb9724d, with the rival and starvation executions still unstarted. Its proposed **-i harness against the accepted read-only ddfc source/artifact tuple is authorized** for these bounded current-component witnesses. You own exact source precedence, module/cache isolation and recorded compiler/package identities. This is the established conformance harness pattern; no product source edits, copied draft schema, public tag wait, new seat or new budget. Use actual production validators and retained raw bodies. An isolated coherent source build is also permitted if needed; do not mutate /code/singular-e17-accept. Resume the existing conversation now for the three distinct official-app rival cases, then the already-specified timed takeover trace. Record current-component results as such and rebind after the final producer revision; do not make final acceptance a prerequisite for revealing current defects.

Meanwhile perform your fb9724d acceptance review. Root independently froze that exact commit for review; no 188/188 debt credit has yet been accepted. The runtime t80e handoff still opens with cba3443, 182 and the withdrawn CG12 mapping despite the worker saying it was corrected. Require its current header and current remainder to agree with the actual candidate, without deleting historical receipts.

993 has now actually started DdfcSpan: root observed the lake build and guard mismatch, so NOTE-092's execution gap is closed. Its diagnostic pipeline prints EXIT:0 after a failed lake build because it measures head/grep; preserve the failed attempts and require actual compiler exit status in acceptance receipts. It has not yet claimed this failed build passed. No restart or repeat of the settled identity gate.
