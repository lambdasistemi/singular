# NOTE-063 — bounded local Fork override authorized through the existing producer owner

2026-09-12T19:14:56.871Z. Read and acknowledge. Root read worker81's Q-001 and full fork-precondition-binding.md, its new terminal journal19:11:21, and actual onchain/flake.nix dependency staging.995 is now authoritatively BLOCKED on your dependency answer, not merely quiet. Consume its packet and answer through your protocol; no new worker.

ROOT AUTHORIZATION: proceed with the minimal v2.0.0 lone-Fork repair as a TRACKED LOCAL PATCH/OVERRIDE in Singular using the existing Nix source staging. Concrete base is the current immutable aiken-lang/merkle-patricia-forestry v2.0.0 fetch, SHA256 sha256-uHVQxA1dYDuPbH+pf6SkGNBF7nBlDXdULrPFkfUDjzU=, source qvd74r6h60g63myf0a2yqm3kc9zqprlc-source. Limit implementation delta to the established prefix-handling defect and required discrimination vectors. Preserve license/upstream origin, full patch digest and disclosed provenance. No new public fork/tag/repository is needed to make this local candidate reproducible and reviewable. An upstream v2.1.0 bump is not selected.

The worker Q omits a material producer boundary: onchain/flake.nix separately fetches v2.0.0 and COPIES it into build/packages for BOTH blueprint and checks, with its own fixed hash and packagesToml. Merely changing aiken.toml/aiken.lock would leave the Nix-built production artifact on the old code. Necessary owned surface is the tracked patch, onchain/flake.nix's shared source staging, disclosed source/patch provenance, regression vectors, script-identity regeneration and any narrowly necessary package metadata/reproduction entrypoint. Keep all build/check/dev-shell reproduction paths honest about the SAME patched bytes; measure actual staged source digests and prevent an existing versionless cache from substituting another package. No unrelated dependency refresh, global cache destruction or upstream source edit.

Acceptance is still owed, not implied by this permission: use retained C witness with exact pre-root fbef6b6d9a253acf2ecf645fd62e663535b828595b8ea38ae2118038d4f9c5a7 and post-root cd1f3928377ab7e6d67e05a217d73c6e63dc796fcb90b752fb554ac3049defcf. The REAL packaged patched validator must accept the valid absence insertion on a fresh devnet and refuse C already present; removing the patch must reproduce the original failure with coherent raw bodies/identities. Bind substrate guards and keep application-layer gaps as debt. Retain function-level checks and investigate any discrepancy rather than calling the offline engine untrustworthy on the strength of a hand transcription. The native ledger is necessary acceptance evidence, not permission to ignore contradictory actual compiled results.

You own final serial integration with the current77 revision. Both changes affect state code and downstream naming/consumer identity; compute actual combined hashes after safe integration, and route one coherent final tuple through root.18 owns downstream replay/coverage and87 compiled checks. Extend the temporary producer fence to the narrow onchain patch/build/provenance paths;18 must not mutate them. All four existing seats preserved, no new audit, no user semantic ruling required because absence behavior is unchanged.

Return your bounded final gate/plan and actual995 RESUMED acknowledgement. No root-direct child commands, no duplicate proof campaign.
