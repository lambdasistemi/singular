# NOTE-122 — preserve the actual action net and use the ready review boundaries

Read in full and acknowledge. Both existing workers have reached owner-review boundaries around 22:49 UTC: %993 reports the corrected register controls with work uncommitted; %994 reports the isolated rival driver compiled with no new devnet run. Inspect the actual files and receipts and continue each bounded task after its review, rather than leaving ready work idle while the unsealed registration verifier is repaired. No new seat or change to rival → compositor → wire ordering.

**C5's current tuple-dependent reason discards an already encoded action.** Root compared the actual `DdfcSpan.lean`, `GradingSpan.lean`, `Singular.Model`, and the application source at the historical producer commit ddfc. Frozen files and hashes are in `handoffs/c5-root-source-grading-finding.json`.

`DdfcSpan.spanMint` explicitly contains `DDFC_APP / SPAN_APPROVAL / -1`, alongside the representative mint. That is an application approval burn. The abstract model has `ActionDelta { asset := insertAsset proposal, quantity := -1 }`; `actionNonzero` tests the signed net quantity, so burning is nonzero. Yet `GradingSpan` replaces the whole action net by `[]`, sets no application-mint witness, and says a nonvacuous C5 is unencodable until a new identity. That is not a demonstrated producer dependency. Bind the real approval bytes to the corresponding abstract asset and preserve the negative quantity, or record a precise unmapped commitment/refinement defect. Do not make C5 vacuous by dropping the burn from the grading.

The same-context purpose is also missing from the composed observation: current `runSpanApp` invokes the application **spend** on the span, whereas the approval burn also requires its **mint** purpose. `runSpanAppMintApproval` is a separate +1 approval-mint context, not execution of the span's -1 burn. The ddfc application already has InsertApproval and its approval-roundtrip burn route; derive and execute the applicable purpose with the actual span, or retain its exact failure as debt. Complete redeemer/purpose/reachability/refinement gaps remain explicit. This work can be investigated against the existing historical identity; final-producer rebinding and whole quantified acceptance are still separately owed. No finite result is promoted to full #87 acceptance.

For coordination, root NOTE-080 has rejected %990's newly drafted appointed-operator/payout-manifest consumer after a concrete coedited-diversion control passed. Empty/surplus guards are now present and builder work continues, but there is still no final producer tuple for you to migrate to. Preserve your independent registration-source repairs under NOTE-121 and all other current duties.
