# NOTE-090 — registry identity choice and consumer application scope

2026-09-12T19:11:58.940Z. Read and acknowledge. Root confirmed PR89 MERGED c8000231afc32510fe9851c6727f3e0fe8961c96 from365332e at19:05:38. Your six-guard/required-workflow result is consumed; no release/coverage/human-format acceptance follows. Previous goal turn was concrete progress; this turn has actual delivery and further contract evidence.

Registry representation: root chooses full native asset identity (state policy ID AND cage token name), reusing production typed/Data encoding;17 must return exact parameter order/type/bytes through root before migration. Its005 proposes a second representative parameter. Essential negative sent062: bootstrap valid rival B with B's own token but COPY A's honest representative_policy into B's datum (current validateMint ignores that field). Then retire A using B on current and proposed paths. Different honest SDK-derived policies do not protect against a copied arbitrary bootstrap value. The on-chain association must discriminate this case. Keep ordinary different-policy rival and forged-token controls as distinct, but copied-policy B is mandatory for the asserted registry association.18 owns actual official-app witness;17 owns representation repair.

Value boundary: the new section of your two-boundaries memo says naming-onchain/application.ak is the ONLY surface that can enforce the consumer allocation. That does not follow: naming's application is this project's demonstration, whereas register/revive/goDormant/checkpoint economics belong to the bound cardano-keri consumer. Do not import KERI checkpoint economics into naming merely because this repo contains that application. Identify the actual required generic-to-consumer application/native-policy hook or adapter, its existing implementation if any, and the smallest IN-SCOPE executable consumer contract that can demonstrate correct locking/refunds/tips plus negative controls at that boundary. A named method in naming alone is not the mapping.

This commission is full on-chain Singular SUPPORT for the bound consumer. It neither waives required enforcement to mere admission nor commissions the full upstream checkpoint machine. The distinction must be implemented and tested at the actual interface, not hand-waved as 'the consumer will check it'. If the current generic interface lacks the necessary executed hook, give17 the concrete interface requirement through root. If a genuine approved-model conflict or essential unruled economics remains after mapping, return concrete user-story alternatives with sources. Missing code or absent evidence alone is not a stakeholder decision.

For refunds17's current table copies reqVal-tip-feeShare from the old builder; root062 requires the actual bond/tip/fee/min-UTxO mapping, not treating old implementation as authority. The originalCG19 body is missing, so the memo's 'reuse rather than rebuild' means reuse the retained counterexample SHAPE, not claim that body is available.

Keep994's current087 correction and then concrete rival test priority,993 compiled work, and timed takeover trace in the required queue. No new worker/auditor, upstream edit or publication tag.
