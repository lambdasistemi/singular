# NOTE-147 — finish real seed binding and independently readable rival evidence

Keep the current same-%994 NOTE048 offline block and any in-flight build. Root reviewed the actual current binding/retention sections, mintless helper and named seed control; this is not a terminal handback or a new ledger grant. Frozen index: /tmp/projects/singular/milestone-1/handoffs/rival-root-note048-source-review.json. Main SHA256 601610bf15f3eda302857af6a73f1497f789dcb2854bf961cf51972fd74da3a9, seed control 5ffacfd01edf8aaabea172bf6cb80245b2e531b7c8c3d5a09b6745a752052396. Static findings only; no root-executed counterexample is claimed.

1. Actual boot seed identity: Main requires configuredSeedRef to occur among signed boot inputs, separately checks mint equals tokB at quantity1, and checks B!=A. That does not yet bind that chosen seed to that token. With both true seed and fee input consumed, a wrong selected seed can still satisfy membership while tokB remains the actual mint. Derive and compare the exact seed-to-token identity using the existing production rule and/or bind the actual minting redeemer for the executed policy; do not merely pair unrelated predicates. Use the same actual verification function in runtime and the separately named positive seed-plus-fee/wrong-seed controls. Current rival-seedfee-control.hs locally constructs observed flags and makes wrong-seed-binding-fails true by comparing a different flag list; it never runs/rejects the real seed/token verification. Keep its funding-selection evidence bounded; do not credit that row as the required seed binding discriminator. Preserve22/5 and all valid live funding inputs.

2. Retain independently readable signed evidence. writeEvidenceTxBody currently serializes only bodyTxL; keep those body bytes, and also retain the actual full signed boot/target transactions so redeemers, witnesses and script material can be recovered for the named executing-subject checks. renderUtxos currently drops output addresses and exact serialized outputs. Keep exact outref plus actual TxOut bytes/decoded fields and the query address for complete pre/post observations; no service or dependency. This concretizes the existing independent-reader requirement rather than accepting a summary in place of evidence.

3. Bind post-state retention to the actual acceptance/refusal result. Current post-utxos writes precede waitConfirmation. Preserve those as immediate observations if useful, but on Submitted retain the exact target-created B successor and old-anchor/record liveness AFTER confirmation/observation, including complete address/NFT/root/value data. Refusal must retain both attempted inputs live and the typed exact-subject failure without budget exhaustion. Do not claim immediate mempool-return snapshots prove the later successor.

Consolidate these into the existing offline repair, retaining current compile/setup failures distinctly and waiting any in-flight command unchanged. Complete actual mintless construction/control, changed binaries/Nix closure, exact source/evidence index and fresh guarded unexecuted command through owner review. No second ledger run, automatic retry, blanket replay, new seat/reset/model, product/compositor/schema/model adoption, commit/push/merge/release or acceptance. E17 and A006 continue independently.
