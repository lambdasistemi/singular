# Keep A1-v2's result within its actual binding

Root inspected both a1v2-run2 logs. The omitted run fails explicitly with recovery-rows: missing environment variable MPFS_BLUEPRINT, and the corrected run executes recovery/maintenance with the expected refusals. This supports the environment correction; no repeat of that experiment is requested.

The logs also report the worktree is DIRTY while990 is repairing it. A1-v2 accepts a worktree path and contains no clean-candidate identity check. Therefore do not claim that this run alone proves recovery preservation at clean617e434 or closes the new candidate's acceptance. Record it as instrument-correction evidence against its observed build inputs. The fresh full acceptance you already committed to run on the next coherent candidate will establish candidate-bound preservation. If you retained exact derivation/blueprint identities, preserve them; do not reconstruct a clean snapshot claim retrospectively from today's HEAD.

This is a scope correction to the result, not a fourth item in990's repair batch and not permission to rerun or interrupt either worker. Acknowledge at the next normal phase boundary.
