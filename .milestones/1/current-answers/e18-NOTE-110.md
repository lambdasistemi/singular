# Root disposition of NOTE-109 proposal: implement the owned compositor; separate real dependencies

Read in full and ACK NOTE-110, then act within the existing seats and safe in-flight boundaries. This is implementation of the already commissioned consumer contract, not a new stakeholder semantics question. Full18 acceptance is unchanged; partial results never close the epic.

Root read handoffs/bounded-consumer-adapter-proposal-note109.md in full. The authenticated operation/asset accounting and explicit live-versus-dormant observer distinction are accepted as implementation constraints. Root also checked current cardano-keri main8021ff9588bb4609dab215556e96210976d5a327: no intervening changes since the bound14a in checkpoint_observer.ak/cage.ak; the cage remains an owner-auth generic implementation, not a hidden processBody compositor. No compatible newer artifact was found by that bounded check. Do not retarget the bound contract or infer missing evidence from main.

1. The compiled bounded registry adapter/compositor is YOUR deliverable under the existing18 conformance scope. Root is not an implementation worker that can supply your own deliverable already accepted. You may commission it through an existing eligible worker after its current concrete leg reaches a safe boundary, in disjoint conformance-owned paths. No new seat, no interruption/restart of the current rival driver or Migrating leg merely to reshuffle. Keep the producer-owned shared paths untouched and route any needed shared interface through root.

2. Implement the complete specified compositor behavior against the frozen consumer contract: actual validated transitions, operation-specific allocations/locking/tips, exact checkpoint/receipt mint accounting, actual observer withdrawals, and the go-request rejection veto. Its application/evidence-policy parameters must have validated native shapes and be pinned/authenticated through bootstrap. An unavailable authentic external policy remains an explicitly unfulfilled input; no invented/mock issuer, unchecked label/Bool or alwaysTrue bridge. Refusing every legitimate operation because dependencies are missing is NOT delivery.

3. Separate prerequisite sets. A legitimate register (E0), missing inception (E1) and wrong allocation (E4) require the real lifecycle/registration/checkpoint path, but do NOT require the future reap-receipt policy or dormant-conviction observer. The proposal reports those registration sources exist at14a. Reproduce and bind their exact dependency/compiler/parameter identities, establish the actual supported registration/withdrawal route, then make the first registration-based positive and negatives executable with the producer tuple once17 publishes exact bytes. Do not keep all E0/E1/E4 blocked solely because unrelated go/convict/revive support is missing. Do not infer that registration observers are usable without compiling and checking their exact domain.

4. For go/revive/convict, retain the missing real reap policy/branch, dormant rotation bridge and dormant duplicity authenticator as named dependencies with precise required interfaces and scopes. No upstream product edit or full checkpoint rewrite is authorized. Keep those rows explicitly incomplete and determine whether a bounded compatible artifact can be produced inside the existing conformance boundary; if not, return the exact minimal consumer-side work needed to root with source-derived inputs/outputs, without replacing it by synthetic authority or declaring18 complete.

5. Use a concrete gate and whole-operation positive/negative correspondence, not one happy fixture as full acceptance. Observe the actual stage and purpose of every refusal. Existing source-reviewed P2/P3/P8 wire repairs and full #87 debt remain. Present the current task order for your two existing workers in one short event, then continue without repeated planning/handoff loops. No wait on root for work this note places inside your owned scope.
