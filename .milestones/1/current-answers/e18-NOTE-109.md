# Root: complete the source-bound consumer adapter contract

Read in full and acknowledge NOTE-109 in epic STATUS, then continue your current source review and bounded executable-consumer proposal in the same run. No worker reset or new seat.

Root independently read the frozen primary handoffs/consumer-value-primary-20260912/registry-as-mpfs.md, heading "The plugin's contract, derived" (approximately lines254-266), and Registry.processBody. This is an additional concrete source requirement for the proposal, not a new product ruling:

- registration/revival: accounted checkpoint-policy mint and bonded checkpoint output; actual inception/rotation authentication;
- go-requests: receipt token minted by the checkpoint reap, consumed and burned by this fold;
- dormant conviction: enforcement observer withdrawal bound to the recorded k, with the currently missing compiled duplicity semantics named honestly;
- refuse Delete, Rejected on a receipt-carrying request, End, and checkpoint/receipt mint or burn not accounted for by actions.

The document explicitly says the receipt is NOT represented in the Lean machine because the model's reap creates the request itself. Therefore an implementation of only processBody plus r.op.userPostable does not establish the full on-chain contract: a coherent caller-chosen go-operation is not an authenticated reap receipt. Include exact receipt-policy identity/provenance and unaccounted-asset controls in the executable proposal/coverage, or name their actual missing implementation. Do not substitute unchecked operation labels, a declared Bool, arbitrary plugin or fabricated receipt issuance.

Preserve the existing scope: generic producer hook belongs to17; a bounded consumer adapter and its witnesses belong to18; no upstream product edit or full checkpoint-machine rewrite. No E0/E1/E4 acceptance on producer bytes alone. Distinguish what a concrete conformance fixture proves from downstream consumer readiness. Supply a bounded implementation proposal and exact unresolved boundary to root while the existing994 rival ledger driver and993 Migrating leg continue. Do not turn this into a new stakeholder decision request.
