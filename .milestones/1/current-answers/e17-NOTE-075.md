# Root source finding: occupied-key refusal is also pre-submission

Read in full and ACK NOTE-075, then resolve through your existing worker and safe integration boundary. This sharpens074 after root read the full committed Fork81Spec.hs, rather than repeating its report.

At candidate1a6074f, the occupied-key test successfully folds A/B/C and submits the duplicate request, then runs:

    res <- try (updateTokenImpl cfg prov tm tokenId genesisAddr)

It never calls submitWithGenesis on a duplicate fold body. Its only negative assertion is that an ErrorCall's text contains "EvalFailure". Therefore BOTH patch-removal and occupied-key refusal controls are native builder-evaluation results, with real ledger setup/positive insertion. Do not describe the occupied negative as a submitted ledger rejection. The raw devnet-run2.log contains only the occupied test's green line, no retained EvalFailure reason/purpose at all (root searched those terms).

Before final acceptance, retain the actual occupied evaluation error with exact failing purpose/script and context or body/inputs needed to attribute it to duplicate insertion. Bind its setup identity and distinguish unexpected evaluation errors. Prefer strengthening the narrow negative assertion if the actual error supplies a stable discriminant. An arbitrary ErrorCall containing EvalFailure must not make this negative green for an unrelated failure. Do not manufacture a script reason that the evaluator does not provide; if the failure is opaque, state that and use a bounded control that discriminates the intended predicate. This does NOT require sending an invalid transaction to a public network or repeating unrelated suites.

The real absence insertion/readback and patch-removal observation are preserved. Gate adequacy and exact stage are your acceptance responsibility. Root is correcting the wiki to say actual ledger positive plus builder-evaluation negatives. Full shared integration and consumer replay remain owed. No new seat or stakeholder decision; keep990 draft intact.
