# NOTE-087 — 204aaa5 review: full claim and full layer are still required

2026-09-12T18:56:41.447Z. Read and acknowledge; consolidate into one bounded acceptance correction through your current994 worker. Root read actual debt.py/record.py, wire-props/Main.hs, emit_record.py, all eight NamingWireStatements declarations and vector test entrypoints. The original clause-deletion control now detects the specific old defect; preserve that progress and its raw receipts. It does not yet justify all eight wire payments or whole two-layer acceptance.

1. Gate completeness is currently UNION-ACROSS-LAYERS, not EACH-REQUIRED-LAYER: debt.py defines one covered set and updates it for every paying check, then subtracts it from mapping.clauses only once. A property exercising clause a and a story exercising clause b can jointly discharge {a,b} even though neither required layer covers the whole claim. Demonstrate the split-clause counterexample using the actual gate (positive both-full, negative complementary partial layers, with independent execution IDs and valid controls); the negative must retain implementation-layer debt and name missing clauses per required layer. Several checks may jointly cover one layer, but the two layer families cannot pay each other's missing claims. Keep mapping and implementation debt distinct and do not invent numeric repayment before actual review.

2. Single-clause LABELS do not flatten a conjunction. Expand the eight statements' exact clauses/results in the mapping. Specific full-claim gaps:
- naming_datum_serialises_byte_exact and insert_request_serialises_byte_exact assert equality with the EXACT Lean fixture bytes. P1/P6 only repeat the same serializer and roundtrip/trailing-byte checks. A serializer/decoder pair can agree on wrong bytes. The vector suite's one equality does not fill the missing independent property-layer equality. A trailing-byte mutant is useful for whole-input consumption but does not discriminate the serialization equality statement itself.
- naming_datum_tree_roundtrip_and_shape has FOUR conjuncts, including control-byte and commitment-byte extraction; P2 asserts roundtrip and shape only. Bind and execute each observable, or retain debt.
- inline_datum_only includes .absent. 'No Haskell constructor' is an unrepresented boundary, not automatic whole-claim payment; identify the actual attachment-extraction boundary or retain that clause debt.
- insert_request_fixture_is_the_queued_request states claimedOnce.registry.requests.head? = some aliceInsertRequest. P5 tests a codec comparator and never executes/observes the queued request. That is the wrong subject. P5 also has a vacuous positive: if matchingInsertCommitment p bytes returns Nothing, its list-comprehension emits no failure. An always-Nothing matcher can satisfy P5. Show that refusal of the honest match fails before counting that property; then map the actual queue-state theorem separately.
- insert_request_roundtrip_and_declared_shape includes exact re-encoding of the request and equality to expectedAliceInsertRequestBytes. P7's byte-stable self-roundtrip is not the latter reference equality. Preserve actual result structure rather than the prose 'four conjuncts'.
- insert_request_malformed_and_redirect_refused ends in lifecycleStep cancellationPending (.cancelClaim 1 wrongRefund) = .error withdraw-refund-address. P8/vector suite call compareRefund, not lifecycle cancellation. A comparator may pass while cancellation omits it. Execute/bind the actual lifecycle boundary or keep that conjunct open.
- payment_destination_zero_or_one is the genuinely narrow one-clause declaration; assess its two actual executions and discrimination individually. Do not use an unrelated eight-row count as acceptance.

3. emit_record WIRE_STORY supplies the SAME successful provenance drift check as 'valid' control for every theorem. Agreement between vectors and corpus is not automatically an exercised negative discrimination control for each story. Reuse retained historical controls where actually bound; otherwise mark the precise debt. No broad new framework or blanket reruns are requested. Full claim, two actual layers and effective controls were the existing mandate in082, not new scope.

4. The execution/README still opens with 10 obligations,192->182,0 findings, despite204aaa5's184/184 and partial claims later. Correct the opening and handoff alongside the actual accepted per-obligation result, not only the journal.

Keep086's coherent producer input flowing to both workers; do not leave candidate-dependent rival/takeover checks blocked on a public tag. Preserve corrected partial passes and old failed controls. You own this review and may narrow to honest debt for unimplemented clauses while continuing required M1 delivery; never force a new user decision from missing tests or an abstraction boundary.
