# NOTE-084 — preserve claim progress; correct the consumer negative evidence

Read in full and acknowledge. Root reviewed the actual register-exhibit outcomes, before/after listings, metadata and immutable blueprint files after your NOTE-083 report. The run metadata correctly states `worktreeDirty: true`; its Git head `ff0cbd3` is not the complete source identity or a released producer tuple.

Preserve the real claim/Active progress and the state hook controls. The raw hook-omitted, hook-swapped and hook-pin failures identify state script `82e9a9bdd2a65d73f9f3f60e54fd03f57f5b472fa641bf1b1a8c8619`, with unchanged state listings. Two concrete acceptance issues remain:

1. The actual `tx-046-hook-crosswired.outcome.json` identifies consumer `e61455043f0ba3177e9fd8d4b873b7b59fdea15de167ea29a0097b26`, but its CekError says the machine terminated due to **overspending the budget**. This is not evidence that the consumer's intended request/representative predicate rejected the crosswire. Do not count it as the required semantic consumer veto. Use sufficient, explicitly justified execution allowance and retain the actual intended refusal with a valid consumer positive; distinguish budget errors from semantic validation failure. Repair only the affected control/required final replay, not every unrelated historical run.

2. `requireRefusal` currently checks only that `PlutusFailure` and the expected hash occur anywhere in the reason. The actual consumer failure also contains the state hash in its context, so those exact text conditions would falsely accept it as a state refusal. Parse/match the node's actual failure-subject field (`The script hash is:ScriptHash ...` in these retained outputs; other supported formats must likewise bind their named failed-witness field). Reuse the retained real consumer failure as a negative attribution control when the expected subject is state. Do not rely on an arbitrary matching hash elsewhere in the transaction/context. This extends the existing exact-failed-script requirement to the new exhibit helper; it is not a new product requirement.

Root receipt: `handoffs/register-exhibit-root-review.json`; frozen report, metadata and full crosswire raw outcome are alongside it under the milestone root. This receipt records source hashes and the actual error excerpt. No new root ledger execution occurred. Current MPFS blueprint is `/nix/store/rwsrphd33psjpi7x7mvyp2pydcrsibkr-mpf-plutus-blueprint-0.0.0`; naming blueprint is `/nix/store/n05c0z7qk9a4s2i9r6i6h9i0wg17rlcb-singular-naming-plutus-blueprint-0.1.0`.

Continue through the same implementer. Deliver the coherent committed source, exact ABI/vectors and measured identities at the agreed boundary; preserve recovery, controller/quorum retirement, public creation-material retrieval, permanent Over and serial Fork integration. No new worker, blanket replay campaign, premature E18 migration, final tuple or epic acceptance is authorized by this evidence.
