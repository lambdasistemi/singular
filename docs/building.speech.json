{
  "build-and-check": [
    {
      "text": "Build the documentation with Nix. The documentation check validates rendered links, anchors, speech and theme assets. Separate model and simulator checks exercise the proved Lean model and finite corpus, and the model check cross-checks the compiled axiom report against the theorem manifest; no application proof is claimed. The browser check launches pinned Chromium and executes actual simulator controls using a temporary local server. The build gate realizes packages, checks, and development shell inputs before downstream jobs.",
      "pause": 400
    }
  ],
  "edit-and-serve": [
    {
      "text": "Use the Nix development shell for a strict MkDocs build or live local server. A separate CI build verifies that shell.",
      "pause": 400
    }
  ],
  "publication-and-accessibility": [
    {
      "text": "Pull requests have a live preview without comments. Main publishes through GitHub Pages. The site includes a theme toggle and section read-aloud controls using browser voices.",
      "pause": 400
    },
    {
      "text": "Diagrams are Mermaid, rendered by the copy the shared toolchain pins as a Nix input and served from the site itself. No page loads a script or stylesheet from outside the site, and the site check fails if one does.",
      "pause": 400
    }
  ],
  "documentation-releases": [
    {
      "text": "Documentation version zero point one point zero is prepared consistently in the version file, release manifest, and changelog. That does not establish a tag or published archive. Release-please proposes later version changes in a pull request. An operator-authorized version tag on its merged commit publishes the Nix-built archive and checksum, verifies uploaded bytes, then updates bookkeeping labels without comments.",
      "pause": 400
    },
    {
      "text": "The archive is also a runnable review workspace. Under artifacts review it carries model and theorem sources, corpora, scenarios, simulator and replay sources, checkers, and a minimal flake with its exact lock. After extracting the actual archive into a fresh directory, verify the internal checksum manifest and run the review flake's check app. Nix may acquire only the exact locked toolchain; all model and replay inputs come from the archive. A checkout pass or presence check does not establish archive reproduction, and no release tag is authorized by this candidate.",
      "pause": 400
    }
  ],
  "_source": {
    "file": "building.md",
    "sha256": "748b7c963ad3044f51297cad1c764d0690178ced6666cc6bf4daf3e692aa04ae"
  }
}
