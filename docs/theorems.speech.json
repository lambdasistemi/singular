{
  "what-the-eleven-promises-are": [
    {
      "text": "The interface states eleven guarantees the registry makes for every application, and the model proves each one over states reachable from genesis by folds. Reachability is a hypothesis, not a weakening: over an arbitrary state value the supply laws are simply false. Every promise is reached through one invariant that the model proves survives each of the seven edges, which is why the statements read as consequences rather than as separate arguments.",
      "pause": 400
    }
  ],
  "exact-declaration-inventory": [
    {
      "text": "Every declaration keeps its qualified name and a digest of its statement text, so a changed or missing obligation is detectable rather than merely unlikely. The column beside each name says what it states, because a theorem can be true and narrower than its name. The absent-insertion transaction statement now binds a refund-only custody payload, its deposit, and the key recovered from its sole absent asset, together with the constructed transaction and logical root effects. Its proof does not certify the current on-chain wire. One statement covers who must sign a fold: at every one of the seven edges the transaction the model builds requires no signer, and neither the step nor the transaction changes when the approval carries a different set of signatures. Five statements cover where a request's deposit goes, each over every way a request can end — a fold of any of the seven edges, a reject or a retract. No exit strands a deposit: some payment the exit owes is at least the deposit. Only a retract owes the tip. What an exit owes is read off the request alone, including, for a retract, the output reference the request sits at, to which its return is bound. Value an exit does not owe is unconstrained: giving the recipients it owes at least as much lovelace — for a retraction, in the one output bound to its request — never turns a settled transaction into an unsettled one, and fees and the folder's tip play no part. And every transaction an exit builds settles what it owes. One more statement says that a retract, in any registry state, pays exactly its obligations and nothing the state holds. The three transaction statements now carry the deposit in the output that pays it: at the cage for an absent insertion, with the token for a registration, and back to the owner, in an output with no datum, for a retirement. Four statements say which pending requests their owner can retract, and when. A retraction is admitted exactly when the request inserts a key or reads a terminal one, its owner signed, and its validity interval lies inside phase 2; a refusal names the first check it fails, in the request script's order: insert-only, then the owner's signature, then the window. The request script names that window refusal not-phase2, as its exact-outcome tests assert. An admitted retraction is the retract exit itself, and admission changes no other exit. And a retraction refused admission is refused for that reason whatever it spends or pays.",
      "pause": 400
    }
  ],
  "how-this-register-is-kept-honest": [
    {
      "text": "A compiled gate runs while the library elaborates and fails the build if any theorem depends on an axiom outside the standard three, so a sorry cannot reach this page. The model checker then cross-checks the sources against the manifests, the compiled axiom report against both, and this page against the manifest. That last check exists because it was missing: before it, the page claimed forty-one declarations while the manifest held forty-four, and three proved statements appeared nowhere.",
      "pause": 400
    }
  ],
  "what-this-establishes-and-what-it-does-not": [
    {
      "text": "These proofs establish properties of the model. They do not establish that the model is the right model, and no build ever will — that is what the operator's rulings, the interface and the simulation are for. The model is also abstract where the chain is concrete: commitments stand for collision-free canonical commitments, and the executable consumer supplies the real hashes. Retraction admission is stated over a finite validity interval, with its lower bound included and its upper bound excluded, as the request script and the ledger read it. The model's bounds are finite, so an interval open at either end is not a case it can be asked: that is a named non-goal, not a gap waiting on a ticket, and the chain side is pinned by the request validator's Aiken exact-outcome tests, which admit the window's two endpoints and return the named refusal not-phase2 one unit before the lower bound and one unit past the upper, and for an open interval alike. The finite cases are compared live: the retraction-window row submits an owner-signed retraction before phase 2 and one after it on a devnet, both refused by the request script and answered not-phase2 by the model, which agrees on every step. The deployed validators are compiled without traces, so this run's live refusals expose no name: they are attributed to the applied request script, and the name itself is never described as a live observation.",
      "pause": 400
    }
  ],
  "_source": {
    "file": "theorems.md",
    "sha256": "bc04aec0cbe1dd744cde72ce4c04d96bcc828a5ce28faecff55827b0b8bb71c8"
  }
}
