# Singular project

## Persistent goal and current candidates

Operator goal: get epic15 done, with the original full six-child20-25 scope, independently accepted executable naming model/contracts/scenarios and published simulator release. Goal active, not completed. Epic owner acknowledged NOTE004 at16:59:54Z, no campaign/budget reset.

Child20 draft PR27 https://github.com/lambdasistemi/singular/pull/27 exists, observed head895f01d759a0904190c4bbeffe94caecb1647bde. This is in-progress, not accepted. Epic owner547090 and its Grok T.O.556714 verified live.

Standalone documentation link issue26 https://github.com/lambdasistemi/singular/issues/26 and draft PR28 https://github.com/lambdasistemi/singular/pull/28 exist outside M1/epic15. Observed headf00b5800024dcc5dfe6d2a64404fed3157ee81d0; docs owner573282 verified live, GLM implementation launch recorded by that owner at16:59:23Z. Exact current child evidence belongs to each immediate owner. No acceptance/merge inferred from draft PR existence.

Wiki d25c9c50f556bf643e60589c5521669b51f242bd:21 stories,6 groups;sim-claim in-progress maps PR27. Presentation gate, renderer byte check and changed diagram render passed. Milestone description corrected to actual execution commission instead of stale planning-only prose. Documentation-link task is an explicit standalone exclusion. Publication still held pending its accepted fix; v0.1.0 not tagged. Current notifier PID is runtime notifications/notify.pid, two sources at600s cadence.

## Current standalone documentation repair and release disposition

User ordered immediate README/docs link fixes and a complete documentation link audit, explicitly aside epic15. Standalone Grok T.O. docs-links is active at%850 (PID573282, START16:47:38Z), runtime /tmp/projects/singular/milestone-1/docs-links, initial window singular-no-epic-t-unknown-doc-links in mpfs-onchain-ms2. It owns its issue/PR/worktree bootstrap and GLM implementation plus fresh Sol audit. It is not a child of15/20; no M1 naming scope is added. Scope: README and documentation links, bound speech freshness and meaningful source/rendered/live link verification. Canonical existing simulator URL https://lambdasistemi.github.io/singular/simulator/ returns200; README simulator/ opens GitHub source and docs/simulation.md ../../simulator/ misresolves.

Epic15 acknowledged NOTE003: link assignment was never forwarded to20, which remains focused on naming Lean/simulator. Overlapping README.md, README.speech.json, docs/simulation.md and docs/simulation.speech.json link-repair edits have temporary priority in the standalone lane; coordinate through milestone, not direct sibling control. Grok20 START verified by epic owner16:40:11Z, pane849 PID556714; other descendants are owned by their immediate T.O.

Release Q001 disposition: PR2 guarded squash merge landed9e8d6b57a6e538719b8f782b521bd456301df71e at16:44:02Z before the link correction. Version/manifest0.1.0 on main, ZERO tags and releases observed. Tag v0.1.0 HELD under NOTE002/003 and A002 pending docs-link fix. No undo/retag and no claim publisher works. After fix lands, inspect actual next release PR/version/head for renewed bounded authorization. Old A001 does not authorize a moving release candidate.20 proceeds independently.

Telegram singular-ms1-844 topic5442 now relays epic15 and standalone docs-links STATUS every600s. Current notifier PID is in runtime notifications/notify.pid, launched under tmux server. It is outbound telemetry, not inbound control. Stop relay only when covered work ends or user requests, preserving sibling topic users.

Acknowledged packets and launchers are copied under resume/. Current live runtime STATUS and handoffs take precedence over historical planning-only statements below. No main source edits by milestone owner; /code/singular remains operator-owned.

## Active execution commission: first epic

The operator explicitly commissioned epic15 with an Opus epic owner, Grok ticket owner, GLM commit owner and Sol-only independent audits. This supersedes all earlier planning-only/idle statements below FOR epic15. Source copy19 stays in epic16; later epics are not started. Six serial children20-25 deliver address-handle Lean/model/contracts and the corresponding docs simulation. The naming Lean slice precedes matching simulator behavior; independent Sol review remains required.

Epic owner: pane%848 window@288 singular-e15-t20-naming-simulator in session mpfs-onchain-ms2; runtime /tmp/projects/singular/milestone-1/epic-15; family claude model claude-opus-5 effort high standard context; PID547090; START2026-09-09T16:33:42Z verified against process. Replay launcher bash /tmp/projects/singular/milestone-1/epic-15/launch.sh. Exact CLI: /run/current-system/sw/bin/claude --dangerously-skip-permissions --model 'claude-opus-5' --effort high, interactive /dev/tty. The explicit named Opus request overrides the standing below-milestone Claude restriction only for this seat.

Epic owner self-bootstraps isolated source worktrees and controls Grok ticket owners; milestone controls only epic owner. Grok uses grok --always-approve -m grok-4.6; GLM uses glm --approve with family glm, harness pi, provider zai, model glm-5.3-flash effort max; Sol audit uses codex --model gpt-5.6-sol --config model_reasoning_effort=high --dangerously-bypass-approvals-and-sandbox --cd <fresh-audit-worktree>. No native agents, no additional standing team, no GLM audit, no replacement staffing without operator authority. Keep /code/singular operator checkout read-only; #10 ownership preserved.

Recovery authority packet: .milestones/1/resume/epic15-brief.md and epic15-launch.sh (copies of live acknowledged brief/launcher). Recover in the same runtime; first verify existing pane/process to avoid duplicate owner. Descendant roster/readiness lives in epic owner's STATUS/map, not inferred from assignment. At this sweep only epic START is verified.

Telegram was explicitly requested for progress: tag singular-ms1-844, topic5442, notifications every600s from epic15 STATUS; notifier PID550332, launched by tmux run-shell independently of model turns. It sends notifications only, no Telegram inbound control is claimed. Stop notifier at terminal work end and close only this topic; do not disable sibling notifications. Runtime notifications/launch.sh and notify.pid record custody. Initial send and confirmed-owner send succeeded.

Wiki d9b79eaa62147cd8dce4ef280406287fdf7ffeb5 marks the first epic running while unreleased stories remain planned. Current import relocation receipt is move-import-verification.json. Parent outcome remains unaccepted; model/browser results will not be represented as ledger implementation.

## Current placement of the source import

Operator correction: #19 is now the first child of #16, where MPFS validators and existing Haskell transaction-building/devnet support are needed for Singular ledger implementation. Native parent19 is16; #19 depends on the accepted #15 release. #20 no longer depends on #19 and is the next planned task. #15 has six serial children #20-25 and releases model/contracts/scenarios and the docs simulator. #25 has no copied-code artifact obligation. #16 still depends on #15 and starts with #19; further implementation children remain for later decomposition. This supersedes earlier first-epic copy wording below.

Copy constraints are preserved: one source repository cardano-foundation/cardano-mpfs-onchain, onchain/ and offchain/ directories, existing locks unchanged, no compilation or test execution in #19, no factoring or dependency/path repair. The indexer and separate off-chain service are excluded. No code was copied or built by this metadata move.

Wiki 555168e6f9681bc21133b280b668224f8df3d034; native parent and dependency readback recorded in .milestones/1/move-import-verification.json. Existing board item, milestone and assignee are preserved. No team or implementation execution is commissioned. Source inspection of /code/singular shows a generic Lean model with Nat application datum and supplied authorization evidence; it does not establish the full address-handle recovery/quorum model. Recommendation to operator: sequential ticket owners complete and verify each naming Lean slice before its matching simulator, with independent acceptance review. No new model completeness or proof acceptance is claimed.

## Prior first-epic decomposition, superseded by import relocation

Only epic #15 is decomposed: #19 source import, #20 naming claim simulator, #21 payment maintenance and pending withdrawal, #22 next-key recovery, #23 owner-or-quorum permanent retirement, #24 concrete registry and cardano-keri scenarios, #25 independently verified simulator/contract release. They are native subissues in serial dependency order, milestone1, Planning Other/Personal with Status unset. Epics16-18 retain their scopes and are not decomposed.

Superseded import placement (now first task of #16): copy MPFS validators into onchain/ and its existing haskell/ transaction-building, node-access and devnet test support into offchain/. The single source is cardano-foundation/cardano-mpfs-onchain (planning inspection 34a5bfbb8cca2cb1911b7060d0e61db28ba21e83). Do not copy the separate cardano-mpfs-offchain service. The indexer is explicitly excluded from M1. Preserve existing locks byte-for-byte, including root flake.lock, record original paths and source mapping; no invented component lock. No compilation, test execution, factoring, lock regeneration, dependency upgrade or path repair during the initial copy. Inherited relative paths affected by directory placement are recorded for later adaptation. Earlier two-repository import wording is superseded by the direct operator clarification.

The current generic docs simulator allows Delete and is not the M1 naming simulator. #20-25 deliver the naming-specific docs simulation and concrete consumer scenarios. Naming forbids Delete/reuse while generic registry behavior remains separate. Each epic is its own versioned release; source/model simulation never establishes ledger acceptance. Scalus14 remains deferred outside M1. Preserve operator-owned10.

Wiki publication: 561bbcafe3a4da8d487a6b71de8056d92c28fb1d; 21 stories in 6 groups, explicit simulator versus on-chain stories. Metadata receipt: .milestones/1/epic-15-expansion-verification.json. This is metadata/planning only; no source import, implementation, compilation, tests, devnet execution, proof execution, tags or implementation dispatch. Milestone outcome remains unaccepted.

Home: lambdasistemi/singular. Product: a permissionless registry on Cardano for unique identities and independent application state.

Operator-appointed project owner: pane %706, 0-projects/singular, runtime /tmp/projects/singular. The governed work session is mpfs-onchain-ms2. Singular is an independent product using MPF; shared MPFS code extraction is not an M1 prerequisite.

## Milestone map

M1: On-chain Singular support for cardano-keri. Planning complete, metadata only; milestone outcome open and no implementation lanes dispatched. Owner: visible milestone owner in mpfs-onchain-ms2, runtime /tmp/projects/singular/milestone-1. GitHub milestone 1 and epic URLs are bound below; founding and initial wiki publication accepted by the project owner.

Outcome: implement and verify the on-chain Singular registry, policies and consumer-facing contracts sufficiently for cardano-keri. A named consumer interface and executable on-chain conformance evidence are required. The agreed naming application is a concrete demonstration/test application; it does not replace cardano-keri acceptance.

The operator has bounded minimal off-chain support to transaction building and devnet verification; the indexer is excluded. No production SDK, indexer, resolver service, wallet integration, backend or full user-facing app is committed by M1. Minimal transaction fixtures/harnesses necessary to validate on-chain contracts are verification support, not an adopted off-chain product.

M2 candidate: factoring common MPFS code. This is a possible later milestone, not founded, active, approved extraction work or an M1 blocker.

Priorities: concrete registry and cardano-keri contracts, on-chain implementation and proofs/conformance, then consumer readiness and naming contract demonstration. Existing issue10 remains operator-owned Lean work; observe live state and do not infer proof acceptance from closure. New theorem/model scope must be coordinated separately.

PR3 merged at b23f1f7c4fb9628971208d5ec80e6b647c5bacbf; its model/simulator/docs are bounded prior evidence, not the implemented milestone outcome. The old MPFS project's commitments remain separately owned.

Next planned work is concrete registry/cardano-keri contract design #15. D-014 defers Scalus experiment #14 outside M1 and removes its blocking edge to #15. Existing bounded Haskell interoperability criteria remain for possible later reconsideration; no future date/milestone, execution or replacement implementation language is selected.

## Founding publication binding

GitHub milestone 1: https://github.com/lambdasistemi/singular/milestone/1. Deferred outside M1: https://github.com/lambdasistemi/singular/issues/14, open with its existing bounded Haskell interoperability criteria; no dependency on it remains in the active milestone map. Ordered epics: https://github.com/lambdasistemi/singular/issues/15, https://github.com/lambdasistemi/singular/issues/16, https://github.com/lambdasistemi/singular/issues/17 and https://github.com/lambdasistemi/singular/issues/18. Board: https://github.com/users/paolino/projects/2. Existing operator-owned https://github.com/lambdasistemi/singular/issues/10 is mapped as foundation, without any inference of proof acceptance.

This snapshot binds the founding artifacts. Execution remains uncommissioned. Wiki publication is complete: https://github.com/lambdasistemi/singular/wiki/Milestone-1, commit 29300629eff46cd2b0588c53cfb16637d2e2836d. The historical pending projection remains in .milestones/1/wiki-pending. Parent review remains distinct from this setup handback. See .milestones/1/ledger.md and the runtime handoffs/founded.md for final evidence and exact commit identity.

Project D-014 is incorporated from the current parent snapshot: Scalus is postponed for the current assurance level, not executed or technically disproved. M1 on-chain outcomes, off-chain-open boundary and possible future MPFS factoring remain unchanged.

## Vertical release map

Each epic is a release inside M1, not a horizontal library layer. Every release includes a retrievable versioned runnable artifact, exact source/script/contract identities, replay instructions, its own usable CI/release path, independent boundary acceptance and explicit measured/unverified limits. Successors consume the prior accepted release and preserve its runnable journeys. No implementation, release tag or language selection is authorized by planning.

### #15: Executable contract and scenario release

An integrator obtains this release, runs its contract/scenario entry point and observes accepted transaction shapes and rejected substitutions against the frozen interface.

Release artifact: a versioned, installable executable contract/model scenario package with the agreed consumer interface, canonical construction and encoding specifications, positive/negative scenarios, proof/audit evidence and replay instructions. It starts from the existing operator-owned generic foundation after scope coordination; it does not depend on deferred experiment #14. This is the design-first release. It establishes stable/proved contracts before on-chain implementation; it does not claim compiled validator deployment or consumer production readiness.

### #16: Claim and maintenance on-chain release

An integrator obtains this release and runs the bounded claim/maintenance journey: claim an absent name, reject a duplicate, observe the exact NFT/checkpoint, maintain its optional destination and exercise the settled withdrawal/refund behavior.

Release artifact: a versioned on-chain script/policy and contract bundle with test-only transaction construction and replay support for certified claim, permissionless fold, authenticated application state, authorized unfulfilled Insert withdrawal and optional address set/replace/clear. It extends the accepted executable contract/scenario release from #15 and carries its contract version and compatibility evidence. This release must be runnable and independently verifiable without waiting for recovery/retirement or final milestone assembly. Consumer ABI compatibility required for these operations and negative cases is checked here; final full consumer acceptance remains the later release.

### #17: Recovery and permanent retirement on-chain release

An integrator starts with a claimed name from the previous release, recovers after losing the everyday key, retires through either authorized route, and observes the permanent refusal of resolution/re-registration after Over.

Release artifact: a versioned extension of the claim/maintenance on-chain bundle and test-only fixtures covering precommitted control rotation, current-controller OR fixed-quorum permanent retirement, pending custody and completed Over. It extends the accepted claim/maintenance release from #16 while retaining that runnable journey and its contract compatibility. This release includes its own executable negative cases for wrong keys, insufficient quorum, quorum-only redirection, replay, withdrawal of retirement and forbidden naming release/reuse. It must be reproducible before the final milestone acceptance release.

### #18: Independently accepted consumer-conformance release

A cardano-keri integrator obtains the release and reproduces the exact agreed consumer transaction shapes and rejection cases, plus the complete bounded naming contract demonstration, from its published commands and artifacts.

Release artifact: the consolidated, installable on-chain release with exact cardano-keri consumer-contract bindings, reproducible conformance fixtures, the bounded naming journey, measured limits and independent outcome-review evidence. It integrates the accepted release from #17 and the preserved contract/claim releases from #15/#16; earlier epics must already have delivered their own releases. This release adds end-to-end on-chain consumer conformance and independent milestone outcome acceptance to the already runnable earlier releases. It cannot substitute a browser model or naming-only demo for consumer evidence. Milestone closure remains a separate owner decision based on this outcome evidence.

